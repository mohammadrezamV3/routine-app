<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- برگشت از پرداخت (mobile/README-billing.md) — هیچ پارامتری از این
                 دیپ‌لینک باور نمی‌شه؛ فقط محرکِ چک‌کردنِ وضعیتِ چک‌اوتِ محلیه. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="arion" android:host="payment-result" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- یادآوری‌های محلی (@capacitor/local-notifications) -->
    <!-- اندروید ۱۳+ (API 33): بدونش دیالوگِ اجازه‌ی نوتیف اصلا نشون داده نمی‌شه -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- اندروید ۱۲+ (API 31): برای زمان‌بندیِ دقیقِ alarm (setExactAndAllowWhileIdle).
         یک اجازه‌ی «special access» است که کاربر باید از تنظیماتِ سیستم بده؛
         عمدا از USE_EXACT_ALARM استفاده نشده چون طبقِ سیاستِ Play، آن فقط برای
         اپ‌هایی مجازه که کارِ اصلی‌شون آلارم/تقویمه (Arion نیست) — بدونِ این
         اجازه هم پلاگین/سیستم به‌صورتِ inexact زمان‌بندی می‌کنه، فقط با کمی
         تاخیرِ احتمالی در Doze، که برای یادآوریِ نرم مشکلی نداره. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
</manifest>
