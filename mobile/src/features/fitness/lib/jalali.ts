// این ماژول فقط یک re-export از هلپر مشترکِ جلالی/ارقام است (`@m/lib/jalali`)
// تا یک نسخه‌ی واحد از منطق تاریخ/ارقام در کل اپ موبایل وجود داشته باشه —
// قبلا اینجا یک کپیِ کامل نگه‌داری می‌شد که با تغییر نسخه‌ی اصلی هماهنگ نبود.
export * from "@m/lib/jalali";
