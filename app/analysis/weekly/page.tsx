// /analysis/weekly — آدرس جدید «آنالیز هفتگی» (منو به این‌جا لینک می‌ده و
// next.config.js هم /report/weekly* رو به این‌جا ریدایرکت می‌کنه). تا وقتی UI
// جدید آنالیز (lib/weeklyAnalysis/types.ts) ساخته نشده، همون صفحه‌ی گزارش
// هفتگی موجود این‌جا سرو می‌شه — قبلا این مسیر اصلا صفحه نداشت و ۴۰۴ می‌داد.
export { default } from "@/app/report/weekly/page";
