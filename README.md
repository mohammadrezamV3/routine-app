# Arion — نسخه Next.js

## نسخه: v1.118.0 (بازبینیِ بزرگِ روتین/بدنسازی/کالری/پنل کاربری + یادآوری دارو)

**فیچرِ جدید — یادآوری دارو**: کارتِ «یادآوری دارو» توی داشبوردِ روتین. اسمِ
دارو، تعدادِ دفعات در روز (۴ بار = هر ۶ ساعت)، ساعتِ اولین نوبت و طولِ دوره رو
می‌گیره و سرِ هر نوبت اعلان می‌ده؛ ویرایش/حذف از منوی سه‌نقطه‌ی کنارِ اسمِ دارو،
و کلِ بخش از «تنظیمات» قابلِ خاموش‌شدنه. داده مثلِ بقیه‌ی روتین روی همون
UserSettingِ کلید/مقدار می‌شینه (مهمان → localStorage، کاربر → دیتابیس).

**برنامه هفتگی**
- آکاردئونِ روزها و ردیف‌های فرمِ «افزودن برنامه» دیگه انیمیشنِ `height:auto`
  ندارن (منشأ اصلیِ سنگینی)؛ حذفِ `overflow:hidden` هاله‌ی روزهای اول/آخر
  (شنبه/جمعه) رو هم درست کرد.
- نشانگرِ SegmentedTabs با transitionِ CSS حرکت می‌کنه، نه لوپِ جاوااسکریپت —
  لگِ سوییچِ تب‌ها در کلِ اپ (اهمیت، هفتگی/ماهانه‌ی کالری، وعده‌ها، …).
- نوارِ روزها عرضِ واقعیِ پیل رو اندازه می‌گیره (روی آیفون ۱۲ پرو فقط سه روز
  نشون می‌داد) و با انگشت سوایپ می‌شه به گذشته/آینده.
- اعتبارسنجیِ «ساعت پایان بعد از ساعت شروع»، دکمه‌ی ثبتِ متن‌دار با لودینگ،
  نکته‌ی اعلانِ زیرِ «میزان اهمیت»، هم‌ردیف‌شدنِ سه‌نقطه، و زنگوله‌ی خاموشِ
  بدونِ بک‌گراند.
- فرمتِ تاریخ به سال/ماه/روز نوشته می‌شه تا خواندن از راست «روز، ماه، سال» بشه.

**ورودی‌های عددی**: کامپوننتِ مشترکِ `NumberInput` جای همه‌ی `type="number"`ها —
ارقامِ فارسی/عربی همون لحظه‌ی تایپ به انگلیسی تبدیل می‌شن (قبلاً مرورگر ورودیِ
فارسی رو بی‌صدا دور می‌ریخت).

**برنامه تمرینی**
- رفعِ یک **حلقه‌ی رندرِ بی‌پایان** در `ExerciseTaskList` (منشأ داغ‌شدنِ گوشی
  موقعِ سوییچِ روزها)، و حذفِ انیمیشنِ FLIPِ گرید (لگِ «شروع تمرین»).
- دایره‌ی ستِ فعلی موج می‌ده، دکمه‌ی توقف با «رد کردن استراحت» عوض شد، و
  «پایان تمرین» با حرکتِ ناتمام حالا «ادامه تمرین» پیشنهاد می‌ده.
- پرامپتِ ساختِ برنامه با نسخه‌ی حرفه‌ای جایگزین شد (بدونِ سقفِ ساختگیِ
  «۲ تا ۵ حرکت»، با تعیینِ تقسیمِ هفتگی/حجم/شدت/ریکاوری).
- مرحله‌ی قوانین: تایتلِ سبز، یک باکسِ واحد، تایمرِ ۳۰ثانیه‌ای قبل از فعال‌شدنِ
  ثبت، و دکمه‌ی بک‌گراند‌دارِ چپ‌چین با اسپینرِ «در حال ساخت برنامه».

**کالری‌شمار**
- **اصلاحِ محاسبه**: هدفِ روزانه دیگه زیرِ BMR نمی‌ره و کفِ ایمنِ ۱۲۰۰/۱۵۰۰
  کالری داره؛ نبودِ برنامه‌ی ورزشی هم دیگه «۳ روز باشگاه» فرض نمی‌شه.
- نمودار: حالتِ «روزانه» حذف شد، هفتگی و ماهانه یک دیزاینِ واحدِ کلیک‌پذیر
  گرفتن با برچسب‌های خوانا و دکمه‌ی سوییچِ هم‌ردیفِ تایتل.
- استریک هفت روزِ کامل، سه باکس در هر ردیفِ «کالری هر وعده»، فرمِ دستیِ
  بدونِ لگ با چیدمانِ سه‌نقطه → اسمِ وعده → کالری، و **هدفِ درشت‌مغذی‌ها**.
- فرمِ افزودن غذا: برچسبِ بالای همه‌ی فیلدها، هم‌عرض/هم‌ارتفاع شدنِ «مقدار» و
  «واحد اندازه‌گیری»، و حذفِ اسکرول‌بارِ دراپ‌داون.

**پنل کاربری**
- **دستگاه‌های فعال (واقعی)** با امکانِ بیرون‌انداختنِ یک دستگاه یا همه‌ی
  دستگاه‌های دیگر — هر توکن یک `sid` داره که به یک ردیفِ `Session` وصله.
- **ورودِ دومرحله‌ای با پیامک** — با روشن‌بودنش، بدونِ کدِ پیامکی نشستی صادر
  نمی‌شه و مسیرِ معمولِ ورود برای اون حساب سمتِ سرور بسته‌ست.
- رفعِ نمایشِ **اشتراکِ اشتباه** (اشتراکِ منقضی به‌عنوانِ پلنِ فعلی نشون داده می‌شد).
- «عمومی» → «تنظیمات»؛ تنظیماتِ روتین و ترید از پشتِ ناوبریِ تودرتو بیرون
  اومدن، بخش‌های خالی حذف شدن، «قابل‌جست‌وجو بودن» رفت به امنیت، و تغییرِ
  یوزرنیم فقط از پروفایل ممکنه.
- دکمه‌ی بازگشت در همه‌ی زیرصفحه‌ها، کارتِ سرصفحه‌ی بازچینش‌شده، و بهینه‌سازیِ
  ناوبری (حذفِ انتظارِ انیمیشنِ خروج، خاموش‌کردنِ `:hover`ِ چسبنده روی لمسی،
  حذفِ فچِ تکراریِ عکسِ پروفایل).

**اشتراک‌ها**: قابلیت‌های نسخه‌ی رایگان روی کارت، تیترِ راست‌چینِ «تمدید اشتراک
فعلی» با باکسِ تیک‌دار، و «خرید این پلن» به‌جای «خرید اشتراک».

## نسخه: v1.117.0 (فیکسِ واقعیِ استریک، حذفِ فونتِ دوم، حذفِ اسکرول‌بارِ صفحه)

- **استریکِ روتین همیشه صفر می‌موند**: هم توی هدر (`useMyStreak`) هم کارتِ دوستان (`friendStats.ts`) هم تقویمِ تاریخچه (`HistoryCalendar`)، «روزِ کامل» شرطِ AND با «بیدارشدنِ دقیقاً سرِ ساعتِ هدف» داشت — یعنی برایِ کاربری که این فیچرِ جدا/اختیاری رو دنبال نمی‌کرد، استریک با اینکه ۱۰۰٪ برنامه‌ش رو انجام می‌داد هیچ‌وقت بالا نمی‌رفت. حالا «روزِ کامل» فقط یعنی همه‌ی برنامه‌های اون روز انجام شده.
- **فونتِ فارسیِ دوم (IBM Plex Sans Arabic) کاملاً حذف شد** — همه‌جای سایت (اعداد/تاریخ/متن‌های فارسی) از همون فونتِ Vazirmatn استفاده می‌کنه که «روتین من» باهاش نوشته می‌شه؛ نه یه فونتِ دومِ متفاوت‌تر. فونت از layout.tsx هم حذف شد (کمتر دانلود).
- اسکرول‌بارِ سراسریِ صفحه (نه فقط داخلِ پاپ‌آپ‌ها) دیگه هیچ‌جا نشون داده نمی‌شه.

## نسخه: v1.116.0 (کدرترشدنِ پاپ‌آپ‌ها روی موبایل + چند فیکسِ دیگه)

- **کدرترشدنِ هدر/منوها/پاپ‌آپ‌ها روی موبایل**: `html[data-perf="low"]` (فیکسِ لگِ اسکرول) بدونِ fallback بلور رو از هدر و خیلی از پاپ‌آپ‌ها هم برمی‌داشت — یعنی روی موبایل تقریباً کاملاً شفاف می‌موندن. حالا برای `.app-topbar`، `.modal-panel.liquid-glass-panel` (راهنماهای اولیه + مودال‌های کالری/تمرین)، `.exercise-catalog-popup-panel`/`.exercise-catalog-detail-card` («مشاهده حرکات»)، و `.wsearch-newform` در تمِ روشن («افزودن برنامه» و مشابه‌ها) — یه بک‌گراندِ تقریباً توپر (نه بلور) اضافه شد: هزینه‌ی هر فریم صفره ولی دیگه پشتشون دیده نمی‌شه.
- **فیکسِ لگِ «مشاهده حرکات»**: لیستِ ۱۴۹تاییِ حرکات هرکدوم یه انیمیشنِ ورودِ جداگانه داشتن که هم‌زمان با بازشدنِ خودِ پاپ‌آپ اجرا می‌شدن — حذف شد.
- **راهنمای اولیه‌ی کالری**: فقط برایِ کاربرِ لاگین‌کرده فعال می‌شه (قبلاً چکِ وضعیتِ ورود نداشت).
- **ستاره‌ی دومِ آیکونِ AI**: از لبه‌ی viewBox بیرون می‌زد و کلیپ می‌شد — پوزیشنش درست شد و یه انیمیشنِ درخشیدنِ ملایمِ پیوسته گرفت.
- **تغییرِ نام**: «برنامه غذایی» → «کالری‌شمار» توی منو و همه‌جای دیگه.
- **دیزاینِ نمودارِ کالری (هفتگی)** با نمودارِ هفتگیِ «روتین من» یکی شد.
- نوارِ قیمت‌های بازار از بالای صفحه‌ی هابِ ترید حذف شد.

## نسخه: v1.115.0 (لوگوی جدیدِ تمِ روز — بهینه‌شده و سبک‌تر)

آیکون/لوگوتایپِ جدید (طلایی/برنزی) فقط برای تمِ روز جایگزین شد؛ تمِ شب دست‌نخورده موند.
از عکسِ مرجع کراپ و پس‌زمینه‌ش شفاف شد (بدونِ هیچ حاشیه‌ی سفید)، بعد با اندازه‌ی واقعیِ
نمایش (نه رزولوشنِ الکی‌بزرگ) و پالتِ محدود دوباره export شد — حجمِ آیکون از ۱۳ کیلوبایت
به ۳.۸ کیلوبایت و لوگوتایپِ کاملِ نوار بالا از ۱۶.۵ به ۹.۳ کیلوبایت رسید.

`components/AuthChrome.tsx` (آیکونِ صفحه‌های ورود/ثبت‌نام) قبلاً تم‌آگاه نبود — همیشه
همون آیکونِ سبزِ تمِ شب رو نشون می‌داد، حتی توی تمِ روز. حالا مثلِ لوگوتایپِ نوارِ بالا
(`NavDrawer.tsx`) بینِ `logo-icon-light-theme.png`/`logo-icon-dark-theme.png` سوییچ می‌کنه.

## نسخه: v1.114.0 (چارتِ محورِ‌زمانِ سشن‌های فارکس، به‌وقتِ ایران)

توی صفحه‌ی «ساعتِ فارکس» (`/trade/clock`) — `components/ForexSessionsTimeline.tsx`
چهار سشنِ سیدنی/توکیو/لندن/نیویورک رو رویِ یه محورِ ۲۴ساعته‌ی مشترکِ «وقتِ ایران» کنارِ هم
می‌ذاره، با یه خطِ عمودیِ زنده برایِ موقعیتِ همین‌الان (هر ۳۰ ثانیه آپدیت می‌شه). مکملِ
`ForexClockPanel`ِ موجود (که ساعتِ محلیِ هر شهر رو جدا نشون می‌ده)، نه جایگزینش.

بازه‌ی باز/بسته‌ی هر سشن از همون منبعِ واحدِ `lib/forexSessions.ts` میاد (تایم‌زونِ IANA
واقعیِ هر شهر، نه عددِ ثابتِ UTC) — همون چیزی که برچسبِ جلسه‌ی هر معامله در ژورنال هم ازش
میاد، پس هیچ‌وقت با هم فرق نمی‌کنن. فقط معکوسِ `wallClockIn` لازم بود: تبدیلِ «امروز، فلان
دقیقه به‌وقتِ محلیِ شهرِ X» به یه لحظه‌ی واقعی، تا بشه به‌وقتِ ایران خوندش.

## نسخه: v1.113.0 (دکمه‌ها فوراً واکنش نشان می‌دهند و دیگر بی‌صدا شکست نمی‌خورند)

شکایت: «دکمه‌ها خیلی طول می‌کشند تا عمل کنند و همش انگار باگ می‌خورد.»
ریشه‌اش یک چیز نبود، پنج چیز بود — و هیچ‌کدام کندیِ سرور نبود:

۱. **هیچ بازخوردی بینِ کلیک تا پاسخ نبود.** آرشیو/حذف/کپی/سنجاق فقط
   `await fetch` بودند و تا برگشتنِ پاسخ روی صفحه هیچ اتفاقی نمی‌افتاد؛ کاربر
   فکر می‌کرد کلیکش نگرفته و دوباره می‌زد. حالا `lib/useAsyncAction.ts` همان
   دکمه را قفل و چرخان می‌کند.

۲. **کلیدواژه‌ی `.trade-spin` تعریف نشده بود.** آیکون‌های Loader2 اصلاً
   نمی‌چرخیدند. `@keyframes tradeSpin` اضافه شد.

۳. **هر تغییر، لیست را با اسکلت جایگزین می‌کرد.** `load()` بعد از هر
   عملیات `loading=true` می‌گذاشت و کلِ محتوا یک لحظه می‌پرید. حالا
   `load(silent)` داریم و رفرشِ بعد از تغییر، لیست را سرِ جایش نگه می‌دارد.
   در تقویم اقتصادی هم تغییرِ فیلتر دیگر لیست را خالی نمی‌کند.

۴. **شکستِ درخواست کاملاً بی‌صدا بود.** پاسخ‌ها نادیده گرفته می‌شدند؛ یک
   خطای ۵۰۰ یعنی دکمه‌ای که هیچ کاری نمی‌کند. حالا پیامِ خطا دیده می‌شود و
   حذفِ خوش‌بینانه در صورتِ شکست برمی‌گردد.

۵. **قطعِ شبکه دکمه را برای همیشه قفل می‌کرد.** در فرمِ معامله، فرمِ حساب و
   ساختِ برچسب، `fetch` بدونِ `try/catch` بود؛ با قطعِ شبکه استثنا بیرون
   می‌زد و `setSaving(false)` هرگز اجرا نمی‌شد. حالا هر سه `finally` دارند.

به‌علاوه: دکمه‌ی «ویرایش» در جزئیاتِ معامله، معامله را دوباره از سرور
نمی‌گیرد — همان داده‌ای که درایور از قبل لود کرده به فرم داده می‌شود (یک
رفت‌وبرگشتِ شبکه کمتر، فرم فوراً باز می‌شود). «بررسی اتصالِ» متاتریدر هم
اسپینر گرفت.

راستی‌آزمایی با مرورگرِ واقعی و سرورِ عمداً کُند (۲.۵ ثانیه): اسپینر بلافاصله
ظاهر می‌شود، کلیکِ دوم درخواستِ تکراری نمی‌فرستد، اسکلتی نمی‌پرد، خطای ۵۰۰ و
قطعِ شبکه هر دو پیام می‌دهند و دکمه دوباره فعال می‌شود، و کلیکِ «ویرایش» هیچ
درخواستِ APIای نمی‌زند.

## نسخه: v1.112.0 (رفعِ علتِ واقعیِ نرسیدنِ پیامک + تشخیصِ migrationِ اجرانشده)

از لاگِ سرورِ production دو مشکلِ کاملاً جدا بیرون آمد.

### ۱. آدرسِ اندپوینتِ ملی‌پیامک اشتباه بود

```
[sms] خطا در ارسال پیامک به 090*****548: HTTP 404
```

`lib/sms.ts` به `/api/SendSMS/BaseNumber` درخواست می‌داد، ولی مسیرِ درستِ
«ارسال با الگو» در REST APIِ ملی‌پیامک **`BaseServiceNumber`** است. سرور
با ۴۰۴ جواب می‌داد و چون ارسال fire-and-forget است، این شکست هیچ‌جا دیده
نمی‌شد — از بیرون فقط «پیامک نمی‌آید» به‌نظر می‌رسید.

مسیر با SDKِ رسمیِ خودِ ملی‌پیامک تطبیق داده شد
(`melipayamak-python/melipayamak/sms/rest.py`، متدِ `send_by_base_number`).
پروبِ عیب‌یابی هم از همان مرجع `GetCredit` را می‌زند.

### ۲. migration روی production اجرا نشده بود

لاگ پر بود از `The table public.TradeAccount does not exist`. علتش این است
که سرویسِ `migrate` در `docker-compose.yml` پروفایلِ `tools` دارد و با
`docker compose up` معمولی اجرا **نمی‌شود** — و `healthcheck` هم سبز
می‌ماند چون `/api/health` عمداً به دیتابیس دست نمی‌زند. یعنی اپ با کلاینتِ
تازه بالا می‌آید و دیتابیس اسکیمای قدیمی دارد، بدونِ هیچ نشانه‌ی بیرونی.

`/api/admin/diagnostics/outbound` حالا این را صریح گزارش می‌کند:

```json
"schema": {
  "ok": false,
  "note": "migration اجرا نشده — دیتابیس از کد عقب است",
  "fix": "docker compose --profile tools run --rm migrate npx prisma migrate deploy",
  "missingTable": "public.TradeAccount"
}
```

**تست‌شده روی دو دیتابیسِ واقعی:** یکی که عمداً migrationهای جدید را ندارد
(شبیه‌سازیِ دقیقِ وضعیتِ سرور) → همان خروجیِ بالا با دستورِ رفع؛ و بعد از
اجرای migration روی همان دیتابیس → `schema.ok: true`.

## نسخه: v1.111.1 (سقفِ زمان برای پیامک + سنجه‌های تشخیصِ کندی)

گزارشِ «سایت دیر جواب می‌دهد و گاهی اصلاً برنمی‌گرداند» بررسی شد. مسیرهای
پرتکرار ممیزی شدند و **در آن‌ها علتی پیدا نشد**:

- `layout` ریشه فقط JWT را decode می‌کند (کالبکِ `session` هیچ کوئریِ
  دیتابیسی ندارد) — پس رندرِ هر صفحه هزینه‌ی دیتابیسی ندارد.
- `UserSetting` و `DailyEntry` هر دو ایندکسِ `(userId, key)` و
  `(userId, date)` دارند.
- پروکسیِ قیمتِ بازار موازی است، سقفِ ۵ ثانیه و کشِ ۳۰ ثانیه دارد و فقط
  روی صفحه‌ی ترید صدا زده می‌شود.
- گیت‌وی AI (۴۵s) و زیبال و زرین‌پال (۱۲s) از قبل سقفِ زمان داشتند.

**تنها شکافِ واقعی:** `lib/sms.ts` تنها فراخوانیِ خروجیِ اپ بود که سقفِ زمان
نداشت. چون ارسال `fire-and-forget` است، یک ملی‌پیامکِ بی‌جواب یعنی یک
Promiseِ معلق که تا تایم‌اوتِ TCPِ سیستم‌عامل (چند دقیقه) حافظه و سوکت نگه
می‌دارد. سقفِ ۱۵ ثانیه گذاشته شد.

**برای اینکه «کُندی» عدد شود** (نه حدس)، `/api/admin/diagnostics/outbound`
حالا سه سنجه‌ی تازه هم برمی‌گرداند:

| سنجه | اگر بالا بود یعنی |
|---|---|
| `db.ms` | مشکل سمتِ Postgres یا استخرِ اتصالِ Prisma است |
| `eventLoopLagMs` | پروسه تحتِ فشارِ CPU/حافظه است (GC thrash) |
| `process.rssMb` در برابر `heapLimitMb` | نزدیکِ سقفِ heap و در آستانه‌ی OOM |

اگر هر سه پایین بودند ولی `outbound` کُند بود، مشکل شبکه‌ی خروجیِ سرور است.

## نسخه: v1.111.0 (شکستِ ارسال پیامک دیگر بی‌صدا نیست + پروبِ ملی‌پیامک)

**مسئله:** گزارش شد «کد پیامکی قبلاً ارسال می‌شد، الان نمی‌شود». علتش در
کد پیدا نشد چون ارسالِ پیامک عمداً `fire-and-forget` است
(`void sendOtpSms(...)` تا کاربر پشتِ اسپینر نماند) و روتْ همیشه
`{ok:true}` برمی‌گرداند. یعنی **هر شکستی — نبودِ env، اتمامِ اعتبار،
غیرفعال‌شدنِ الگو، بسته‌بودنِ مسیرِ خروجی — فقط در stdoutِ کانتینر می‌نشست**
و نه کاربر می‌فهمید نه ادمین.

بدتر: اگر `MELIPAYAMAK_*` تنظیم نباشد، `sendOtpSms` عمداً
`{ok:true, simulated:true}` برمی‌گرداند تا در dev قابلِ تست باشد — روی
production همین یعنی «هیچ پیامکی نمی‌رود ولی همه‌چیز موفق به‌نظر می‌رسد».

**دو تغییر:**

۱. هر چهار حالتِ شکستِ ارسال حالا با `logError` در جدولِ خطا ثبت می‌شود و
   در `/admin/system/errors` دیده می‌شود. نبودِ env سطحِ `CRITICAL` می‌گیرد
   چون یعنی سرویس کاملاً از کار افتاده. شماره ماسک می‌شود
   (`0912***567`) تا شماره‌ی کاملِ کاربر در جدولِ خطا ننشیند.

۲. `/api/admin/diagnostics/outbound` حالا ملی‌پیامک را هم پروب می‌کند
   (اندپوینتِ اعتبار، نه ارسال — هیچ پیامکی نمی‌فرستد) و سه env مربوطه را
   به‌صورت «هست/نیست» گزارش می‌دهد. خروجی سه حالت را از هم جدا می‌کند:
   env تنظیم نشده · مسیرِ شبکه بسته است · پنل ارسال را رد کرد (با متنِ
   خودِ ملی‌پیامک، مثلاً اتمامِ اعتبار) — و در حالتِ سالم مقدارِ اعتبار را
   نشان می‌دهد.

**تست‌شده:** درخواستِ واقعیِ کد در محیطی بدونِ `MELIPAYAMAK_*` — پاسخ به
کاربر همان `{ok:true}` ماند (رفتار عوض نشد) ولی یک خطای `CRITICAL` با
شماره‌ی ماسک‌شده در جدولِ خطا ثبت شد، و پروب همان علت را گزارش کرد.

## نسخه: v1.110.1 (رفعِ دکمه‌ی دوکلیکی در فرم معامله)

**باگ:** انتخابِ نماد از لیستِ پیشنهاد روی موبایل با یک تپ کار نمی‌کرد.

**ریشه:** لیست با `onBlur` بسته می‌شد و آیتم‌ها `onMouseDown` داشتند. روی
صفحه‌ی لمسی ترتیبِ رویدادها فرق می‌کند: `blur` پیش از رسیدنِ کلیک اجرا
می‌شود، لیست بسته و آیتم از DOM حذف می‌شود، پس اولین تپ عملاً هدر می‌رفت و
کاربر باید دوباره می‌زد.

**رفع:** بستنِ لیست دیگر به `blur` گره نخورده — با `pointerdown`ِ بیرونِ
فیلد بسته می‌شود. خودِ آیتم‌ها هم از `<div onMouseDown>` به
`<button onPointerDown>` تبدیل شدند؛ `pointerdown` هم روی ماوس و هم روی لمس
*پیش از* blur اجرا می‌شود، پس انتخاب با همان اولین تپ ثبت می‌شود.

چون آیتم‌ها حالا `<button>`اند، ریستِ سراسریِ `button{}` (که بک‌گراندِ شیشه‌ای
می‌دهد) صریح خنثی شد تا ظاهرشان دقیقاً همان ردیفِ ساده‌ی قبل بماند — همان
قانونِ همیشگیِ پروژه درباره‌ی بک‌گراندِ ناخواسته.

**بررسیِ کامل:** با مرورگرِ واقعی و ویوپورتِ لمسیِ موبایل، ۱۲ مسیرِ اصلیِ
ماژول تست شد (کارت‌های هاب، ورود به حساب، ویرایش، افزودن معامله، تب‌های فرم،
انتخاب احساس، بستن فرم، نمایش آرشیو، تب‌های متاتریدر، ساخت کد اتصال، دکمه‌ی
«معامله»ی چک‌لیست، فیلترِ تقویم). هر ۱۲ مورد با **یک تپ** کار می‌کنند و
هیچ دکمه‌ی دوکلیکیِ دیگری باقی نمانده.

## نسخه: v1.110.0 (ترید: هم‌سان‌سازی کامل دیزاین با بقیه اپ + حذف زیرمنو + رفع کندی)

### دیزاین — دیگر زبانِ بصریِ جداگانه‌ای ندارد
بازنویسیِ قبلی از صفر استایل نوشته بود و برای همین «خام و HTML‌مانند» دیده
می‌شد. حالا هیچ الگوی تازه‌ای وجود ندارد؛ همان چیزهایی که بقیه‌ی اپ دارد:

| عنصر | قبلاً | حالا |
|---|---|---|
| فیلدها | بدونِ هیچ کلاسی (خامِ مرورگر) | `wsearch-newform-name` + `trade-glass-field` — همان فیلدِ کالری/بدنسازی/ثبت‌نام |
| کارت‌ها | فقط `border` تخت | شیشه‌ی `.rm-box`/`.account-card` (surface + blur 28 + گردیِ ۱۸px) |
| دکمه‌ی اصلی | بک‌گراندِ ساده | الگوی `.account-sub-cta-btn` (accent + `active:scale(.97)`) |
| آیکون‌ها | نداشت | دایره‌ی نرمِ `.account-row2-icon` |
| هاب | باکس‌های تخت | عیناً ردیفِ پنل کاربری: آیکون + عنوان + توضیح + شِوران |

۳۴ فیلد در ۷ کامپوننت کلاسِ استانداردِ اپ را گرفتند. سایه‌ها، گردی‌ها،
هاورها و انتقال‌های `.18s` همه از همان توکن‌های سراسری می‌آیند.

### منو — زیرمجموعه حذف شد
«ترید» دیگر گروهِ آکاردئونی نیست؛ یک لینکِ مستقیم است و با زدنش هابِ ترید
بالا می‌آید. انتخابِ بخش داخلِ خودِ صفحه انجام می‌شود.

### کندی و خشکی — سه علتِ واقعی پیدا و رفع شد
۱. **پرشِ «وارد شوید»**: تا وقتی `useSession()` هنوز `loading` بود، شرطِ
   `authenticated ? ... : <AuthGate/>` باعث می‌شد کاربرِ لاگین‌کرده یک لحظه
   پیامِ «وارد شوید» ببیند و بعد صفحه عوض شود. بخشِ بزرگی از حسِ «ناگهانی و
   سخت» همین بود. حالا در حالتِ loading اسکلتِ همیشگیِ اپ دیده می‌شود.
۲. **N+1 در صفحه‌ی متاتریدر**: به‌ازای هر حساب یک درخواستِ جدا می‌رفت (با ۱۰
   حساب، ۱۱ درخواست). وضعیتِ اتصال حالا در همان یک درخواستِ حساب‌ها می‌آید.
۳. **نبودِ حرکت**: محتوا بی‌مقدمه ظاهر می‌شد. حالا ورودِ مرحله‌ایِ
   framer-motion (همان منحنی و تأخیرِ `AccountSectionCard`) روی کارت‌های
   حساب/چک‌لیست/یادداشت/هاب، و ورودِ نرمِ کلِ صفحه.

به‌علاوه: `prefetch` روی لینک‌های هاب، پیش‌درخواستِ `/api/trade/checklists`،
و پوسته‌ی مشترکِ `TradePageShell` که هر شش زیرصفحه را یکدست می‌کند.

### تست‌شده
با مرورگرِ واقعی (Playwright، ویوپورتِ موبایل) از هر شش صفحه و از مودالِ
حساب و فرمِ تب‌دارِ معامله اسکرین‌شات گرفته و با بقیه‌ی اپ مقایسه شد؛ دو
ایرادِ چیدمانی که فقط در رندرِ واقعی دیده می‌شدند (چسبیدنِ عنوان و توضیحِ
هاب، و فاصله‌ی عمودیِ فیلدها در چیدمانِ ستونیِ موبایل) همان‌جا رفع شدند.
`npm run check`، تایپ‌چک، بیلد و هر ۳۶ تست سبز.

## نسخه: v1.109.0 (ترید فاز ۷ — اتصال متاتریدر، برای هر حساب جداگانه)

اتصال عمداً روی **خودِ حساب** می‌نشیند نه روی کاربر: هر کاربر ده‌ها حسابِ
معاملاتی دارد و هرکدام ترمینال و لاگینِ خودش را دارد، پس یک اتصالِ سراسری
معنی نداشت. پنلِ اتصال داخلِ صفحه‌ی همان حساب است؛ `/trade/metatrader` فقط
فهرستِ حساب‌ها و وضعیتِ اتصالشان را نشان می‌دهد.

### مدل امنیتی
- **رمزِ حسابِ معاملاتی هیچ‌وقت خواسته و ذخیره نمی‌شود.** ارتباط یک‌طرفه و
  خواندنی است: EA فقط داده می‌فرستد، Arion هیچ دستوری برنمی‌گرداند.
- کاربر در پنل یک **کدِ اتصالِ یک‌بارمصرف** می‌گیرد (۱۲ کاراکتر ≈ ۶۰ بیت
  آنتروپی، ۱۵ دقیقه اعتبار) و همان را در EA می‌گذارد. EA یک‌بار آن را
  می‌فرستد و در عوض توکنِ دائمی می‌گیرد؛ کد بلافاصله باطل می‌شود.
- نه کد و نه توکن خام ذخیره نمی‌شوند — فقط SHA-256شان. چون هر دو رشته‌ی
  تصادفیِ پرآنتروپی‌اند (نه رمزِ انتخابیِ انسان)، هشِ سریع درست است و برخلافِ
  bcrypt جست‌وجوی مستقیم با ایندکس را هم ممکن می‌کند.
- ابطال **فوری** اثر می‌کند، چون هر درخواست مستقیم روی دیتابیس چک می‌شود نه
  روی یک JWTِ امضاشده. گرفتنِ کدِ جدید هم اتصالِ قبلی را باطل می‌کند.
- ریت‌لیمیت روی هر دو اندپوینتِ EA؛ پیامِ خطای «کد اشتباه» و «کد منقضی» عمداً
  یکسان است تا به حدس‌زننده اطلاعات ندهد.

### همگام‌سازی
معاملات با کلیدِ یکتای `(accountId, externalId)` ثبت می‌شوند — در MT4 شماره‌ی
تیکت، در MT5 شناسه‌ی پوزیشن. پس اجرای دوباره‌ی sync (که در EA کاملاً عادی
است) هیچ‌وقت معامله‌ی تکراری نمی‌سازد.

**نکته‌ی مهم:** روی معامله‌ی همگام‌شده، فیلدهای دستیِ کاربر (احساسات،
چک‌لیست، برچسب، دلایل، عکس، یادداشت) در sync بعدی **دست نمی‌خورند** — فقط
اعداد به‌روز می‌شوند. وگرنه تحلیلی که کاربر نوشته با اولین sync پاک می‌شد.

### اکسپرت‌ها
`public/ea/Arion-MT4.mq4` و `public/ea/Arion-MT5.mq5` — سورس، چون فایلِ
کامپایل‌شده (`.ex4`/`.ex5`) را فقط خودِ MetaEditor می‌سازد. راهنمای داخلِ پنل
مرحله‌ی کامپایل (F7) را دارد.

⚠️ **این دو فایل روی متاتریدرِ واقعی تست نشده‌اند** — چنین محیطی این‌جا در
دسترس نبود. سمتِ سرور کامل تست شده (شبیه‌سازیِ EA با curl)، ولی خودِ MQL قبل
از تحویل به کاربر باید روی یک ترمینالِ واقعی امتحان شود.

⚠️ متاتریدر برای `WebRequest` الزاماً نیاز دارد کاربر دستی دامنه را در
Tools → Options → Expert Advisors اضافه کند. یعنی «Connect → Install → Done»
بدونِ هیچ تنظیمی ممکن نیست؛ این مرحله در راهنمای پنل صریح آمده است.

### تست‌شده روی سرور واقعی (شبیه‌سازی EA)
کدِ غلط → ۴۰۱ · کدِ درست → توکن · همان کد بارِ دوم → ۴۰۱ (یک‌بارمصرف) ·
ارسالِ ۳ معامله که یکی بدشکل بود → ۲ ساخته شد و بدشکل بی‌صدا رد شد ·
ارسالِ دوباره → `created:0, updated:1` (ضدتکرار) · جلسه‌ی معاملاتی خودکار ·
تحلیلِ دستیِ کاربر بعد از sync دوباره باقی ماند ولی عدد به‌روز شد ·
توکنِ باطل‌شده → ۴۰۱ · بدونِ توکن → ۴۰۱.

## نسخه: v1.108.0 (ترید فاز ۳ تا ۶ — تقویم اقتصادی، ساعت فارکس، یادداشت‌ها، هشدار خبر)

### تقویم اقتصادی
اپ **همیشه** از جدولِ خودمان (`EconomicEvent`) می‌خواند، هیچ‌وقت مستقیم از یک
سرویسِ بیرونی. سه دلیلِ عملی: CSPِ production اجازه‌ی تماسِ مرورگر با هاستِ
خارجی را نمی‌دهد؛ این ماژول نباید به در دسترس بودنِ یک سرویسِ خارجی گره
بخورد؛ و هیچ فیدِ رایگانی برای استفاده‌ی تجاری مجوزِ روشن ندارد.

پس تقویم از همین حالا و بدونِ هیچ وابستگیِ خارجی کار می‌کند: ادمین از
`/admin/economic-calendar` رویداد ثبت می‌کند. اگر بعداً فیدی خریداری شد، فقط
`ECONOMIC_CALENDAR_URL` (و در صورت نیاز `ECONOMIC_CALENDAR_API_KEY`) ست
می‌شود و کرانِ روزانه همان را نرمال‌سازی و upsert می‌کند — هیچ‌جای دیگرِ اپ
عوض نمی‌شود. رویدادهای دستی (`source=MANUAL`) از همگام‌سازیِ خودکار
دست‌نخورده می‌مانند چون کلیدِ یکتا شاملِ `source` است.

صفحه‌ی کاربر (`/trade/calendar`): تب‌های امروز/فردا/این هفته، فیلترِ ارز و
سطحِ تأثیر، و همه‌ی ساعت‌ها به وقتِ محلیِ خودِ کاربر (گروه‌بندیِ روزها هم بر
اساسِ روزِ محلی است، نه UTC — وگرنه رویدادِ ۰۳:۳۰ بامداد برای کاربرِ ایران در
گروهِ «دیروز» می‌افتاد).

### هشدار قبل از اخبار مهم
کرانِ `/api/cron/economic-alerts` (هر ۵ دقیقه) نوتیفِ Web Push می‌فرستد. هر
کاربر خودش تعیین می‌کند: روشن/خاموش، ۱۵ یا ۳۰ یا ۶۰ دقیقه قبل، کدام سطحِ
تأثیر، کدام ارزها. دو نکته‌ی مهندسی:
- هر رویداد فقط **یک‌بار** هشدار می‌گیرد (ردِ ارسال در یک کلیدِ
  سرور-مدیریتِ `tradeNewsAlertLog` که کاربر نمی‌تواند بنویسدش — وگرنه با
  پرکردنش می‌شد هشدارها را برای همیشه خاموش کرد).
- اگر ارسال شکست بخورد، رویداد در لاگ ثبت **نمی‌شود** تا اجرای بعدی دوباره
  تلاش کند، و شکستِ یک کاربر بقیه‌ی کاربرها را از اجرا نمی‌اندازد. (این
  دقیقاً باگی بود که تستِ انتها‌به‌انتها پیدا کرد: بدونِ کلیدهای VAPID، کلِ
  کران با ۵۰۰ می‌افتاد.)

### ساعت فارکس
`/trade/clock` — ساعتِ زنده‌ی سیدنی/توکیو/لندن/نیویورک، وضعیتِ باز/بسته،
همپوشانی‌های پرنقدینگی و وضعیتِ خودِ بازار (تعطیلیِ آخرِ هفته با ساعتِ
نیویورک حساب می‌شود، پس تمامِ سال درست می‌ماند). کلِ منطقِ ساعت از همان
`lib/forexSessions.ts` می‌آید که برچسبِ جلسه‌ی هر معامله را هم می‌سازد — پس
«چیزی که ساعت نشان می‌دهد» هیچ‌وقت با «چیزی که به معامله برچسب می‌خورد» فرق
نمی‌کند.

### یادداشت‌ها
`/trade/notes` با مدلِ سبکِ مستقلِ `TradeNote` — عمداً نه `NotepadPage`/
`NotepadBlock`، چون نیازِ این‌جا «یادداشتِ کوتاه با برچسب و رنگ و جست‌وجو»ست
نه ویرایشگرِ بلاکیِ تودرتو. برچسب‌ها همان برچسب‌های ماژولِ ترید هستند (مشترک
با حساب و معامله)، تا کاربر مجبور نباشد «Psychology» را دو بار بسازد.
جست‌وجو با تأخیر، فیلترِ برچسب، سنجاق‌کردن، و اتصالِ اختیاری به حساب/معامله.

### تست‌شده روی سرور واقعی
ثبتِ رویداد از پنلِ ادمین، فیلترِ ارز و سطحِ تأثیر، ساخت و جست‌وجوی یادداشت،
فیلترِ برچسب، ۴۰۱ کرانِ بدونِ رمز، رد شدنِ بی‌صدای همگام‌سازی وقتی منبعی
تنظیم نشده، و هشدارِ خبری: از دو رویداد (HIGH و LOW) فقط HIGH فرستاده شد و
اجرای دومِ کران صفر داد (ضدتکرار).

## نسخه: v1.107.0 (بازنویسی ماژول ترید — فاز ۱ و ۲: حساب‌های معاملاتی + چک‌لیست چندگانه)

ماژول ترید از یک «دفترچه‌ی تخت» به یک سیستمِ حساب‌محور بازنویسی شد. تا پیش از
این همه‌ی معاملاتِ کاربر در یک لیستِ واحد بودند و چک‌لیست هم فقط **یک** لیستِ
ثابت برای هر کاربر بود.

### چه چیزی عوض شد
- **حساب معاملاتی** (`TradeAccount`): هر کاربر تا ۱۰ حسابِ فعال (ریل/دمو/پراپ/
  بک‌تست) با ارز، بالانس اولیه، اهرم، رنگ، برچسب و **هدف سودِ مخصوصِ خودش**
  (مبلغ یا درصدِ بالانس). «حذف» یعنی آرشیو — تاریخچه‌ی معاملات هیچ‌وقت با یک
  کلیک از بین نمی‌رود؛ حذفِ کامل پشتِ تایپِ نامِ حساب قفل است.
- **معامله**: از ۱۳ فیلد به یک رکوردِ ساختاریافته رسید — دلایلِ ورود/خروج،
  احساسِ قبل/بعد، میزان اطمینان، «طبق پلن بودم؟»، ستاپ، برچسب، تا ۴ تصویر، و
  وضعیت (باز/بسته/لغو).
- **ساعتِ واقعیِ معامله**: فرمِ قبلی همه‌ی معاملات را ساعتِ ۱۲:۰۰ ثبت می‌کرد،
  یعنی آمارِ جلسه‌ی معاملاتی اصلاً ممکن نبود. حالا زمان تا دقیقه ثبت می‌شود و
  **جلسه‌ی معاملاتی سمتِ سرور و با DSTِ واقعیِ هر شهر** محاسبه می‌شود
  (`lib/forexSessions.ts` + ۹ تستِ واحد). همپوشانی‌ها (لندن×نیویورک) هر دو
  ثبت می‌شوند، نه یکی.
- **سود/زیانِ درست**: فرمولِ قبلی `(خروج − ورود) × لات` بود که contract size را
  ۱ فرض می‌کرد و برای XAUUSD/شاخص/کریپتو ده‌ها هزار برابر غلط درمی‌آمد. حالا
  مقدارِ سود/زیان را خودِ کاربر وارد می‌کند و سیستم فقط یک عددِ **پیشنهادی**
  (با اندازه‌ی قرارداد، کمیسیون و سواپ) کنارش نشان می‌دهد.
- **R و آمار**: `R = سود/زیان ÷ ریسکِ اولیه` سمتِ سرور محاسبه و ذخیره می‌شود.
  آمارِ حساب حالا شامل فاکتور سود، میانگین R، امید ریاضی و بیشترین افت سرمایه
  هم هست (`lib/tradeAnalytics.ts` — یک تعریفِ واحد، مشترکِ سرور و کلاینت).
- **چک‌لیست چندگانه**: چند چک‌لیستِ نام‌دار، با کپی، مرتب‌سازیِ درگی و دکمه‌ی
  «معامله» که فرمِ ثبت را با همان چک‌لیست باز می‌کند. **ناقص بودنِ چک‌لیست
  هیچ‌وقت جلوی ثبتِ معامله را نمی‌گیرد** — حتی وقتی «الزامی» علامت خورده باشد؛
  فقط هشدار می‌دهد. وضعیتِ تیک‌ها در لحظه‌ی ثبت **اسنپ‌شات** می‌شود، پس ویرایش
  یا حذفِ بعدیِ چک‌لیست آمارِ معاملاتِ گذشته را عوض نمی‌کند (تست‌شده).
- **عکس‌ها** از ستونِ روی خودِ ردیفِ معامله به جدولِ جدا (`TradeImage`) منتقل
  شدند تا لیستِ معاملات چند مگابایت data URL را با خودش نکِشد؛ تا ۴ عکس، با
  چسباندنِ مستقیمِ Ctrl+V.
- **مسیرها**: `/trade` حالا هابِ شش‌بخشی است؛ `/trade/journal` (حساب‌ها)،
  `/trade/accounts/[id]` (آمار + معاملات)، `/trade/checklists`. مسیرهای قدیمیِ
  `?tab=` برداشته شدند.

### مهاجرت
`20260830220000_trade_module_accounts` دست‌نویس است نه خروجیِ خامِ Prisma —
چون ستون‌های NOT NULLِ جدید روی جدولِ پر می‌نشستند. معاملاتِ موجود به یک
«حساب اصلی» و آیتم‌های چک‌لیستِ قبلی به یک «چک‌لیست من» منتقل می‌شوند و عکسِ
تکِ قبلی به جدولِ عکس‌ها می‌رود. روی داده‌ی واقعی تست شد و خروجیِ نهایی‌اش
دقیقاً با `schema.prisma` یکی است (diff خالی).

### تستِ انجام‌شده
ساخت حساب/برچسب/چک‌لیست/معامله روی سرورِ واقعی، محاسبه‌ی جلسه (LONDON+NEWYORK)
و R، سبک‌بودنِ لیست در برابر سنگین‌بودنِ جزئیات، ماندگاریِ اسنپ‌شات بعد از
بازنویسی و حذفِ چک‌لیست، آرشیو، پنج حالتِ اعتبارسنجی، ۴۰۱ برای مهمان، ۴۰۳ برای
کاربرِ بی‌اشتراک، و IDOR بین دو کاربر.

## نسخه: v1.106.0 (قیمتِ جدیدِ پلن‌ها، پلنِ فعلی+تمدید، سقفِ ماهانه‌ی AI، فیکسِ چند باگ)

- **قیمتِ پلن‌ها**: بدنسازی/ترید ۱۵۰,۰۰۰ تومان، مکس ۲۵۰,۰۰۰ تومان (ماهانه؛ ۳/۶/۱۲ماهه با همون تخفیفِ ۱۲.۵٪ قبلی محاسبه شد) — `lib/planPricing.ts` + `components/PlanShowcase.tsx` (که باید هم‌زمان با هم عوض بشن) + `prisma/seed.ts` (برای هم‌خونیِ پنلِ ادمین).
- **جزئیاتِ داخلِ کارتِ پلن**: فهرستِ امکانات + سقفِ ماهانه‌ی «ساختِ برنامه‌ی تمرینی با AI» (بدنسازی ۳بار/ماه، مکس ۵بار/ماه) — این سقف حالا واقعاً سمتِ سرور enforce می‌شه (`lib/aiQuota.ts`، رویِ مدلِ ازقبل‌موجودِ بلااستفاده‌ی `AiMonthlyQuota`)، نه فقط یه عددِ تزئینی.
- **پلنِ فعلیِ کاربر**: به‌جای نوشته‌ی ثابتِ «پلنِ فعلیِ تو»، حالا یه تیکِ بزرگِ وسطِ کارت + زیرش یه دکمه‌ی «تمدیدِ اشتراک» که با کلیک مدت رو می‌پرسه و به چک‌اوتِ همون پلن می‌بره.
- دکمه‌ی خریدِ هر پلن همه‌جا «خرید این پلن» شد (نه «خرید اشتراک»).
- **فیکس**: انیمیشنِ لرزش/جابجاییِ فیلدها روی خطای رمزِ اشتباه (لاگین) و OTP (ثبت‌نام) حذف شد — پیامِ خطا هنوز هست، فقط دیگه چیزی جابجا نمی‌شه.
- **فیکس**: دکمه‌ی «اعمال»ِ کدِ تخفیف یه بک‌گراندِ سبزِ ناخواسته روی هاور/تاچ چشمک می‌زد (همون باگِ همیشگیِ اسپسیفیسیتیِ `button:hover` سراسری) — حالا صریح خنثی شده.
- **فیکس**: منوها (همبرگری/اعلان‌ها) روی موبایل چند فریمِ اول بدونِ بلور دیده می‌شدن و بعد بلور «می‌رسید» — بلاکِ `data-perf=low` جدا از بلور، `will-change` رو هم سراسری صفر می‌کرد و لایه‌ی زودهنگامِ پروموت‌شده‌ی این منوها رو از بین می‌برد؛ حالا برای همون منوها `will-change` هم برگردونده شد.

## نسخه: v1.105.0 (صفحه‌ی سفید با 0.0.0.0:3000 — فیکسِ قبلی کافی نبود)

کاربر گزارش داد پرداختِ **موفق** هم به صفحه‌ی سفید با آدرس `0.0.0.0:3000`
می‌رود. آن آدرس دقیقاً همان چیزی است که سرورِ standalone نکست داخلِ کانتینر
به آن bind می‌شود، پس `req.nextUrl.origin` همان می‌شد.

**اشکالِ فیکسِ قبلی:** `getSiteUrl()` اگر `NEXTAUTH_URL` ست نبود، به همان
originِ خراب برمی‌گشت — یعنی روی سروری که آن env را ندارد، فیکس **بی‌اثر**
بود. و خروجیِ اندپوینتِ تشخیص هم `NEXTAUTH_URL` را گزارش نمی‌کرد، پس
نمی‌شد فهمید ست هست یا نه.

حالا به‌جای تکیه بر تنظیماتِ سرور، هر گزینه از فیلترِ `isPublicHost` رد
می‌شود: `localhost`، `0.0.0.0`، `::1`، `127.*` و شبکه‌های خصوصیِ داکر
(`10.*`، `192.168.*`، `172.16–31.*`) هرگز به مرورگرِ کاربر داده نمی‌شوند.
اگر هیچ گزینه‌ای عمومی نبود، دامنه‌ی واقعی برگردانده می‌شود.

تست‌شده روی هر شش حالت — همه به `https://arionapp.ir` می‌رسند:

| حالت | نتیجه |
|---|---|
| `NEXTAUTH_URL` درست | ✅ |
| `NEXTAUTH_URL` با http | ✅ |
| **هیچ env ست نیست** | ✅ |
| `NEXTAUTH_URL` خودش localhost | ✅ |
| origin داخلیِ داکر (`172.18.*`) | ✅ |
| origin عمومیِ درست | ✅ |

اندپوینتِ تشخیص حالا `NEXTAUTH_URL` و مهم‌تر `resolvedSiteUrl` را هم
برمی‌گرداند — یعنی آدرسی که واقعاً لینک‌های بازگشت از آن ساخته می‌شوند،
تا دفعه‌ی بعد بدونِ حدس قابلِ بررسی باشد.

## نسخه: v1.104.0 (صفحه‌ی سفید بعد از پرداخت + برگشت به صفحه‌ی انتخاب درگاه)

### باگ: صفحه‌ی سفید
همان ریشه‌ای که آدرسِ بازگشتِ درگاه را شکسته بود، در روتِ **verify** جا مانده
بود. هر ۹ ریدایرکتِ آن از `req.nextUrl.origin` ساخته می‌شدند؛ اپ پشتِ nginx
است، پس آن مقدار می‌توانست `http://127.0.0.1:3000` دربیاید — و مرورگرِ کاربر
به آدرسی می‌رفت که روی دستگاهِ خودش هیچ چیزی نیست. **صفحه‌ی سفید.**

حالا همه از `getSiteUrl()` می‌آیند (`lib/siteUrl.ts`، همان که برای callback
ساخته شد). تأیید شد: حتی با originِ `http://127.0.0.1:3000`، مقصد
`https://arionapp.ir/...` می‌شود.

### تغییر: پرداختِ ناموفق به صفحه‌ی انتخابِ درگاه برمی‌گردد
قبلاً به `/subscription` می‌رفت، یعنی کاربر باید کلِ مسیرِ انتخابِ پلن و مدت و
کدِ تخفیف را از اول می‌رفت. حالا با حفظِ `plan` و `duration` به
`/subscription/checkout` برمی‌گردد و می‌تواند همان‌جا دوباره — یا با درگاهِ
دیگر — امتحان کند. بنرِ «پرداخت ناموفق بود یا لغو شد — چیزی از حسابت کم
نشده» همان بالا نشان داده می‌شود.

اگر پلن/مدت معلوم نباشد (سشن منقضی)، همچنان به `/subscription` می‌رود.
پرداختِ موفق هم مثل قبل به `/subscription?checkout=success`.

## نسخه: v1.103.0 (کنترل بار سرور — حذف یک روتِ پرداختِ بی‌احراز‌هویت)

### ۱. روتِ پرداختِ باز روی اینترنت (جدی‌ترین یافته)
`/api/payment/request` و `/api/payment/verify` **هیچ احراز هویتی نداشتند**،
ریت‌لیمیت نداشتند، و **مبلغ را از خودِ کلاینت می‌گرفتند**. هر فراخوان یک
تراکنش واقعی روی زیبال می‌ساخت و یک اتصالِ سرور را نگه می‌داشت — یعنی دقیقاً
همان بردارِ حمله‌ای که سرور را می‌خواباند.

هر دو **کدِ مرده بودند**: هیچ‌جای اپ صدایشان نمی‌زد، به
`NEXT_PUBLIC_BASE_URL` وابسته بودند که هیچ‌جا تعریف نشده، و به صفحه‌های
`/payment/success` و `/payment/failed` ریدایرکت می‌کردند که **اصلاً وجود
ندارند**. جریانِ واقعیِ خرید از `/api/subscription/checkout` می‌رود. حذف
شدند — وصله‌کردنِ یک روتِ بی‌مصرف بی‌معنی بود.

### ۲. ریت‌لیمیت روی روت‌هایی که واقعاً AI صدا می‌زنند
هر فراخوانِ AI هم پول خرج می‌کند هم یک اتصالِ سرور را چند ده ثانیه اشغال.
گیتِ ماژول فقط می‌گوید «مشترک است»، نه «نمی‌تواند صدبار پشت‌سرهم بزند».

- `GET /api/reports/weekly` — ۴۰ در ساعت (بارِ اولِ هر هفته واقعاً AI می‌زند)
- `POST /api/exercise/plan` — ۱۰ در روز

### ۳. پولینگ در تبِ مخفی متوقف شد
نوار قیمت (۳۰ ثانیه) و موتور اعلان‌ها (۲ دقیقه) حتی وقتی تب ساعت‌ها در
پس‌زمینه بود درخواست می‌فرستادند. مرورگر تایمرِ تبِ مخفی را کُند می‌کند ولی
**متوقف نمی‌کند** — یک تبِ فراموش‌شده روزی ~۲۹۰۰ درخواستِ بی‌فایده می‌زد.

اندازه‌گیری‌شده در مرورگر: تبِ مخفی → **۰ درخواست** · لحظه‌ی برگشتِ کاربر →
**۱** تازه‌سازیِ فوری (پس داده‌ی بیات هم نمی‌بیند).

### برگشتِ ناموفق از درگاه
از قبل کار می‌کرد و تأیید شد: کاربر به `/subscription?checkout=failed`
برمی‌گردد با پیامِ «پرداخت ناموفق بود یا لغو شد — چیزی از حسابت کم نشده»، و
دلیلِ دقیق در بک‌اند به‌صورت رویدادِ `checkout_failed` ثبت می‌شود
(`no_session` / `gateway_canceled_or_error` / `verify_rejected` / …) که در
پنل Owner › Funnel دیده می‌شود.

## نسخه: v1.103.0 (فیکسِ کندیِ ارسالِ کدِ پیامکی)

درخواستِ کدِ OTP (فراموشیِ رمز و تاییدِ شماره در ثبت‌نام) تا الان منتظرِ
جواب‌دادنِ کاملِ ملی‌پیامک/SMTP می‌موند و بعد به کلاینت جواب می‌داد — یعنی
اگه اون سرویس چند ثانیه طول می‌کشید، کاربر همون‌قدر پشتِ اسپینر می‌موند.
حالا کد اول توی دیتابیس ذخیره می‌شه و فوراً به کلاینت جواب داده می‌شه؛
ارسالِ واقعیِ پیامک/ایمیل در پس‌زمینه (بدونِ await) ادامه پیدا می‌کنه —
چون این اپ روی یه Node process همیشه‌روشن (Docker) اجرا می‌شه نه سرورلس،
این کار امنه و پیامک بازم می‌رسه.

## نسخه: v1.102.0 (علتِ واقعیِ خرابیِ خرید — آدرسِ بازگشتِ درگاه)

اندپوینتِ تشخیصِ نسخه‌ی قبل جواب داد و **دو فرضیه‌ی قبلیِ من را رد کرد**:

```
zibal:      ok, status 200, 359ms     ← شبکه سالم، درگاه سریع
zarinpal:   ok, status 422, 403ms
ai-gateway: ok, status 401, 310ms
ZARINPAL_MERCHANT_ID: false
NEXT_PUBLIC_SITE_URL: null
```

یعنی نه تایم‌اوت مشکل بود، نه دسترسیِ شبکه. دو باگ واقعی:

### ۱. آدرسِ بازگشت از `req.nextUrl.origin` ساخته می‌شد
اپ پشتِ nginx است؛ آن‌چه به Node می‌رسد یک درخواستِ سادهٔ HTTP روی
`127.0.0.1:3000` است، پس `origin` می‌تواند `http://arionapp.ir` (بدون s) یا
حتی `http://127.0.0.1:3000` دربیاید. **درگاه‌های ایرانی آدرسِ بازگشت را
اعتبارسنجی می‌کنند** — زیبال با کدِ ۱۰۶ ردش می‌کند. یعنی پرداخت از همان
اولین قدم می‌شکست، بی‌ربط به کلیدِ مرچنت و شبکه.

`lib/siteUrl.ts` حالا از `NEXTAUTH_URL` می‌سازدش (روی هر دیپلویِ سالمی حتماً
درست است، وگرنه خودِ ورود کار نمی‌کرد)، اسلشِ آخر را می‌برد و https را
تضمین می‌کند.

### ۲. درگاهِ پیش‌فرضِ کلاینت `zarinpal` بود ولی کلیدش ست نیست
یعنی هر کاربری که دستی زیبال را انتخاب نمی‌کرد، مستقیم به یک درگاهِ
تنظیم‌نشده می‌خورد. `GET /api/subscription/gateways` حالا می‌گوید کدام
درگاه‌ها روی این سرور واقعاً کار می‌کنند، و صفحه‌ی چک‌اوت فقط همان‌ها را
نشان می‌دهد و پیش‌فرض را روی اولینِ سالم می‌گذارد.

تست‌شده روی هر سه حالت: فقط زیبال → `["zibal"]` · هر دو →
`["zibal","zarinpal"]` · هیچ‌کدام → `[]`.

## نسخه: v1.101.0 (همان باگ در گیت‌وی AI + اندپوینت تشخیص اتصال خروجی)

### الگویی که پیدا شد
دو صفحه‌ی کاملاً بی‌ربط — خرید اشتراک و گزارش هفتگی — با **یک پیام یکسان**
می‌شکستند («مشکلی در اتصال به سرور»)، در حالی که هرچیزی که فقط دیتابیس
می‌خواست درست کار می‌کرد. وجه اشتراکشان: هر دو یک درخواست HTTPS به بیرون
می‌زنند.

### دو باگ واقعی
1. **`app/api/reports/weekly/route.ts` هیچ `try/catch` نداشت.** این روت به
   گیت‌وی AI وصل می‌شود؛ هر خطایی باعث می‌شد نکست یک صفحه‌ی **HTML** با کد
   ۵۰۰ برگرداند نه JSON، و کلاینت روی `r.json()` خطا بخورد و پیام گمراه‌کننده
   نشان دهد. حالا همیشه JSON فارسی برمی‌گرداند (۵۰۳ برای env تنظیم‌نشده،
   ۵۰۴ برای تایم‌اوت، ۵۰۲ برای بقیه).
2. **`lib/aiClient.ts` تایم‌اوت نداشت** — دقیقاً همان باگی که در `zibal.ts` و
   `zarinpal.ts` بود. `fetch` در Node تا ابد صبر می‌کند ولی nginx نه. حالا
   ۴۵ ثانیه (بیشتر از درگاه‌ها، چون تولید متن ذاتاً کند است).

### اندپوینت تشخیص
`GET /api/admin/diagnostics/outbound` (فقط سوپرادمین). یک بار بازکردنش
می‌گوید کدام سرویس بیرونی در دسترس است، با چه تأخیری، و کدام env تنظیم شده
(فقط هست/نیست، هیچ مقداری برنمی‌گردد). تفاوت «DNS حل نمی‌شود» با «کند است»
با «۴۰۳ می‌دهد» را روشن می‌کند — بدون نیاز به خواندن لاگ کانتینر.

همچنین `location` جدا در nginx برای `^/api/(reports|roadmaps)/` با مهلت ۶۰
ثانیه، چون روت‌های AI ذاتاً کندند و ۱۵ ثانیه‌ی پیش‌فرض همیشه می‌کشتشان.

## نسخه: v1.100.0 (فیکس دیده‌شدنِ پشتِ منوها روی موبایل)

منویِ اصلی (همبرگری) و منوهایِ شناورِ دیگه (پنلِ اعلان/پروفایل، منویِ
سه‌نقطه‌ی داشبورد) روی گوشی‌ها بی‌بلور دیده می‌شدن و پشتشون کامل معلوم بود
— چون بلاکِ `data-perf=low` (فیکسِ لگِ اسکرولِ موبایل، `lib/perfTier.ts`)
با یه قانونِ سراسری `backdrop-filter:none` رو از *همه‌چیز* حذف می‌کرد، نه
فقط از عناصرِ همیشه‌مرئیِ گرون (هدر/نوارِ پایین/کارت‌ها) که واقعاً هدفش بودن.
حالا برایِ همون منوها (`.nav-drawer-glass`, `.notif-panel`,
`.dash-context-menu`) بلور صریحاً برگردونده شده — چون این‌ها فقط
به‌درخواستِ کاربر و اکثراً با اسکرولِ قفل‌شده باز می‌شن (نه هر فریمِ اسکرول)،
هزینه‌شون یه‌بارِ باز شدنه، پس لگِ موبایل که اون فیکس برایِ حلش ساخته شده
بود دوباره برنمی‌گرده.

## نسخه: v1.99.0 (تحویلِ خودکارِ گزارشِ هفتگی + دکمه‌ی تستِ ادمین)

گزارشِ هفتگی حالا یه مسیرِ cronِ واقعی داره: `app/api/cron/weekly-report`
(پشتِ همون `CRON_SECRET`ِ موجودِ `send-reminders`، منطقِ چک‌کردنش به
`lib/cronAuth.ts` منتقل شد تا دو جا تکرار نشه) — هر شنبه ساعت ۹ِ صبح یه
crontabِ بیرونی صداش می‌زنه، برایِ هر کاربرِ دارایِ ماژولِ AI_INSIGHT
گزارشِ هفته‌ی تازه‌تموم‌شده رو generate می‌کنه و با Web Push خبر می‌ده.
راهنمای دقیقِ crontab: `deploy/cron.example`.

برایِ تستِ Trend/Streak/Correlation (که به چند هفته داده نیاز دارن و روی
حسابِ تازه‌ساز چیزی برای نشون‌دادن ندارن)، پنلِ Owner › تنظیمات یه دکمه‌ی
«ساختِ داده‌ی تست» گرفته — ۱۰ هفته داده‌ی مصنوعیِ نوسان‌دار برایِ هر ۵ بخش
رویِ حسابِ خودِ سوپریوزر می‌سازه (`app/api/admin/weekly-report/seed-test-data`).

## نسخه: v1.98.2 (نوار قیمت‌ها هر دور قطع می‌شد)

نوار قیمت بالای صفحه‌ی ترید هر چرخه یک پرش/قطعی داشت و بی‌درز لوپ نمی‌شد.

علت هندسیِ محض بود: ترک دو کپیِ پشت‌هم از لیست است (۲N آیتم) و انیمیشن دقیقاً
`translateX(-50%)` می‌شود تا لحظه‌ی چرخش کپی دوم مو‌به‌مو جای کپی اول بنشیند.
ولی `gap:24px` بین ۲N آیتم فقط **۲N−۱** فاصله می‌گذارد، نه ۲N — یعنی نصف عرض
کل، نصفِ یک gap کمتر از عرض واقعی یک کپی است.

اندازه‌گیری‌شده روی CSS واقعی بیلد در حالت RTL:

| | ناهماهنگی هر چرخه |
|---|---|
| با `gap:24px` | **۱۱.۷۸px** (≈ نصف gap) |
| با `margin-inline-end` روی آیتم | **۰.۰۵px** |

فاصله‌ی بصری بین آیتم‌ها دقیقاً همان ۲۸px قبلی ماند.

## نسخه: v1.98.1 (مبلغ تخفیف‌خورده روی دکمه‌ی پرداخت نمایش داده نمی‌شد)

با کد تخفیف ۹۰٪ روی پلن ۹۹,۰۰۰ تومانی، بالای صفحه «۹۰٪ تخفیف اعمال شد» می‌آمد
ولی دکمه همچنان «پرداخت ۹۹,۰۰۰ تومان» می‌گفت. علت: `price` مستقیم از
`plan.prices[duration]` — یعنی رشته‌ی *بدون تخفیف* — خوانده می‌شد و هیچ‌وقت با
`percentOff` بازمحاسبه نمی‌شد.

سرور مبلغ درست را حساب می‌کرد، پس **پول درست کم می‌شد** — ولی کاربر عدد غلط
می‌دید، که هم گیج‌کننده است هم اعتمادسوز روی صفحه‌ی پرداخت.

حالا از `amounts` (عدد خام) و **با همان فرمول سمت سرور** حساب می‌شود، و قیمت
اصلی خط‌خورده کنارش می‌ماند. تأیید شد: ۹۹,۰۰۰ با ۹۰٪ → ۹,۹۰۰ تومان.

## نسخه: v1.98.0 (فیکس لگ موبایل — آستانه‌ی تشخیص خیلی سخت‌گیر بود)

فیکس v1.97.0 روی گوشی‌های واقعی **فعال نمی‌شد**. آستانه‌اش `deviceMemory ≤ ۴`
یا `hardwareConcurrency ≤ ۴` بود، ولی گوشی‌های امروزی ۸ گیگ رم و ۸ هسته
گزارش می‌کنند — پس هیچ‌وقت علامت نمی‌خوردند و لگ سرجایش بود.

اشتباهِ تحلیل: مسئله **رم نیست، GPU است**. حتی پرچم‌دارها هم یک GPU موبایل با
پهنای‌باند حافظه‌ی محدود دارند، و بلور ۲۸ پیکسلی پشتِ یک هدر ثابت در هر فریمِ
اسکرول دوباره محاسبه می‌شود. حالا معیار ساده است: **هر دستگاه لمسی**.
دسکتاپ (`pointer:fine`) دست‌نخورده می‌ماند.

دو منبع بار دیگر هم پیدا و مهار شد:
- `.wbox-liquid` یک `blur(9px)` روی کانتینری دارد که بچه‌هایش (`.wbox-blob`)
  مدام transform می‌شوند — یعنی آن لایه در **هر فریم** دوباره rasterize
  می‌شود، و این جعبه‌ها روی صفحه‌ی هفتگی چندتایی‌اند. همین یکی عاملِ
  «بعد از چند ثانیه کند می‌شود» بود.
- انیمیشن‌های تزئینیِ همیشه‌درحال‌اجرا (`taskMissedPulse`) متوقف و شعله‌ی
  استریک کندتر شد (حذف نشد چون بازخورد معنادار کاربر است).

اندازه‌گیری روی پروفایل گوشی میان‌رده (۸ گیگ/۸ هسته، CPU چهار برابر کندتر):

| | قبل | بعد |
|---|---|---|
| میانگین زمان فریم | ۴۲.۹ms | **۱۶.۶ms** |
| بدترین ۵٪ (p95) | ۵۸.۱ms | **۱۷.۹ms** |
| فریم‌های افتاده | ۱۷۱ از ۱۸۰ | **۰ از ۱۸۰** |

## نسخه: v1.97.0 (لگِ گوشی‌های ضعیف، درگاه زیبال، و محتوای سئوی فارسی)

### ۱. لگ روی گوشی‌های ردهٔ پایین — اندازه‌گیری‌شده
علت: حدود ۱۵۰ جا `backdrop-filter` (افکت شیشه‌ای) با شعاع بلور تا ۳۴px. چون
هدر/کارت‌ها ثابت‌اند و صفحه زیرشان اسکرول می‌شود، مرورگر در **هر فریم** باید
همه‌ی پیکسل‌های پشتشان را دوباره نمونه‌برداری و بلور کند. هزینه‌ی rasterize
تقریباً با مجذور شعاع رشد می‌کند.

راه‌حل: `lib/perfTier.ts` قبل از اولین پینت دستگاه را می‌سنجد (فقط لمسی +
`deviceMemory ≤ ۴` یا `hardwareConcurrency ≤ ۴`) و `data-perf="low"` روی
`<html>` می‌گذارد. زیر آن attribute بلورها، `will-change`ها و انیمیشن‌های
framer-motion (با یک `MotionConfig` واحد در ریشه، نه ۴۲ فایل جدا) خاموش
می‌شوند. **روی دسکتاپ، آیفون و اندروید ردهٔ بالا هیچ چیز تغییر نمی‌کند.**

اندازه‌گیری با CPU ۶ برابر کندتر، اسکرول ۱۸۰ فریمی روی صفحه‌ی اصلی:

| | قبل | بعد |
|---|---|---|
| میانگین زمان فریم | ۴۹.۱ms | **۱۶.۷ms** |
| بدترین ۵٪ (p95) | ۵۷.۵ms | **۱۹.۷ms** |
| فریم‌های افتاده (>32ms) | ۱۷۵ از ۱۸۰ | **۱ از ۱۸۰** |

یعنی عملاً ~۲۰fps → ~۶۰fps. بک‌گراند نیمه‌شفاف عناصر دست‌نخورده ماند (فقط
بلور برداشته شد)، پس هیچ بک‌گراندی اضافه نشد.

### ۲. درگاه زیبال: «مشکلی در اتصال به سرور پیش اومد»
علت: `fetch` در Node **تایم‌اوت پیش‌فرض ندارد**، ولی nginx روی `location /`
`proxy_read_timeout 15s` دارد. اگر درگاه دیرتر جواب می‌داد، nginx اتصال را
می‌بست و یک صفحه‌ی **HTML** با کد ۵۰۴ می‌داد؛ کلاینت که کورکورانه
`res.json()` می‌زد throw می‌کرد و می‌افتاد در `catch` — و دقیقاً همان پیام
گمراه‌کننده را نشان می‌داد، انگار اینترنت کاربر قطع است.

- `lib/zibal.ts` و `lib/zarinpal.ts`: تایم‌اوت ۱۲ ثانیه‌ای + پیام‌های فارسی
  به‌ازای کدهای خطای زیبال + مدیریت پاسخ غیر-JSON.
- `checkout/route.ts`: خواندن سشن و ریت‌لیمیت هم داخل `try` رفتند — هر خطای
  بیرون از آن یعنی HTML به‌جای JSON.
- `checkout/page.tsx`: دیگر کورکورانه `res.json()` نمی‌زند؛ کد واقعی HTTP را
  به کاربر می‌گوید تا مشکل قابل تشخیص باشد.
- `deploy/nginx.conf.example`: `location` جدا برای روت‌های پرداخت با
  `proxy_read_timeout 30s` (بیشتر از تایم‌اوت ۱۲ ثانیه‌ای اپ، تا همیشه اپ
  زودتر تصمیم بگیرد و JSON برگرداند).

### ۳. محتوای سئوی فارسی
| صفحه | قبل | بعد |
|---|---|---|
| `/about` | ۱۹ کلمه | **۳۸۰ کلمه** |
| `/faq` | ۲۴۹ کلمه (۶ سوال) | **۷۱۰ کلمه (۱۵ سوال)** |

`/about` عملاً یک صفحه‌ی خالی بود که برای سئو مضر است. حالا پنج بخش واقعی
دارد و عبارت‌هایی را پوشش می‌دهد که کاربر فارسی‌زبان واقعاً جست‌وجو می‌کند.
سوالات جدید FAQ روی کوئری‌های واقعی هدف‌گذاری شده‌اند (تقویم شمسی، ژورنال
ترید چیست، برنامه‌ی بدنسازی، کالری‌شماری غذای ایرانی، نصب روی گوشی).
عنوان همه‌ی صفحه‌ها با `absolute` زیر ~۶۰ نویسه آمد تا گوگل بریده نمایششان
ندهد. لینک‌دهی داخلی از `/about` به `/faq` و `/terms` اضافه شد.

## نسخه: v1.96.0 (راه‌های ارتباطی: تلگرام و اینستاگرام)

- آیدی تلگرام پشتیبانی (`@Arionapp`) و اینستاگرام (`@Arionapp`) به صفحه‌ی
  «درباره ما» (`/about`) و صفحه‌ی پشتیبانیِ پنل (`/account/support`) اضافه شد.
- `lib/brand.ts` منبعِ واحدِ این لینک‌هاست (مثل بقیه‌ی نامِ برند)، چون در سه جا
  استفاده می‌شوند و جدا نوشتنشان به واگرایی می‌انجامید.
- `components/SocialIcons.tsx`: آیکون‌های تلگرام/اینستاگرام به‌صورت SVG خام،
  چون `lucide-react` از نسخه‌ی ۱.x لوگوهای برند را حذف کرده. با `currentColor`
  و بدونِ هیچ بک‌گراندی، تا دقیقاً مثل بقیه‌ی آیکون‌های صفحه رفتار کنند.
- **سئو:** `sameAs` روی اسکیمای `Organization` و `rel="me"` روی خودِ لینک‌ها.
  این همان چیزی است که گوگل برای وصل‌کردنِ یک برند به پروفایل‌های رسمی‌اش
  استفاده می‌کند — و برای دامنه‌ای که هنوز صفر بک‌لینک دارد (طبق Search
  Console: «Referring page: None detected») یکی از معدود سیگنال‌هایی است که
  می‌شود از داخلِ خودِ سایت داد.

## نسخه: v1.95.0 (سئو — نامِ فارسیِ برند در خودِ صفحه، و og:image)

### مسئله
جست‌وجوی «آریون» یا «آریون اپ» به سایت نمی‌رسید. دلیلش پیکربندیِ فنی نبود —
robots.txt، sitemap، canonical و JSON-LD همه از قبل درست بودن. مسئله ساده‌تر و
بنیادی‌تر بود: **کلمه‌ی «آریون» عملاً در صفحه وجود نداشت.** در HTMLِ صفحه‌ی اصلی
«Arion» ۱۸ بار می‌آمد و «آریون» صفر بار (فقط داخلِ description). گوگل تطابقِ برند
را روی محتوای واقعیِ صفحه انجام می‌دهد، پس یک جست‌وجوی فارسی هیچ چیزی برای تطابق
پیدا نمی‌کرد. ضمناً از نظر گوگل «آریون» و «Arion» دو موجودیتِ بی‌ربط بودند، چون
هیچ‌جا به هم وصل نشده بودند.

### تغییرات
- `lib/brand.ts` — منبعِ واحدِ نامِ برند. این رشته‌ها حالا در title، description،
  JSON-LD و h1 پخش‌اند؛ اگر هرکدام جدا نوشته می‌شدند مرور زمان از هم واگرا
  می‌شدند (همان درسی که با لیستِ کلیدهای تنظیمات گرفتیم).
- `alternateName` روی هر سه اسکیمای Organization/WebSite/SoftwareApplication —
  این همان چیزی است که گوگل برای وصل‌کردنِ یک برند به املاهای دیگرش استفاده
  می‌کند («آریون»، «Arion»، «آریون اپ»، «اپ آریون»، «Arion App»).
- عنوان و توضیحِ صفحه‌ی اصلی و h1 حالا هر دو املا را دارند:
  «همه‌ی نظم زندگی‌ات، توی آریون (Arion)». تعدادِ «آریون» در HTML از ۰ به ۴۸ رسید.
- **باگ: og:image هیچ‌جا سرو نمی‌شد.** نکست فیلدِ `openGraph` را بین layout و page
  ادغام **نمی‌کند**، جایگزین می‌کند — پس هر صفحه‌ای که `openGraph: { url: … }`
  خودش را داشت (`/`، `/about`، `/faq`، `/terms`) بی‌سروصدا og:image و og:type و
  og:locale و og:site_name را از دست می‌داد. هر لینکی که در تلگرام/واتساپ به
  اشتراک گذاشته می‌شد یک کارتِ بی‌تصویر بود. با `OG_BASE` که در همه‌ی این صفحه‌ها
  spread می‌شود حل شد.
- `public/og.png` (۱۲۰۰×۶۳۰، ساخته‌شده با `scripts/make-og.js`) — قبلاً اصلاً
  وجود نداشت.
- `scripts/check-setting-keys.js`: رفعِ یک هشدارِ نادرست — `ai_cost_rate` مربوط به
  مدلِ `AppSetting` (تنظیماتِ سراسریِ سرور) است و هیچ‌وقت از `/api/settings/[key]`
  رد نمی‌شود؛ فقط چون تابعِ داخلیِ `lib/appSettings.ts` هم اتفاقی `getSetting` نام
  داشت در الگو گیر می‌افتاد.

### تأیید شد
`npm run check` سبز · بیلدِ production · هر چهار صفحه‌ی عمومی og:image کامل دارند ·
صفحه‌های خصوصی همچنان `X-Robots-Tag: noindex` · دقیقاً یک متای theme-color ·
بدونِ خطای جاوااسکریپت.

### مهم — این بخش با کد حل نمی‌شود
کد فقط سایت را *قابلِ پیدا شدن* می‌کند؛ خودِ ایندکس‌شدن کارِ گوگل است و به
اقدامِ دستی نیاز دارد (ثبت دامنه در Search Console، درخواستِ ایندکس، و لینک از
جاهای دیگر). جزئیات در توضیحِ همین PR.

## نسخه: v1.94.0 (دادهٔ لود داخلِ خودِ HTML — صفر درخواستِ API در بازکردنِ صفحه)

### مسئله
حتی بعد از جمع‌شدنِ همه‌چیز در یک `/api/bootstrap`، و حتی وقتی تأخیرِ شبکه **صفر** بود، اولین بایتِ داده ۱۳۲ms بعد می‌رسید: مرورگر باید اول HTML را پارس کند، بعد اسکریپتِ inline را اجرا کند، بعد یک رفت‌وبرگشتِ کاملِ دیگر بزند. آن رفت‌وبرگشت ذاتی نبود — سرور همان لحظه‌ای که HTML را می‌سازد به همان داده دسترسی دارد.

### چی تغییر کرد
- **`components/InlineBootstrap.tsx`** — دادهٔ لودِ اولیه (تنظیمات، حساب، آواتار، بازهٔ ۹۰روزهٔ روزانه، دوستان و درخواست‌های دوستی) داخلِ یک `<script type="application/json">` در خودِ HTML می‌آید. برای مهمان‌ها هیچ کوئری‌ای نمی‌زند و چیزی رندر نمی‌کند.
- **سشن از سرور پاس داده می‌شود** به `SessionProvider`، پس دیگر `/api/auth/session` صدا زده نمی‌شود.
- منطقِ آمارِ دوستان به `lib/friendStats.ts` منتقل شد تا هم روتِ API هم کامپوننتِ سرور از یک کد استفاده کنند (Next اجازه نمی‌دهد `route.ts` چیزی جز هندلرهای HTTP export کند).

**نتیجه: لودِ `/weekly` از ۴ درخواستِ API به صفر رسید.** داده دقیقاً هم‌زمان با HTML می‌رسد، در هر تأخیرِ شبکه‌ای.

### هزینه‌ای که دارد
`cookies()` در layout یعنی همهٔ صفحه‌ها dynamic شدند (قبلاً static بودند). زمانِ رسیدنِ HTML از ~۲۶ms به ~۶۶ms رفت — ولی یک رفت‌وبرگشتِ کامل (که در RTT ۱۰۰ms حدود ۲۷۰ms بود) حذف شد. معاملهٔ روشنی است.

### آنچه *حل نشد* — و صادقانه باید گفت
هدف «همه‌چیز زیرِ ۲۵۰ms» برای **داده** محقق شد، ولی برای **دیده‌شدنِ صفحه** نه:

| RTT | HTML | FCP |
|---|---|---|
| ۳۰ms | ۶۶ms | **۳۶۴ms** |
| ۵۰ms | ۶۵ms | **۳۲۰ms** |
| ۱۰۰ms | ۸۵ms | **۳۷۶ms** |

اندازه‌گیری نشان داد CSS و فونت **اصلاً** رندر را بلاک نمی‌کنند؛ هرچه قبل از FCP است JavaScript است و FCP دقیقاً بعدِ آخرین chunk می‌آید. علتش ساختاری است: صفحه‌ها client component هستند، پس تا React هیدریت نکند چیزی نقاشی نمی‌شود.

رساندنِ FCP به زیرِ ۲۵۰ms یعنی تبدیلِ صفحه‌ها به server component (رندرِ محتوای واقعی در HTML) — بازنویسیِ بزرگی که در این تغییر انجام **نشده**.

## نسخه: v1.93.0 (بنچمارکِ ۲۵۰۰ کاربر — گلوگاه CPU بود نه دیتابیس)

### چطور اندازه‌گیری شد
۲۵۰۰ کاربرِ واقعی در دیتابیس (۱۵٬۰۰۰ رکوردِ تنظیمات، ۲۲۵٬۰۰۰ رکوردِ روزانه، ~۲۵٬۰۰۰ رابطه‌ی دوستی)، بیلدِ production، و الگوی درخواستِ واقعیِ یک لودِ `/weekly` — نه یک اندپوینتِ تکی.

### گلوگاهِ واقعی
حدس اولیه «کوئریِ کند» بود؛ اندازه‌گیری خلافش رو نشون داد:
- زیرِ بار Postgres **بی‌کار** بود: ۹ کانکشن idle، صفر کوئریِ منتظر.
- ولی `/api/health` — که نه به DB دست می‌زنه نه به سشن — از **۳ms به ۲۳۰ms** می‌رفت.
- ظرفیتِ خامِ نود: بدونِ auth ~۱۲۶۴ req/s، با auth ~۷۶۰ req/s.

یعنی گلوگاه **CPU/event loopِ نود** بود، و هر لودِ صفحه **۱۲ درخواست** می‌زد که هرکدوم هزینه‌ی کاملِ `getServerSession` (رمزگشایی JWT) رو جدا می‌داد.

### چی تغییر کرد
- **`/api/bootstrap`** — همه‌ی چیزی که هر صفحه موقعِ لود لازم داره (تنظیمات، حساب، آواتار، بازه‌ی ۹۰روزه‌ی روزانه) در **یک** درخواست، با **یک** چکِ سشن و **دو** کوئریِ موازی. آواتار ستونی از خودِ `User` بود پس با همون کوئریِ حساب میاد، نه جدا. **`/weekly`: ۱۲ → ۴ درخواست.**
- **`/api/friends` ۹ برابر کندتر از بقیه بود** (۵۸ در برابر ۵۱۷ req/s). علتش: برای ساختِ یه پاسخِ ۲ کیلوبایتی، **۹۰۰ ردیفِ DailyEntry** (۱۰ دوست × ۹۰ روز، با کلِ JSONِ `completedItems`) می‌خوند. استریک به محضِ اولین روزِ ناقص می‌شکنه، پس حالا اول ۲۱ روز خونده می‌شه و فقط برای کسی که استریکش به لبه‌ی پنجره رسید بقیه‌اش گرفته می‌شه. ضمناً فقط ستون‌های لازم select می‌شن. **۵۸ → ۱۸۷ req/s.**
- **`/api/push/subscribe`** که در production ۵۰۰ می‌داد حالا خطا رو هندل می‌کنه: حالتِ P2002 (دو تب هم‌زمان همون endpoint رو بفرستن — `subscribeToPush` از دو جای NavDrawer صدا زده می‌شه) دیگه خطا نیست و آپدیت می‌شه؛ هر خطای دیگه با علتِ واقعی **لاگ** می‌شه و ۵۰۳ برمی‌گردونه به‌جای ۵۰۰ِ مبهم و بی‌سرنخ.
- `notifPrefs` و `dashboardPrefs` و آواتار همگی از لایه‌ی داده‌ی مشترک رد می‌شن (قبلاً `fetch`ِ خامِ خودشون رو داشتن، پس از bootstrap تغذیه نمی‌شدن).

### نتیجه (۲۵۰۰ کاربرِ واقعی)
| هم‌زمان | قبل | بعد |
|---|---|---|
| ۱۰۰ | ۳۲ لود/ث · p50 ۲۷۱۷ms | **۷۰** · **۱۰۶۲ms** |
| ۵۰۰ | ۴۳ · ۱۰۲۰۱ms | **۸۵** · **۴۴۲۶ms** |
| ۱۰۰۰ | ۴۵ · ۲۰۲۶۹ms | **۸۸** · **۸۶۵۹ms** |
| ۲۵۰۰ | ۴۰ · ۳۳۹۸۰ms | **۷۸** · **۱۸۴۴۱ms** |

خطاهای `ECONNRESET` که قبلاً از ۱۰۰۰ هم‌زمان به بالا ظاهر می‌شدن (۱۰۵ تا ۲۲۰ مورد) **به صفر رسیدن**.

> ظرفیت روی ۴ هسته در ~۸۰–۹۰ لودِ صفحه بر ثانیه اشباع می‌شه. بالاتر از اون، کاربرِ بیشتر فقط صف می‌سازه — قدمِ بعدی برای عبور از این سقف، اجرای چند instance پشتِ nginx است (که اون‌وقت rate limitِ در-حافظه هم باید با Redis عوض بشه).
## نسخه: v1.92.0 (چند فیکسِ گزارش‌شده: بدنسازی/کالری/رنگ‌بندی/سایز پاپ‌آپ‌ها)

### چی تغییر کرد
- **فیلدِ کدِ تخفیفِ چک‌اوت**: placeholderِ نمونه («مثلاً کدِ دوستت») به یه متنِ ساده («کد تخفیف») تغییر کرد.
- **دکمه‌ی بازگشتِ چک‌اوت**: قانونِ سراسریِ `button{}` (پدینگ/بک‌گراند/باکس‌شدو/بلورِ شیشه‌ای) روی این دکمه اثر می‌ذاشت چون این صفحه ریستِ `.dash-scope` رو نداره؛ الان صریح خنثی شده (بدونِ بک‌گراند، سایزِ درست، انیمیشنِ hover/active).
- **آواتارِ پیکسلیِ تمِ روشن**: پخشِ رنگِ پالت (`HUE_SPREAD`) از ۴۵ به ۱۵ کم شد — قبلاً hue تا زرد/زیتونی می‌رفت که چشم اونو «سبز» می‌دید، الان توی خانواده‌ی نارنجی/قرمزِ خودش می‌مونه.
- **منوی پروفایل**: به‌جای دکمه‌ی همیشه‌قرمزِ «خروج از حساب»، اول یوزرنیم نشون داده می‌شه؛ با زدنِ روش گزینه‌ی خروج ظاهر می‌شه.
- **برنامه‌ی هوشمندِ بدنسازی**: خطای واقعیِ عدم‌استفاده از AI (نبودِ کلید/آدرسِ گیت‌وی یا خطای موقتِ سرویس) قبلاً کاملاً بی‌صدا قورت داده می‌شد؛ الان هم توی لاگِ سرور ثبت می‌شه، هم اگه یه پلن با قالبِ ایستا (نه AI) ساخته بشه، توی خودِ داشبوردِ تمرینی صریح اعلام می‌شه.
- **فیلدهای «مقدار» و «واحد» توی پاپ‌آپِ افزودنِ غذا**: قبلاً فیلدِ مقدار کلِ عرض رو پر می‌کرد و دکمه‌ی واحد کوچیک بود؛ الان هردو دقیقاً هم‌سایز و کنارِ هم، نه کشیده‌شده روی کلِ ردیف.
- **اسکن غذا با هوش مصنوعی**: فعلاً غیرفعال شد — دکمه بلور می‌خوره و یه نشانِ «به‌زودی» روش می‌شینه، عیناً همون الگوی ردیف‌های «به‌زودی» توی جدولِ مقایسه‌ی پلن‌ها.
- **دکمه‌های سواپِ وعده‌ی غذایی (صبحانه/ناهار/شام/میان‌وعده)**: انیمیشنِ نشانگرِ لغزنده از ۳۸۰ به ۲۲۰ میلی‌ثانیه سریع‌تر شد تا حسِ کند نده.
- **رنگِ باکسِ برنامه‌ی هفتگی و چک‌لیستِ ترید**: به‌جای تینتِ جداگونه‌ی خودشون، الان دقیقاً همون رنگِ بقیه‌ی کارت‌های داشبوردِ «روتین من» (`bg-dash-card`) رو دارن — یکدست، هم تویِ تمِ روشن هم تیره.
- **پنلِ کاربری روی موبایل**: بازم جمع‌وجورتر شد (۸۲٪→۷۸٪ عرض، ۶۸vh→۵۸vh ارتفاع، آواتارِ کوچیک‌تر).

## نسخه: v1.91.0 (ادامه‌ی باگِ رنگِ نوارِ بالا — قاعده‌ی معیوب باید *حذف* می‌شد، نه کنارش نوشته می‌شد)

### چرا فیکسِ قبلی کافی نبود
توی v1.90.0 قاعده‌ی سالمِ `html[data-theme="light"]` **اضافه** شد ولی قاعده‌ی قدیمیِ `html:has(body[data-theme="light"])` هم «برای اطمینان» نگه داشته شد. این دقیقاً همون چیزی بود که باگ رو زنده نگه داشت، چون `:has()` اختصاصیتِ آرگومانش رو می‌گیره:

```
html:has(body[data-theme="light"])  →  (0,1,2)   ← برنده
html[data-theme="light"]            →  (0,1,1)
```

اثباتِ عملی (دو قاعده با مقدارِ متضاد، `html` تاریک و `body` روشن): مرورگر مقدارِ `:has()` رو برمی‌گردونه. یعنی تا وقتی اون قاعده اون‌جا بود، قاعده‌ی سالم **هیچ‌وقت** نمی‌تونست خنثی‌اش کنه. سافاری در بازمحاسبه‌ی `:has()` بعد از تغییرِ اتریبیوتِ یک *فرزند* باگ داره — وقتی `body` از روشن به تاریک برمی‌گشت، اون سلکتور بیات می‌موند و `<html>` با رنگِ کرمی جا می‌موند. همون «بعد از یک دور سوییچ، رنگِ بالای صفحه گیر می‌کنه».

### چی تغییر کرد
- **همه‌ی `:has()` های وابسته به تم حذف شدن** — هم قاعده‌ی `background` هم چهار قاعده‌ی اسکرول‌بار. حالا همه مستقیم از `data-theme` روی خودِ `<html>` می‌خونن. (`:has()` های باقی‌مانده در فایل به *وجودِ عنصرِ فرزند* وابسته‌ن نه به اتریبیوتی که toggle می‌شه، پس این ریسک رو ندارن.)
- **`color-scheme` اضافه شد** (`dark` پیش‌فرض، `light` روی `html[data-theme="light"]`). این قطعه اصلاً گذاشته نشده بود. هر ناحیه‌ای که *خودِ مرورگر* رنگ می‌زنه — اورراسکرولِ لاستیکی، پشتِ ناچ قبل از اولین پینت، کنترل‌های فرم، نوارِ سیستمی — از `color-scheme` تصمیم می‌گیره نه از `background` ما. بدونش سافاری پیش‌فرضِ روشن رو فرض می‌کنه و توی تمِ تاریک اون نوارها با رنگِ روشنِ سیستمی پر می‌شن — همون حسِ «فول‌اسکرین نیست، بالا و پایین یه نوارِ غریبه هست».

### تست
- **۱۳/۱۳** سبز: لودِ اول، **ده دورِ کاملِ سوییچ پشتِ‌سرهم**، و ریلود بعد از سوییچ در هر دو تم — `html`، `body`، `color-scheme` و متا هر چهار تا همیشه هم‌گام.
- اختصاصیتِ `:has()` مستقیم در مرورگر اثبات شد (تستِ دو قاعده‌ی متضاد).

> صادقانه: باگِ `invalidation`ِ سافاری در این محیط قابلِ بازتولید نیست (کرومیوم این باگ رو نداره، پس تستِ بالا قبل از این تغییر هم سبز می‌شد). چیزی که *قابلِ اثبات* بود و اثبات شد، مشکلِ اختصاصیته؛ `color-scheme` هم راه‌حلِ استانداردِ همین دسته از باگ‌های iOSه. تأییدِ نهایی روی خودِ دستگاه لازمه.

## نسخه: v1.91.0 (چک‌اوتِ واقعیِ خرید اشتراک + چند فیکسِ UI)

### چی تغییر کرد
- **صفحه‌ی «اشتراک» داخلِ حساب حالا دقیقاً همون کارت‌ها/جدولِ مقایسه‌ی صفحه‌ی لندینگ رو نشون می‌ده** — کدِ پلن‌ها/جدول از لندینگ به یک کامپوننتِ مشترک (`components/PlanShowcase.tsx`) منتقل شد تا دو جا دوبار نوشته نشه؛ پلنِ فعلیِ کاربر به‌جای دکمه‌ی خرید یه نشانِ «پلنِ فعلیِ تو» می‌گیره.
- **چک‌اوتِ واقعی اضافه شد**: زدنِ «خرید این پلن» می‌بره به یک صفحه‌ی جدید (`/subscription/checkout`) با خلاصه‌ی سفارش، فیلدِ کدِ تخفیف (رفرالِ یه کاربرِ دیگه، ۱۰٪ تخفیف)، پذیرشِ قوانینِ سایت، و انتخابِ درگاه (فعلاً فقط زرین‌پال). دکمه‌ی پرداخت به `/api/subscription/checkout` وصله که مبلغ رو از جدولِ قیمتِ سمتِ سرور (نه ورودیِ کلاینت) حساب می‌کنه و درخواستِ پرداخت رو به زرین‌پال می‌فرسته؛ `/api/subscription/verify` بعدِ برگشت از درگاه، پرداخت رو با خودِ زرین‌پال تایید می‌کنه و اشتراک/دسترسیِ ماژول‌ها رو فعال می‌کنه. بدونِ `ZARINPAL_MERCHANT_ID` (که هنوز روی هیچ دیپلویی ست نشده)، چک‌اوت با پیامِ صادقانه‌ی «درگاه هنوز راه‌اندازی نشده» رد می‌شه، نه یه خطای خام.
- **آواتارِ دوستان برعکس شد**: حالا خودِ عکس/آواتارِ پیکسلی سمتِ راستِ اسمه (قبلاً چپش بود) — هم توی کارتِ خلاصه‌ی داشبورد هم پاپ‌آپِ دوستان.
- **دکمه‌ی بازگشتِ تکراری توی ویزاردِ ثبت‌نام حذف شد** — قبلاً دو تا دکمه‌ی بازگشت بود (یکی بالا-راست برای خانه، یکی بالا-چپ برای گامِ قبل)؛ حالا فقط یکی، بالا-راست، که روی گام‌های ۲/۳ به‌جای رفتن به خانه، یک گام برمی‌گرده.
- **فیلدِ «نام و نام‌خانوادگی» توی ثبت‌نام به دو فیلدِ جدا (نام / نام‌خانوادگی) تقسیم شد** — ستونِ `lastName` از قبل توی دیتابیس بود ولی جایی استفاده نمی‌شد؛ الان واقعاً ذخیره و توی پروفایل نمایش داده می‌شه.
- **باگِ واقعیِ چک‌باکسِ «این یک دوره است»** (فرمِ افزودنِ برنامه‌ی جدید): این چک‌باکس روی همون خطِ دکمه‌ی «افزودن روز دیگر» قبل از خودش می‌نشست (چون هردو `inline-flex` بودن) و وسطِ فرم معلق می‌موند؛ با تستِ زنده پیدا شد — الان `display:flex` می‌گیره تا خطِ خودش رو داشته باشه.
- **بک‌گراندِ باکسِ برنامه‌ی هفتگی در تمِ روشن از تینتِ سفید (که با پس‌زمینه‌ی کرمیِ خودِ تم مخلوط می‌شد و «شیری» دیده می‌شد) به تینتِ خودِ رنگِ زمینه با opacityِ بالاتر تغییر کرد — حالا واقعاً کرمی/توپر دیده می‌شه.

## نسخه: v1.90.0 (رنگِ نوارِ بالای صفحه بعد از سوییچِ تم گیر می‌کرد)

### باگ
بعد از یک دور عوض‌کردنِ تم، رنگِ نوارِ وضعیت/ناچ روی رنگِ تمِ *قبلی* گیر می‌کرد و دیگه عوض نمی‌شد.

علتش این بود که `themeColor` توی export‌ِ `viewport`ِ نکست تعریف شده بود — یعنی **نکست مالکِ اون تگ بود** — ولی اسکریپتِ inline و `ThemeProvider` مستقیم با `setAttribute` دستکاریش می‌کردن. سیستمِ متادیتای نکست بعدِ هیدریت نسخه‌ی خودش رو دوباره تزریق می‌کرد و صفحه با **دو تا** متا می‌موند:

```
<meta name="theme-color" content="#F4E3C9">   ← درست (تمِ روشن)
<meta name="theme-color" content="#0E1011">   ← بیات، دوباره تزریق‌شده
```

مرورگر بینشون یکی رو می‌گرفت و `ThemeProvider` هم فقط `querySelector` (یعنی *اولی*) رو آپدیت می‌کرد، پس اون یکی برای همیشه با رنگِ اشتباه می‌موند. بازتولیدشده: ریلود در تمِ روشن → دو متا (تست‌های ۷ و ۸ قرمز).

### چی تغییر کرد
- **`themeColor` از `viewport` برداشته شد** و کلاً به `lib/themeColor.ts` منتقل شد. حالا خودِ اپ تنها مالکِ اون تگه؛ `syncThemeColorMeta` همیشه دقیقاً **یکی** نگه می‌داره (اضافه‌ها رو حذف می‌کنه، اگه نبود می‌سازه).
- **متا در هر اجرای افکت هم‌گام می‌شه**، حتی اجرای اول — قبلاً اجرای اول کامل زودهنگام `return` می‌کرد، پس اگه چیزی بعدِ هیدریت متای دومی تزریق می‌کرد هیچ‌وقت پاک نمی‌شد.
- **`data-theme` حالا روی `<html>` هم هست، نه فقط `<body>`** — پس‌زمینه‌ی *خودِ* `<html>` همونیه که سافاری توی ناحیه‌ی امن (زیرِ ناچ، بالای نوارِ خانه) و موقعِ اورراسکرول نشون می‌ده. قبلاً این کار با `html:has(body[data-theme="light"])` انجام می‌شد؛ حالا یه سلکتورِ ساده کافیه، بدونِ وابستگی به `:has()`، و از همون **اولین بایتِ HTML** درست کار می‌کنه.
- **پدینگِ `body` اینست‌های واقعیِ دستگاه رو حساب می‌کنه** (`env(safe-area-inset-*)`). با `viewport-fit=cover` صفحه زیرِ ناچ و نوارِ خانه کشیده می‌شه ولی پدینگِ ثابتِ قبلی اون نواحی رو نمی‌دید — برای همین بالا/پایینِ صفحه «بریده» به‌نظر می‌رسید. روی دستگاهِ بدونِ ناچ این مقدار صفره، پس چیزی عوض نمی‌شه.

### تست
- **۸/۸** سناریوی تم سبز: لودِ اول، سوییچ به روشن، سوییچِ برگشت، ریلود در هر دو تم، و ناوبریِ کلاینتی در هر دو تم — همیشه دقیقاً **یک** متا با رنگِ درست.
- **۱۰/۱۰** روی پنج صفحه × دو تم: `html`، `body` و متا هر سه هم‌گام، بدونِ هیچ خطای JS یا ۴xx.
- با تزریقِ اینست‌های واقعیِ آیفونِ ناچ‌دار: پس‌زمینه در هر دو تم کلِ ناحیه‌ی امن رو می‌پوشونه.

> یه رگرسیون هم وسطِ کار ساخته و گرفته شد: نسخه‌ی اولِ فیکس، اتریبیوت رو در اجرای اولِ افکت از روی stateِ ری‌اکت می‌نوشت — ولی اون‌جا state هنوز مقدارِ اولیه‌ی «dark»ـه (عمداً، تا با رندرِ سرور یکی باشه) و این تمِ روشنِ کاربر رو موقعِ لود به تاریک برمی‌گردوند. حالا اجرای اول همون چیزی که اسکریپتِ inline روی DOM گذاشته رو مرجع می‌گیره (`readThemeFromDom`).

## نسخه: v1.89.0 (بازطراحیِ کارتِ دوستان + درخواست‌های دوستی + سرعتِ اعلان‌ها)

### چی تغییر کرد
- **کارتِ دوستان (خلاصه و پاپ‌آپ) برعکس شد**: حالا اسم/آواتار سمتِ راست، دایره‌ی وضعیت/دکمه‌ها سمتِ چپ — هم توی حالتِ خلاصه‌ی داشبورد هم توی لیستِ داخلِ پاپ‌آپ.
- **جستجوی دوست فقط با یوزرنیم**: قبلاً اسمِ نمایشی هم توی جستجو دخیل بود؛ الان فقط یوزرنیم مچ می‌شه (هم توی روتِ API هم توی placeholderِ اینپوت).
- **بخشِ «درخواست‌های دوستی» به پاپ‌آپِ دوستان اضافه شد** — قابلِ قبول/ردکردن مستقیماً همون‌جا؛ دیگه توی پنلِ اعلان‌ها نیست.
- **آواتارِ پیش‌فرضِ دوستان** وقتی عکسِ پروفایل ندارن، به همون کامپوننتِ پیکسلیِ `AgentAvatar` برمی‌گرده (نه رنگِ تصادفیِ قبلی).
- **اعلانِ «فلانی درخواستِ دوستیت رو قبول کرد»**: با قبول‌شدنِ درخواست، هم یک نوتیفِ پوش (اگه سابسکرایب باشه) هم امکانِ نمایش توی پنل اضافه شد.
- **بارگذاریِ پنلِ اعلانات سریع‌تر شد**: fetchهای مستقلش موازی اجرا می‌شن (به‌جای زنجیره‌ی سریال)، و یک `preloadNotifications()` جدید همزمان با بازشدنِ صفحه صدا زده می‌شه (مثلِ پریلودِ آواتار) — هم پنل سریع‌تر باز می‌شه هم یک بج‌ِ تعدادِ نخوانده روی زنگوله نشون داده می‌شه که با بازکردنِ پنل صفر می‌شه.
- **باگِ واقعیِ shadow روی day-pillِ انتخاب‌شده**: توی سوییچِ واحدِ «ثانیه/دقیقه»یِ فرمِ دستیِ ورزش (`.manual-timer-unit-picker`)، گلوی پسِ‌زمینه‌ی pillِ انتخاب‌شده رادیوسِ کوچیک‌تری از خودِ pill داشت و لبه‌ی تیز از زیرش بیرون می‌زد — با تستِ زنده پیدا و فیکس شد (رادیوسِ `::after` با خودِ pill یکی شد).
- **باکسِ تاریخِ تولد در لاگین/ثبت‌نام** حالا دقیقاً همون بک‌گراند/بوردر/بلورِ شیشه‌ایِ بقیه‌ی فیلدهای فرم رو داره (قبلاً استایلِ عمومیِ دکمه رو داشت، نه استایلِ اسکوپ‌شده‌ی فیلدهای فرمِ auth).
- **رگرسیونِ فونتِ دسکتاپ فیکس شد**: یک تغییرِ قبلی `--font-latin` رو قبل از فونتِ فارسی روی `body` و `button` سراسری گذاشته بود که باعثِ کلفت/نامتناسب‌شدنِ کلماتِ لاتین (مثلِ «Arion») کنارِ متنِ فارسی می‌شد — برگردونده شد به حالتِ قبل، فقط جاهایی که واقعاً محتوای عددی/لاتینِ خالص دارن اون فونت رو نگه داشتن.

## نسخه: v1.88.0 (تاییدِ پیامکیِ ثبت‌نام + رفعِ چند باگِ واقعی که با تست زنده پیدا شد)

### چی تغییر کرد
- **ثبت‌نام حالا شماره موبایل رو با یک کدِ ۵رقمیِ پیامکی تایید می‌کنه** — قدمِ جدیدی بینِ واردکردنِ شماره و بقیه‌ی فرم اضافه شد. مدلِ جدیدِ `SignupOtp` (بدونِ نیاز به یوزرِ از‌قبل‌موجود، برخلافِ `PasswordResetOtp`) و دو روتِ جدید (`/api/auth/signup/otp/request`, `/api/auth/signup/otp/verify`) همون زیرساختِ ملی‌پیامکِ فراموشی‌رمز رو دوباره استفاده می‌کنن. تاییدیه سمتِ سرور هم برای ثبت‌نامِ نهایی الزامیه (نه فقط چک کلاینتی) — با curl مستقیم به `/api/auth/signup` بدونِ تاییدیه تست شد و رد می‌شه.
- **باگِ واقعی در فرمِ «افزودن برنامه»**: روزِ شنبه به‌صورتِ پیش‌فرض توی انتخابگرِ روز «انتخاب‌شده» بود، ولی انتخابِ روزِ دیگه فقط بهش اضافه می‌کرد (نه جایگزینش می‌کرد) — یعنی هر برنامه‌ی جدید بی‌صدا هم روی روزِ دلخواه هم روی شنبه ثبت می‌شد و باعثِ خطای «تداخلِ زمانی» یا برنامه‌های تکراری می‌شد. با تستِ زنده (Playwright + دیتابیس واقعی) پیدا و فیکس شد — این احتمالاً ریشه‌ی گزارشِ «تیکِ برنامه رو نمی‌تونم بزنم» بوده.
- دکمه‌ی «خرید اشتراک» روی پیش‌نمایشِ تارِ ماژول‌های قفل، انیمیشنِ pulseِ اسکیل‌دار داشت که hit-boxش هر فریم جابه‌جا می‌شد — روی موبایل تپ‌کردنش می‌تونست نامطمئن به‌نظر برسه. حالا فقط سایه پالس می‌زنه، نه اندازه‌ی واقعیِ دکمه.
- بخشِ «چرا ما؟» صفحه‌ی لندینگ از یک مارکیِ بی‌پایان به یک گریدِ ثابت (بدونِ چرخش) تغییر کرد؛ چند عددِ فارسیِ هاردکد (قیمت/درصد) توی همون صفحه به انگلیسی تبدیل شد.
- آواتارِ پیش‌فرضِ پروفایل (canvas پیکسلی) دیگه رنگِ تصادفیِ هرکاربر رو نداره — سبز برای تمِ تاریک، قرمزنارنجی برای تمِ روشن (همون hueِ رنگِ accentِ خودِ تم)، تا با بقیه‌ی UI هماهنگ باشه.
- بلور+CTAِ ماژول‌های قفل (اشتراک‌نداشته) و بک‌گراندِ منوی سه‌نقطه‌ی «برنامه‌های امروز» با تستِ زنده (کاربرِ واقعیِ بدونِ اشتراک، دو تمِ روشن/تاریک) بررسی شدن — هردو از فیکس‌های قبلیِ همین سشن درست کار می‌کردن، نیازی به تغییرِ بیشتر نداشتن.

## نسخه: v1.87.0 (شروعِ زودهنگامِ درخواست‌های داده — کوتاه‌کردنِ زنجیرهٔ سریالِ لود)

### مسئله (اندازه‌گیری‌شده، نه حدسی)
لودِ لوکال هیچ‌وقت این رو نشون نمی‌داد چون تأخیرِ شبکه صفره. با تزریقِ تأخیرِ واقعی (۴۰۰ms در هر رفت‌وبرگشت، شبیهِ موبایلِ واقعی) روی `/weekly` معلوم شد:

```
 400ms  HTML می‌رسه
~800ms  باندلِ JS می‌رسه (رفت‌وبرگشتِ دوم)
~870ms  React هیدریت می‌کنه و *تازه* اولین درخواستِ داده می‌ره
~914ms  سنگین‌ترین‌ها (بازه‌های DailyEntry) تازه شروع می‌شن
~1200ms همه‌چیز رسیده
```

یعنی سه رفت‌وبرگشتِ **سریال** قبل از اولین بایتِ داده. خودِ درخواست‌ها سریع بودن — دیر **شروع** می‌شدن. روی `/exercise` بدتر بود: `/api/exercise/plan` در ۸۲۴ms و بعد `bodyMetrics` که منتظرش می‌موند، در ۱۰۴۲ms.

### چی تغییر کرد
- **`lib/preload.ts` + اسکریپتِ inline در `layout.tsx`** — درخواست‌های همیشگی همون لحظه‌ای که مرورگر HTML رو پارس می‌کنه فرستاده می‌شن، موازی با دانلودِ JS به‌جای بعدش. `lib/storage.ts` بعداً همون promiseها رو برمی‌داره، پس **هیچ درخواستِ اضافه‌ای ساخته نمی‌شه** — فقط زودتر شروع می‌شن.
- **بازهٔ DailyEntry هم پیش‌درخواست می‌شه** — عمداً یک بازهٔ *پهن* (۹۰ روز قبل تا ۷ روز بعد)، چون کشِ بازه‌آگاه هر زیربازه‌ای رو از یه بازهٔ پوشاننده می‌بره. این‌جوری لازم نیست حسابِ تاریخِ اسکریپت مو‌به‌مو با کامپوننت‌ها یکی باشه (که شکننده می‌بود). دو درخواستِ جدا شدن یکی.
- **پیش‌درخواستِ وابسته به مسیر** — `/exercise` هم `plan` هم `bodyMetrics` رو موازی می‌گیره (زنجیرهٔ سریال شکست)، `/trade` هم `checklist` و پنج کلیدِ تنظیماتش. `/api/trade/entries` عمداً بیرون موند: بازه‌اش ماهِ *جلالیِ* جاریه و تکرارِ اون حساب داخلِ اسکریپتِ inline شکننده می‌بود.
- **کوکیِ راهنمای `arion-auth`** — کوکیِ سشنِ next-auth از نوعِ httpOnlyه و JS نمی‌بیندش، پس بدونِ یه نشانهٔ جدا این اسکریپت برای مهمون‌ها هم چند تا ۴۰۱ الکی می‌فرستاد. **تصمیمِ امنیتی نمی‌گیره** — سرور همچنان خودش سشن رو چک می‌کنه؛ نبودنش فقط یعنی پیش‌درخواست انجام نمی‌شه. `SessionBridge` خودش با وضعیتِ واقعیِ سشن هم‌گام نگهش می‌داره (هم کاربرِ از قبل لاگین، هم سشنِ منقضی‌شده).
- **`SessionBridge`** — نتیجهٔ همون فچِ سشنی که `SessionProvider` می‌زنه رو به لایهٔ داده می‌رسونه، به‌جای `getSession()` که فچِ *مستقلِ دومی* می‌زد. `/api/auth/session` از ×۲ به ×۱ رسید.
- **اسکلتِ واقعی به‌جای `fallback={null}`** — `/exercise` و `/trade` هردو `<Suspense fallback={null}>` داشتن و چون محتواشون `useSearchParams()` صدا می‌زنه، Next همون `null` رو توی HTMLِ پری‌رندر می‌ذاشت: HTMLِ اولیهٔ این دو صفحه عملاً خالی بود (`/exercise` فقط ۴۳ کاراکتر متن). حالا `PanelSkeleton` — استاتیک، بدونِ JS — از همون اولین بایتِ HTML ساختارِ صفحه رو نشون می‌ده.
- **`dashboardPrefs` و `/api/account/avatar`** حالا از کش/پیش‌درخواستِ مشترک رد می‌شن (قبلاً `dashboardPrefs` فچِ خامِ خودش رو داشت، پس پیش‌درخواستش هدر می‌رفت و در تایم‌لاین دوبار دیده می‌شد).
- **`DashFriendsCard` برای مهمون دیگه درخواست نمی‌زنه** — دو تا ۴۰۱ در هر لودِ صفحه بود، در حالی که کارت همون حالتِ «وارد شوید» رو نشون می‌ده.

### نتیجه (RTT ۴۰۰ms، بیلدِ production، Postgres واقعی)
| صفحه | آخرین دادهٔ رسیده — قبل | بعد |
|---|---|---|
| `/exercise` | ۱۲۹۲ms | **۷۴۱–۷۸۴ms** |
| `/trade` | ۱۳۱۹ms | **۱۰۵۹–۱۱۲۸ms** |
| `/weekly` | ۱۲۱۰ms | **۸۶۴–۱۱۷۰ms** |

روی `/exercise` و `/weekly` همهٔ درخواست‌های بحرانی حالا در **یک موجِ موازی حدودِ ۵۱۰ms** می‌رن، به‌جای سه موجِ پشتِ‌سرهم.

> اعداد بینِ اجراها ~۲۰۰ms نوسان دارن؛ بازه‌ها همون نوسانِ واقعی‌ان، نه تخمین.

> چیزی که امتحان شد و **برداشته شد**: `SessionBridge` به‌تنهایی یه درخواست کم کرد ولی زمان رو عملاً تکان نداد — فرض اولیه‌ام که اون فچِ دوم *سریال* بود غلط بود، موازی بود. نگه داشته شد چون یه درخواستِ تکراری رو حذف می‌کنه و پلِ لازم برای هم‌گام‌سازیِ کوکیِ راهنماست، ولی سودِ زمانیش به‌ حسابِ این تغییر نوشته نشده.

## نسخه: v1.86.0 (کشِ بازه‌آگاه برای DailyEntry — حذفِ درخواست‌های هم‌پوشان)

### چی تغییر کرد
- **کشِ `lib/storage.ts` کلیدش URL بود، پس دو بازه‌ی تاریخیِ متفاوت هیچ‌وقت به‌هم نمی‌رسیدن — حتی اگه یکی کاملاً داخلِ اون یکی بود.** اندازه‌گیری روی `/weekly` نشون داد چهار درخواستِ هم‌پوشان از یک جدول می‌رفت: `range 08-13..08-27` (تایم‌لاینِ صفحه)، `range 05-22..08-19` (استریکِ ۹۰ روزه)، `range 08-15..08-21` (آمارِ همین هفته)، و `daily 08-20` (امروز — که *داخلِ* بازه‌ی اول بود).
- حالا بازه‌های درخواست‌شده نگه داشته می‌شن و اگه بازه‌ی تازه (یا یک روزِ تکی) زیرمجموعه‌ی یکی از اون‌ها باشه — چه رسیده باشه چه هنوز در راه — از همون بریده می‌شه، بدونِ درخواستِ جدید. **`/weekly`: ۱۵ → ۱۳ درخواست.**
- بازه‌ی ناموفق از کش بیرون انداخته می‌شه (تا یه قطعیِ لحظه‌ای بقیه رو گمراه نکنه) و اگه بازه‌ی پوشاننده شکست بخوره، خودِ درخواست‌کننده مستقیم می‌گیره، نه اینکه خالی برگردونه. نوشتن (`setDaily`) و ورود/خروج هم این کش رو پاک می‌کنن.

> چیزی که امتحان شد و **برداشته شد**: یه پنجره‌ی batchingِ هم‌تیکی که بازه‌های هم‌زمان رو در یک درخواستِ اجتماع‌شده بگیره. توی عمل هیچ سودی نداشت — دو بازه‌ی باقی‌مانده در دو موجِ جدا (با یک رفت‌وبرگشتِ شبکه فاصله) درخواست می‌شن، نه در یک تیک. ~۶۰ خط کدِ بی‌اثر بود، پس نگه داشته نشد.

## نسخه: v1.85.0 (سوییچِ فراخوانِ AI از Anthropic به گیت‌وی آروان‌کلود)

### چی تغییر کرد
- **همه‌ی چهار جای اپ که از AI استفاده می‌کنن** (تولید رودمپ، برنامه‌ی هوشمندِ ورزشی، جایگزینیِ حرکت، اسکنِ عکسِ غذا) دیگه مستقیم به Anthropic Messages API وصل نیستن — به گیت‌وی AIِ آروان‌کلود (GPT-4o-mini، با یک API سازگار با OpenAI روی endpointِ `chat/completions`) سوییچ کردن. `lib/anthropic.ts` به `lib/aiClient.ts` تغییرِ نام داد و همه‌ی ۴ فراخوان از یک تابعِ مشترک (`callAiChat`) رد می‌شن.
- **خروجیِ خام‌JSON دو لایه تضمین می‌شه**: هم با `response_format:{type:"json_object"}` در سطحِ خودِ فراخوان (پارامترِ استانداردِ OpenAI-compatible که مدل رو مجبور می‌کنه فقط JSON برگردونه)، هم با system promptِ صریح — دقیقاً همون الگویی که قبلاً برای Anthropic هم رعایت می‌شد.
- **آدرسِ گیت‌وی (شاملِ خودِ توکنِ دسترسی) هیچ‌جا کامیت نشده** — فقط از `process.env.ARVAN_AI_BASE_URL` خونده می‌شه؛ متغیرِ جدید توی `.env.example` (بدون مقدارِ واقعی) مستند شده، دقیقاً مثلِ `ANTHROPIC_API_KEY` قبلی.
- ⚠️ **این تغییر توی سندباکس تست‌شونده نیست**: شبکه‌ی این محیط به `arvancloudai.ir` دسترسی نداره (فقط چند دامنه‌ی محدود مثل registry.npmjs.org/anthropic.com اجازه دارن)، پس فراخوانِ واقعی به گیت‌وی از این‌جا قابل‌تأیید نبود. کد بر اساسِ قراردادِ استانداردِ OpenAI Chat Completions نوشته شده (که تقریباً همه‌ی گیت‌وی‌های `/v1` همینو پیاده می‌کنن)، ولی بعدِ دیپلوی حتماً یک تستِ واقعی (مثلاً ساختِ یک رودمپ) لازمه.

### برای دیپلوی
باید `ARVAN_AI_BASE_URL` رو با آدرسِ کاملِ گیت‌وی (همونی که شاملِ توکنه) به `.env` روی VPS اضافه کنی — دقیقاً مثلِ بقیه‌ی متغیرهای حساس، توی گیت کامیت نشه.

## نسخه: v1.84.0 (سخت‌سازیِ امنیتی ورودی‌ها + رفعِ N+1 و سبک‌سازیِ باندلِ ورزش)

### امنیت — هر مورد قبل از فیکس با حمله‌ی واقعی روی بیلدِ production تأیید شد

- **SQL Injection: ساختاراً ممکن نبود، و حالا نگهبان هم داره.** کلِ ریپو گشته شد: هیچ `$queryRaw`/`$executeRaw`/`*Unsafe`ای وجود نداره، هیچ‌جا ورودیِ کاربر با رشته داخلِ Prisma spread نمی‌شه (`where: {...body}`)، و همه‌ی کوئری‌ها شکلِ ثابت و پارامتری دارن — یعنی مقدارِ کاربر هیچ‌وقت به‌عنوان *ساختارِ* SQL تفسیر نمی‌شه. برای اینکه این تضمین با کدِ آینده نشکنه، `npm run check:sql` اضافه شد که APIهای خامِ Prisma رو می‌گیره و بیلد رو fail می‌کنه.
- **همه‌ی روت‌های تاریخ‌دار با ورودیِ بدشکل ۵۰۰ می‌دادن.** `?date=garbage`، `?date=9999-99-99`، `?date=2026-02-31` (سرریزِ تقویمی) همگی `new Date()` رو به `Invalid Date` می‌بردن، Prisma throw می‌کرد و روت با **HTTP 500ِ هندل‌نشده** می‌افتاد — هم راهِ ارزونِ اذیت‌کردنِ سرویس، هم ریختنِ استکِ خطا توی لاگ. `parseIsoDate()` اضافه شد (فقط `YYYY-MM-DD`ِ واقعی، با گرفتنِ سرریزِ تقویمی) و همه‌ی این روت‌ها حالا ۴۰۰ می‌دن.
- **بازه‌های تاریخیِ بی‌انتها.** `from=0001-01-01&to=9999-12-31` با ۲۰۰ جواب می‌گرفت و عملاً یه full-table scan بود. حالا `parseDateRange()` سقفِ ۴۰۰ روز می‌ذاره و روت‌های پرحجم `take` هم گرفتن.
- **`/api/settings/[key]` یه فروشگاهِ کلید/مقدارِ نامحدود بود.** یک کاربرِ لاگین‌کرده می‌تونست (۱) بی‌نهایت کلیدِ دلخواه بسازه و **۳ مگابایت** توی هرکدوم بریزه — تست شد، قبول و ذخیره و کامل هم پس داده شد؛ و (۲) `pushSentLog` رو بازنویسی کنه، همون کلیدی که کرانِ سرور می‌خونه تا بفهمه یادآوریِ امروز فرستاده شده — با تاریخِ آینده، یادآوری‌ها برای همیشه خفه می‌شدن. حالا allowlistِ کلیدها (`lib/userSettingKeys.ts`)، جداسازیِ کلیدهای «مدیریت‌شده توسط سرور»، و سقفِ ۶۴KB روی هر مقدار.
- **هیچ سقفی روی بدنه‌ی درخواست نبود.** Next برای route handlerها پیش‌فرضی نمی‌ذاره. `readJsonBody()` اضافه شد (سقفِ ۲۵۶KB، سنجشِ بایتِ واقعی نه طولِ رشته، چون متنِ فارسی چندبایتیه) و روت‌های نویسنده بهش وصل شدن.
- **`/api/market/prices` کاملاً باز بود.** بدونِ هیچ حسابی ۲۰۰ می‌داد — یعنی هرکسی می‌تونست از این سرور به‌عنوان پروکسیِ رایگانِ یاهو استفاده کنه (هزینه‌ی پهنای‌باند + ریسکِ بلاک‌شدنِ IPِ سرور). بدتر: کشِ داخلیش هیچ سقف/تخلیه‌ای نداشت، پس با نمادهای ساختگیِ بی‌نهایت می‌شد بی‌سروصدا حافظه‌ی پروسه رو پر کرد. حالا لاگین لازمه، سقفِ نرخ داره، نماد با regex اعتبارسنجی می‌شه، و کش سقفِ ۲۰۰ ورودی با تخلیه‌ی LRU-ish داره.
- **ورودی‌های جستجو بی‌سقف بودن** (`notepad/search`، `friends/search`، `calorie/foods`) — یه `q`ی چندصدکیلوبایتی روی هر ردیف/بلاک اجرا می‌شد. همه با `clampQuery()` بریده شدن.
- **رمزِ کرانِ `/api/push/send-reminders` با `!==` مقایسه می‌شد** (نشتِ زمانی). حالا `timingSafeEqual`، با رسیدگی به طولِ نابرابر.
- **سقف روی ستون‌های `Json`**: `completedItems` (روتین و ورزش) هر چیزی که فرستاده می‌شد رو عیناً ذخیره می‌کرد؛ حالا فقط کلیدهای معتبر، با سقفِ تعداد و طول.

### بهینه‌سازی

- **N+1 در `/api/friends`** — برای *هر* دوست ۵ کوئری جدا می‌زد (تنظیمات + رکوردِ امروز + ۹۰ روز تاریخچه + ساعتِ بیداری). با pg_stat_statements اندازه گرفته شد: **۲۶ کوئری برای ۲۰ دوست → ۵ کوئری**، و مهم‌تر اینکه دیگه با تعدادِ دوست رشد نمی‌کنه (۱۰۰ دوست هم همون ۵ تا). زمانِ پاسخ روی لوکال ۶۵ms → ۲۷ms.
- **N+1 در کرانِ یادآوری‌ها** — به‌ازای هر کاربرِ سابسکرایب‌شده ۴ تا ۶ کوئریِ جدا برای تنظیمات، و یک نوشتنِ دیتابیس به‌ازای *هر یادآوری*. حالا یک کوئریِ دسته‌ای برای خواندن و یک نوشتن به‌ازای هر کاربر.
- **۸۵KB کاتالوگِ حرکات از باندلِ صفحه‌ی `/exercise` بیرون رفت.** ریشه‌ش یه لبه‌ی نامرئی بود: چند کامپوننتِ کلاینتی فقط برای *typeِ* `ExerciseDay` ماژولِ `exercisePlans` رو import می‌کردن، و اون ماژول `EXERCISE_CATALOG` رو static import می‌کرد — پس کلِ اون دیتا توی باندلِ صفحه می‌نشست، حتی برای کسی که هیچ‌وقت لیستِ حرکات رو باز نمی‌کنه. توابعِ وابسته به کاتالوگ به `lib/exerciseCatalogUtils.ts` منتقل شدن، importهای صرفاً typeای به `import type` تبدیل شدن، و دو مودالِ سنگین `dynamic()` شدن. نتیجه: **`/exercise` از ۴۹.۴kB/۲۳۵kB به ۳۳.۵kB/۲۱۶kB**؛ کاتالوگ حالا یه chunkِ جداست که فقط با کلیک لود می‌شه.

### نگهبان‌های جدید (`npm run check`)
- `check:sql` — هر SQLِ خامی که به کد اضافه بشه رو می‌گیره.
- `check:settings` — هر کلیدِ تنظیماتی که استفاده بشه ولی توی allowlist نباشه رو می‌گیره. (لازم بود: همین بررسی شش کلیدِ trade/bodyMetrics رو گرفت که اولین نسخه‌ی allowlist جا انداخته بود و در production بی‌سروصدا با ۴۰۰ می‌شکستن.)

### چطور تست شد
بیلدِ production + Postgres واقعی + کاربرِ تست + ۲۰ دوستِ seed‌شده:
- **۱۸ تستِ حمله/رگرسیون، همه سبز** — تاریخِ بدشکل ۴۰۰، بازه‌ی بی‌انتها ۴۰۰، کلیدِ دلخواه ۴۰۰، `pushSentLog` ۴۰۰، بدنه‌ی ۳MB ۴۱۳، market بدونِ لاگین ۴۰۱، کرانِ بدون/غلط ۴۰۱ و با رمزِ درست ۲۰۰، و همه‌ی مسیرهای سالم همچنان ۲۰۰.
- تستِ مرورگری روی ۷ صفحه: بدونِ خطای JS و بدونِ هیچ ۴xx/۵xxِ ناخواسته؛ تغییرِ تم و پایداریش بعد از ریلود سالم؛ مودالِ کاتالوگ بعد از lazy-load درست باز می‌شه و دیتای واقعی نشون می‌ده.

## نسخه: v1.83.0 (ریشه‌ی واقعیِ کندیِ لود: طوفانِ درخواست‌های API + یک مگابایت تصویرِ بهینه‌نشده)

### چی تغییر کرد
- **طوفانِ درخواست‌های API (علتِ اصلیِ «دیر لود می‌شه / ناقص لود می‌شه»)**: اندازه‌گیریِ واقعی با مرورگرِ headless روی بیلدِ production نشون داد یک بار باز کردنِ `/weekly` برای کاربرِ لاگین‌کرده **۳۵ درخواستِ API** می‌زد — `/api/auth/session` ×۶، `settings/removedOccurrences` ×۶، `settings/customOccurrences` ×۶، `tasks/daily/range` ×۵ (که سه‌تاش دقیقاً یک بازه‌ی تکراری بود)، `tasks/daily` ×۳ و… . دلیلش این بود که هر کامپوننتی که به این داده‌ها نیاز داشت (`NotificationEngine`، `useMyStreak`، `DashReminderCard`، `HistoryCalendar`، `getTodayStats`، خودِ صفحه) مستقلاً همون کلیدهای ثابت رو می‌خوند. مرورگر به هر هاست فقط ~۶ کانکشنِ همزمان می‌ده، پس این درخواست‌ها چند «موج» پشتِ سرِ هم می‌شدن و صفحه تیکه‌تیکه/دیر بالا می‌اومد. حالا **۳۵ → ۱۵** درخواست.
- **لایه‌ی کش/دیدوپ در `lib/storage.ts`**: (۱) وضعیتِ لاگین دیگه به‌ازای هر خواندنِ داده یک فچِ تازه به `/api/auth/session` نمی‌زنه — قبلاً هر `getSetting`/`getDaily` عملاً **دو رفت‌وبرگشتِ سریالیِ** شبکه بود (اول session، بعد خودِ داده)، یعنی عمقِ waterfall دو برابر. (۲) پاسخِ GETهای خواندنی (`settings/*`، `tasks/daily`، `tasks/daily/range`) با TTLِ کوتاه کش و دیدوپ می‌شه. نوشتن‌ها write-through هستن (`setSetting`/`setDaily` کش رو با مقدارِ تازه پر می‌کنن و بازه‌های متاثر رو دور می‌ریزن)، پس هیچ داده‌ی بیاتی دیده نمی‌شه.
- **پاک‌سازیِ کش موقعِ ورود/خروج**: چون لاگین با `signIn(redirect:false)` + ناوبریِ کلاینتیه (نه ریلودِ کامل)، بدونِ `invalidateStorageCache()` صفحه‌ی بعد از ورود همچنان داده‌ی «مهمان» رو نشون می‌داد. صفحه‌ی ورود و دکمه‌های خروج (منو + پنلِ کاربری) حالا صریحاً صداش می‌زنن.
- **~۱.۳MB تصویرِ بهینه‌نشده روی *هر* صفحه**: `sharp` نصب نبود، پس بهینه‌سازِ تصویرِ نکست عملاً بی‌اثر بود و PNGِ خام رو دست‌نخورده پس می‌داد (`/_next/image?...&w=384` → همون ۳۰۹KB). خودِ فایل‌ها هم چند برابرِ ابعادِ نمایششون ذخیره شده بودن: لوگوی هدر ۸۷۳×۲۱۵ / ۳۰۹KB، `logo-icon` ۴۲۷KB برای عنصری که ۳۸px دیده می‌شه، و favicon (`app/icon.png`) ۱۹۶KB. با نصبِ `sharp` + فشرده‌سازیِ منابع (`scripts/optimize-images.js`) مجموع به **۱۳۶KB** رسید و بهینه‌ساز حالا واقعاً WebP می‌ده (۳۰۹KB → ۱۴.۵KB).
- **پری‌لودِ فونت‌ها**: `next/font` برای هر وزن و هر subset یک `<link rel="preload">` با بالاترین اولویت می‌ذاشت — ۹ پری‌لود و ~۲۵۰KB فونت که با CSS/JSِ بحرانی سرِ پهنای‌باند رقابت می‌کرد. `IBM_Plex_Sans_Arabic` (۶ تا از اون ۹ تا) در همه‌ی استک‌های `globals.css` جایگاهِ *دوم* داره، پس `preload:false` شد: همچنان دقیقاً همون‌جاها استفاده می‌شه، فقط با اولویتِ عادی. حالا ۳ پری‌لود / ۱۲۷KB.
- **`/api/account` دو بار در هر لودِ صفحه**: `NavDrawer` (قفلِ آیتم‌های منو) و `ModuleGate` (گیتِ صفحه) هردو مستقلاً همون پاسخ رو می‌گرفتن — با `lib/accountCache.ts` (همون الگوی دیدوپ + کشِ کوتاه) به یکی رسید.
- **یک نوشتنِ بی‌مورد در دیتابیس به‌ازای هر بازدیدِ هر صفحه**: `ThemeProvider` بعد از خوندنِ تمِ ذخیره‌شده، همون مقدار رو دوباره ذخیره می‌کرد (`GET /api/settings/theme` بعدش یک `POST` با همون مقدار). حالا فقط انتخابِ واقعیِ کاربر نوشته می‌شه؛ ضمناً ریسِ «کاربر قبل از رسیدنِ مقدارِ ذخیره‌شده تم رو عوض کنه» هم بسته شد.
- **`getTodayStats()` دو بار در `useEffect`ِ صفحه‌ی هفتگی** صدا زده می‌شد (یکی مستقیم، یکی داخلِ `refresh()`) — سه درخواستِ شبکه‌ی تکراری، بدونِ هیچ داده‌ی تازه‌ای.
- **ریسِ فچِ `useDashboardPrefs`**: کشِ ماژولی داشت ولی دیدوپ نداشت، پس کارت‌هایی که هم‌زمان mount می‌شدن همه قبل از رسیدنِ اولین جواب کش رو خالی می‌دیدن و هرکدوم جدا فچ می‌زدن.

### نتیجه‌ی اندازه‌گیری‌شده (بیلدِ production، مرورگرِ headless)
| | قبل | بعد |
|---|---|---|
| درخواستِ API در لودِ `/weekly` | ۳۵ | ۱۵ |
| درخواست‌های تکراریِ بی‌مورد | `session`×۶ · `settings`×۶ ×۲ · `range`×۳ تکراری | فقط ۳ بازه‌ی *متفاوت* + `session`×۲ |
| حجمِ کلِ صفحه‌ی اول (مهمان) | ۱۰۲۷KB | ۴۱۱KB |
| تصاویر | ~۵۰۰KB | ۲۸KB |
| فونت (پری‌لودشده) | ۹ فایل / ~۲۵۰KB | ۳ فایل / ۱۲۷KB |
| first-paint (لوکال) | ۵۸۰ms | ۱۰۸ms |

> بررسیِ سلامتِ بک‌اند: بیلد بدونِ خطا کامل می‌شه، همه‌ی migrationها اجرا می‌شن، `/api/health` سالمه، ورود/ثبتِ داده/پایداریِ بعد از ریلود تست شد، و ایندکس‌های Prisma (`@@unique([userId,key])`، `@@unique([userId,date])` و…) برای همه‌ی کوئری‌های داغِ این مسیرها موجودن. هیچ روتِ API به‌تنهایی کند نبود — مشکل تعدادِ درخواست‌ها بود، نه هزینه‌ی هرکدوم.

## نسخه: v1.82.0 (فیکسِ دسته‌جمعیِ «روتین من» — کارت‌ها، تایم‌لاین، فرمِ افزودنِ برنامه)

### چی تغییر کرد
- **کلیکِ کارتِ برنامه محدود به اسم شد**: توی لیستِ «برنامه‌های امروز» (DashTaskRow)، قبلاً کلیک روی کلِ ردیف (تگ/بج‌اهمیت/ساعت هم) کارتِ برنامه رو باز می‌کرد؛ حالا فقط خودِ متنِ اسمِ برنامه کلیک‌پذیره.
- **قابلیتِ بازشدنِ کارت از یادآوری‌ها حذف شد**: کارتِ «یادآوری‌ها» دیگه هیچ‌جاش کلیک‌پذیر برای بازکردنِ ProgramCard نیست — تنها تعاملِ ممکن روی هر ردیف، دکمه‌ی زنگوله‌ست.
- **تایم‌لاینِ برنامه‌ی هفتگی دیگه اسکرولِ افقی نداره**: قبلاً روزهای شلوغ باعثِ بزرگ‌شدنِ عرضِ track و اسکرول می‌شد؛ حالا track همیشه دقیقاً عرضِ کارت رو می‌گیره، الگوریتمِ ضدِّ همپوشانیِ فاصله‌ها (lib/weeklyTimeline.ts) دست‌نخورده می‌مونه.
- **کارتِ دوستان برای بیش از ۳ نفر دیگه بزرگ نمی‌شه**: لیستِ کوچیکِ داشبورد سقفِ ارتفاع گرفت (اسکرولِ بی‌نوار به‌جای رشدِ کارت).
- **منویِ سه‌نقطه‌ی «برنامه‌های امروز» دیگه پشتِ کارت‌های دیگه نمی‌ره**: چون DashCard با backdrop-blur یه stacking-context جداگونه می‌سازه، z-index داخلش محدود به همون کارت می‌موند؛ الان با createPortal به body می‌ره (مثلِ بقیه‌ی پاپ‌آپ‌های اپ) و این مشکل کلاً حل شد.
- **تگِ برنامه برگشت کنارِ اسمِ برنامه** (نه گوشه‌ی چپِ باکس، کنارِ ساعت).
- **باگِ صفحه‌ی اول «روتین من» (چسبیدنِ فیلدها به چپ) پیدا و رفع شد**: تنها جای اپ که `<input type="time">` خام استفاده می‌کرد همین صفحه بود — این نوع اینپوت توی اکثرِ مرورگرها `text-align` رو نادیده می‌گیره و همیشه چپ‌چین می‌مونه. با همون کامپوننتِ TimeInput که همه‌جای دیگه‌ی اپ استفاده می‌شه جایگزین شد.
- **قبل از این صفحه، یه راهنمای کوتاه اضافه شد**: بارِ اولی که کاربر بدونِ تنظیمِ قبلی وارد می‌شه، اول یه باکسِ خوش‌آمدگویی/توضیح می‌بینه، بعد فرمِ ساعتِ بیداری/خواب.
- **دکمه‌ی «بعدی» در افزودنِ برنامه بزرگ‌تر شد**: یه قاعده‌ی CSS با `!important` استایلِ inlineِ بزرگ‌ترکننده رو بی‌اثر می‌کرد؛ با یه کلاسِ مادیفایرِ اختصاصی حل شد.
- **چک‌باکسِ «این یک دوره است» با متنش هم‌ردیف شد**: یه قاعده‌ی عمومی‌تر (`.wsearch-newform label{display:block}`) اسپسیفیسیتیِ بالاتری از استایلِ فلکسِ چک‌باکس داشت. اندازه‌ی چک‌باکس هم کمی بزرگ‌تر شد (۱۹→۲۲px) برای لمسِ راحت‌تر.
- **رنگِ متنِ روزهای هفته موقعِ دی‌سلکت‌کردن دیگه سفید نمی‌مونه**: افکتِ hover فقط رویِ دستگاه‌های با ماوسِ واقعی (`hover:hover`) اعمال می‌شه — روی موبایل، hoverِ بعدِ تپ «چسبیده» می‌موند و ظاهرِ انتخاب‌شده رو حفظ می‌کرد.
- **«افزودن روز دیگر» حالا با انیمیشن باز می‌شه** (نه یهویی) — ردیفِ جدید با فیدِ ارتفاع/شفافیت وارد می‌شه.
- **دکمه‌ی «حذف این روز» به سمتِ راست منتقل شد و یه آیکونِ منفی گرفت** — قبلاً `justify-content:flex-end` توی یه فلکسِ RTL اشتباهاً سمتِ چپ می‌نداختش.
- **همیشه حداقل یک روز انتخاب‌شده می‌مونه** — دیگه نمی‌شه با کلیک، آخرین روزِ باقی‌مونده رو هم دی‌سلکت کرد.
- **خطاهای اعتبارسنجی حالا زیرِ فیلدها نوشته می‌شن**: قبلاً فقط بوردرِ قرمز نشون داده می‌شد (بدونِ متن) به‌جز برای تاریخِ دوره؛ حالا اسم/روزها/ساعت‌ها هم پیامِ متنیِ مشخص دارن.

## نسخه: v1.81.0 (آواتارِ واقعیِ agent-avatar از smoothui)

### چی تغییر کرد
- **آواتارِ پیش‌فرضِ پروفایل عوض شد**: نسخه‌ی قبلی (حرفِ اول روی گرادیانِ چرخان) با کدِ واقعیِ کامپوننتِ `agent-avatar` از smoothui جایگزین شد — این‌بار عیناً، چون کاربر خودش سورس‌کدِ اصلیِ کامپوننت رو داد (شبکه‌ی سندباکس به `smoothui.dev`/`ui.shadcn.com` بسته‌ست، هر دو با ۴۰۳ از پروکسی رد می‌شدن، طبق `curl`/`npx shadcn add` هم دوباره تأیید شد).
- کامپوننتِ جدید یه `<canvas>`ه که با seedِ قطعی (اسم/یوزرنیم/ایمیلِ کاربر) یه گریدِ ۶×۶ پیکسلِ رنگی می‌کِشه — هر کاربر همیشه دقیقاً همون الگو/پالت رو می‌گیره — با چهار لایه‌ی انیمیشنِ هم‌زمان (pulseِ تک‌پیکسلی، breathe سراسری، موجِ مورب، جرقه‌های تصادفی) و یه حلقه‌ی glowِ بیرونی؛ به `prefers-reduced-motion` احترام می‌ذاره.
- `AgentAvatar` توی `NavDrawer.tsx` (سایز ۲۷) و `AccountPanel.tsx` (سایز ۷۶) به prop جدید (`seed` به‌جای `name`) وصل شد؛ CSSِ قدیمیِ letter-fallback (بک‌گراند/بوردر/حرفِ اول دستی) حذف شد چون canvas خودش کاملاً خودکفاست.

## نسخه: v1.80.0 (عیب‌یابیِ کندی/هنگِ سایت — صفحه‌ی اصلی دیگه خالی سرو نمی‌شه)

### چی تغییر کرد
- **ریشه‌ی «سایت بالا نمیاد، چند بار باید ریلود بزنم» پیدا و رفع شد**: `app/page.tsx` روی `status === "loading"` مقدارِ `null` برمی‌گردوند. چون این کامپوننت سمتِ سرور هم با همون وضعیتِ اولیه («loading») پری‌رندر می‌شه، یعنی HTMLِ استاتیکِ صفحه‌ی اصلی **عملاً خالی تولید می‌شد** — بازدیدکننده تا وقتی که کلِ باندلِ JS دانلود/پارس بشه، React هیدریت کنه، و یه فچِ شبکه‌ای به `/api/auth/session` برگرده، صفحه‌ی سفید می‌دید؛ و اگه اون فچ کند/ناموفق بود، صفحه اصلاً بالا نمی‌اومد. اندازه‌ی HTMLِ پری‌رندرشده‌ی `/` از **۲۳KB (پوسته‌ی خالی) به ۱۰۴KB (لندینگِ کامل)** رسید — یعنی حالا محتوا بدونِ هیچ JSای و بدونِ هیچ درخواستِ شبکه‌ای دیده می‌شه.
- **باگِ nginx که باعثِ هنگ‌های متناوب می‌شد**: `proxy_set_header Connection "upgrade"` روی *همه‌ی* درخواست‌ها ثابت ست شده بود، نه فقط WebSocket — برای یه درخواستِ HTTPِ معمولی این keep-alive رو خراب می‌کنه و کانکشن‌ها نیمه‌کاره می‌مونن. با `map $http_upgrade $connection_upgrade` (الگویِ استاندارد) درست شد.
- **بقیه‌ی بهبودهای nginx**: `upstream` با `keepalive 64` (به‌جای TCP handshakeِ تازه برای هر درخواست)، فعال‌کردنِ gzip، کشِ دائمیِ `/_next/static` (فایل‌هاش هش‌دارن پس امنه — بازدیدکننده‌ی برگشتی اصلاً به Node نمی‌رسه)، و `proxy_read_timeout` از ۶۰ ثانیه به ۱۵ ثانیه (تا یه لحظه گیرکردنِ Node یه دقیقه صفحه‌ی سفید نده).
- **`/api/health`**: روتِ سبک که عمداً به دیتابیس دست نمی‌زنه و فقط uptime و مصرفِ حافظه رو برمی‌گردونه. به‌عنوان healthcheckِ داکر وصل شد تا وضعیتِ واقعیِ اپ توی `docker compose ps` دیده بشه، و برای عیب‌یابیِ دستیِ نشتیِ حافظه.
- **سقفِ heapِ Node** (`NODE_HEAP_MB`، پیش‌فرض ۵۱۲MB): بدونش Node روی VPSِ کوچیک تا نزدیکِ کلِ RAM heap می‌گیره و کلِ ماشین (از جمله Postgres) به swap می‌خوره — از بیرون شبیهِ «سایت هنگ کرده».
- **هزینه‌ی گرافیکیِ شیشه‌ها روی موبایل**: ۳۹ عنصر با `backdrop-filter:blur(20–32px)` وجود داشت؛ این گرون‌ترین افکتِ وبه (هر فریم، هرچی پشتِ عنصره دوباره بلور می‌شه) و روی GPUِ موبایل موقعِ اسکرول باعثِ افت فریم می‌شه. فقط روی `pointer:coarse` شعاعِ بلور ~۵۵٪ کم شد؛ `saturate` و رنگ/بوردر/سایه دست‌نخورده موندن، دسکتاپ هم اصلاً تغییر نکرده.

## نسخه: v1.79.0 (فیکسِ دسته‌جمعیِ موبایل + محدودسازیِ رودمپ/نوتپد به سوپرادمین)

### چی تغییر کرد
- **زوم موبایل**: فوکوس روی اینپوت‌ها دیگه صفحه رو زوم نمی‌کنه (فونتِ اینپوتِ مشترک زیرِ ۶۴۰px به ۱۶px رسید — زیرِ این مقدار سافاری خودکار زوم می‌کنه) و کلاً پینچ/دابل‌تپ زوم رویِ کلِ سایت غیرفعال شد (`maximumScale`/`userScalable` در `app/layout.tsx`).
- **بک‌گراندِ کانواس با تأخیر جدا از منو لود می‌شد**: `BackgroundCanvasLoader` با `ssr:false` رندر می‌شد یعنی مارک‌آپِ استاتیکش فقط بعدِ hydration ظاهر می‌شد؛ حذف شد تا همراهِ اولین رندرِ سرور بیاد.
- **بک‌گراند کل صفحه رو نمی‌گرفت (کوتاه از بالا/پایین)**: `body` حالا `min-height:100dvh` هم داره (علاوه بر fallback قدیمیِ `100vh`) تا با نوارِ پویایِ مرورگرِ موبایل هماهنگ بمونه.
- **اندازه‌ی پنل کاربری/اطلاعیه رویِ موبایل خیلی بزرگ بود**: `.notif-panel` جمع‌وجورتر شد و یه override جدید برای `.account-modal-panel` زیرِ ۴۸۰px اضافه شد.
- **فونتِ لاتین/اعداد زشت بودن**: چون هر دو فونتِ فارسی (Vazirmatn، IBM Plex Sans Arabic) گلیفِ لاتینِ خودشون رو دارن، هیچ‌وقت نوبت به فالبک نمی‌رسید؛ یه فونتِ فقط-لاتین (Inter) اول‌ِ استکِ فونت اضافه شد که چون گلیفِ فارسی نداره متنِ فارسی درست فال‌بک می‌کنه به بقیه، ولی لاتین/اعداد حالا با Inter رندر می‌شن (۷۵ جا در `globals.css`).
- **بک‌گراندِ باکسِ برنامه‌ی هفتگی خیلی شیری بود**: `.weekly-glass` مثلِ بقیه‌ی گلس‌کارت‌ها یه بک‌گراندِ ثابت + اورراید مخصوصِ تمِ روشن گرفت (قبلاً اصلاً اورراید نداشت).
- **سوییچِ دارک/لایت توی سافاری پایینِ صفحه گیر می‌کرد**: `html{background:var(--bg)}` هیچ‌وقت رنگِ تمِ روشن رو نمی‌گرفت چون custom property فقط از `body` به پایین کسکید می‌شه نه به بالا؛ با همون الگویِ `:has()` که قبلاً برایِ اسکرول‌بار استفاده شده بود فیکس شد.
- **رودمپ و نوتپد حالا فقط برایِ سوپرادمین فعالن**: گیتِ جدیدِ سمتِ سرور (`lib/requireSuperAdmin.ts`) رویِ همه‌ی روت‌هایِ API رودمپ و نوتپد (۱۷ هندلر) و گیتِ کلاینتی (`SuperAdminGate`) رویِ صفحاتشون؛ لینکِ نویگیشن هم برایِ بقیه‌ی کاربرها مخفی می‌شه.
- **اطلاعیه‌ی خوش‌آمدگویی + راهنمایِ نصبِ PWA**: دو آیتمِ اطلاعیه‌یِ ثابت (`STATIC_NOTIFS` در `NotificationPanel.tsx`) به همه نشون داده می‌شه؛ کلیک روش یه پاپ‌آپِ وسطِ صفحه با متنِ کامل باز می‌کنه (خوش‌آمدگویی + توضیحِ نصبِ PWA رویِ iOS Safari و Android Chrome)، بعدِ دیدن دیگه تکرار نمی‌شه (`dismissedStaticNotifs` روی `lib/storage.ts`).

## نسخه: v1.78.0 (نوتیفِ واقعیِ Web Push برای یادآوریِ برنامه/تمرین)

### چی تغییر کرد
- **یادآوری‌ها حالا حتی وقتی اپ کاملاً بسته‌ست هم می‌رسن**: تا الان `lib/notifications.ts` فقط از `Notification` API خودِ مرورگر استفاده می‌کرد که فقط وقتی تب باز باشه کار می‌کنه. یه زیرساختِ کاملِ Web Push اضافه شد: مدلِ `PushSubscription` (هر کاربر چندتا دستگاه/مرورگر می‌تونه داشته باشه)، سرویس‌ورکرِ `public/sw.js`، `lib/webPush.ts` (ارسال + پاک‌سازیِ خودکارِ سابسکریپشن‌های مرده)، و `lib/pushClient.ts` (ثبتِ سابسکریپشنِ سمتِ کلاینت، دقیقاً همون‌جایی که قبلاً فقط اجازه‌ی نوتیفِ مرورگر رو می‌گرفت).
- **`/api/push/send-reminders`**: روتی که یه crontabِ بیرونی (نه خودِ کاربر) هر چند دقیقه صداش می‌زنه — پشتِ `CRON_SECRET` قفله. همون منطقِ یادآوریِ برنامه («تا ۳۰ دقیقه دیگه» / «الان») و یادآوریِ تمرینِ ثبت‌نشده که قبلاً فقط توی پنلِ نوتیفِ کلاینت حساب می‌شد، این‌جا سمتِ سرور برای هر کاربرِ سابسکرایب‌شده تکرار شد؛ یه لاگِ کوچیکِ روزانه (روی همون `UserSetting`) از ارسالِ تکراریِ همون یادآوری توی همون روز جلوگیری می‌کنه.
- **باگِ کشف‌شده حینِ همین کار**: `.env` عمداً توی `.dockerignore`ه (رمزهای دیتابیس توشه)، یعنی متغیرهای `NEXT_PUBLIC_*` هیچ‌وقت به مرحله‌ی `docker build` نمی‌رسیدن تا توی باندلِ کلاینت جاسازی بشن — این باگ از قبل هم رویِ `NEXT_PUBLIC_MARKET` بود (بی‌سروصدا همیشه پیش‌فرضِ IRAN می‌موند)، نه فقط کلیدِ جدیدِ Push. هر دو حالا صریح به‌عنوانِ Docker build arg از `docker-compose.yml` رد می‌شن.
- **`.env.example`**: `NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT` (با `npx web-push generate-vapid-keys` ساخته می‌شن) و `CRON_SECRET` اضافه شدن، با توضیحِ کاملِ نحوه‌ی ساخت.

## نسخه: v1.77.0 (سقفِ سوپریوزر، صفحاتِ auth، ناچِ سافاری، اپتیمایزِ موبایل)

### چی تغییر کرد
- **سوپریوزر حالا واقعاً همه‌چیزش نامحدوده**: تا قبل از این، فقط دسترسیِ ماژول‌ها (`requireModule`) برای سوپریوزر نامحدود بود؛ ولی rate limitِ روزانه/ساعتیِ روی رودمپِ AI، اسکنِ کالریِ AI، جایگزینیِ حرکت، عکسِ پروفایل، تغییرِ یوزرنیم، جستجوی دوستان، و همه‌ی روت‌های نوت‌پد (ساختِ صفحه/دیتابیس/رکورد/ذخیره‌ی بلاک) هنوز روی سوپریوزر هم اعمال می‌شد. حالا `requireModule` پرچمِ `isSuperAdmin` رو برمی‌گردونه و همه‌ی این روت‌ها قبل از rate-limit چکش می‌کنن.
- **صفحاتِ ورود/ثبت‌نام/فراموشی‌رمز**: دکمه‌ی بازگشت‌به‌خانه از چپ رفت راست (پیش‌فرضِ کامپوننت عوض شد)، دکمه‌ی «گام قبل» ویزاردِ ثبت‌نام برای جلوگیری از تداخل رفت چپ (فلشش هم برعکس شد)، ضخامتِ خطِ هر دو آیکون کمی بیشتر شد (کمتر تیز/برنده به‌نظر بیاد)، پلیس‌هولدرِ فیلدِ شماره همه‌جا شد یه نمونه‌ی واقعی (`09123456789`) به‌جای الگوی `xxxxxxxxx`، و زیرِ لوگوی صفحه‌ی ثبت‌نام «به آریون خوش اومدی!» اضافه شد.
- **گلیچِ ناچ/نوارِ وضعیتِ سافاری**: صفحه اصلاً `viewport-fit=cover`/`theme-color` نداشت، پس سافاری بالای صفحه (زیرِ ناچ) رو با یه نوارِ سیستمیِ سیاهِ توپر پر می‌کرد. اضافه شدن؛ `.app-topbar` هم `env(safe-area-inset-top)` گرفت تا زیرِ ناچِ واقعی گم نشه؛ رنگِ theme-color هم با تاگلِ تم (روشن/تاریک) سینک می‌شه.
- **اپتیمایزِ بیشترِ موبایل**: شعاعِ بلورِ بلاب‌های aurora (که چه در لندینگ چه سراسرِ سایت مانتن) روی دستگاه‌های لمسی از ۱۲۰px به ۶۰px کم شد — هزینه‌ی rasterize تقریباً با مجذورِ شعاع رشد می‌کنه، پس این کاهش تأثیرِ محسوسی روی فشارِ GPU (خصوصاً فایرفاکس/کرومِ اندروید) داره، بدونِ از دست رفتنِ افکتِ بصری.

## نسخه: v1.76.1 (فیکسِ خالی‌شدنِ نوارِ «چرا ما؟» روی سافاری موبایل)

### چی تغییر کرد
- **باگِ همون خانواده‌ی هدرِ ثابت، این‌بار روی نوارِ «چرا ما؟»**: `.whyus-viewport` یه `-webkit-mask-image` داره (برای محوشدنِ لبه‌ها) و بچه‌ش (`.whyus-track`) دائم در حالِ انیمیشنِ transform ـه — این ترکیب روی سافاری موبایل شناخته‌شده‌ست که بعد از یه repaintِ اجباری (اسکرولِ سریع، جمع‌شدنِ نوارِ آدرس) کلِ نوار رو بدونِ محتوا (کاملاً خالی) رندر می‌کنه تا یه تعاملِ بعدی مجبورش کنه دوباره ریپینت بشه. فیکس: هم `.whyus-viewport` و هم `.whyus-track` با `translateZ(0)`/`will-change:transform` روی لایه‌ی ترکیبِ خودشون نگه داشته می‌شن — دقیقاً همون راه‌حلی که برای گلیچِ هدر (`.app-topbar`) توی v1.76.0 استفاده شد.

## نسخه: v1.76.0 (تخفیفِ ۳/۶ماهه + فیکسِ اسلایدر/مارکی/هدر)

### چی تغییر کرد
- **تخفیفِ ۱۲.۵٪ روی پلن‌های ۳ و ۶ ماهه هم اضافه شد** — همون نرخِ سالانه («۴۵ روزِ رایگان به‌ازای هر سال»)، این‌بار به‌صورتِ تناسبی روی مدت‌های کوتاه‌تر هم اعمال شد.
- **بخشِ «چرا ما»**: سرعتِ عبورِ باکس‌ها کم شد (خیلی سریع بود)، و `useSeamlessMarquee` از `useEffect` به `useLayoutEffect` تغییر کرد تا durationِ واقعی همون فریمِ اول محاسبه بشه — دیگه یه جهشِ محسوس از fallbackِ ۴۰ثانیه‌ای به durationِ واقعی نداره.
- **اسلایدرِ «معرفی قابلیت‌ها» (FeatureCarousel)**: جهتِ سوایپ برعکس بود (کشیدن به راست می‌رفت عقب) — فیکس شد: چپ=عقب، راست=جلو. همچنین انیمیشنِ اسلایدِ نرم (ورود/خروجِ جهت‌دار با `AnimatePresence`) جایگزینِ تعویضِ یهوییِ قبلی شد.
- **هدرِ ثابتِ بالای صفحه (`.app-topbar`)**: یه گلیچِ شناخته‌شده‌ی سافاری موبایل که حینِ انیمیشنِ جمع‌شدنِ نوارِ آدرس، عنصرِ `position:fixed` رو یه فریمِ کوتاه از بقیه‌ی صفحه جدا نشون می‌داد (خطِ سبزِ بریده‌بریده). با `transform:translateZ(0)` + `will-change:transform` رویِ هدر، لایه‌ی ترکیبِ خودش رو گرفت و دیگه جدا نمی‌شه.

## نسخه: v1.75.0 (فیکسِ نهاییِ دکمه‌های مدت)

### چی تغییر کرد
- **ترتیب برعکس شد**: از راست، ۱ ماهه → ۳ ماهه → ۶ ماهه → ۱۲ ماهه.
- **نشانِ چک‌مارک حذف شد** — فقط رنگِ پرِ دکمه نشون می‌ده کدوم انتخاب‌شده.
- **قیمتِ خط‌خورده رفت سمتِ چپِ قیمتِ اصلی** (قبلاً برعکس بود)، و خطِ توضیحِ «۴۵ روز رایگان» زیرش حذف شد.
- **باگِ واقعی‌ای که موقعِ این فیکس پیدا شد**: قانونِ سراسریِ `button:hover` توی globals.css اسپسیفیسیتی‌ش از یه کلاسِ Tailwindِ تکی بیشتره — روی هاور/لمسِ دکمه‌ی انتخاب‌شده، رنگِ پرِ سبز رو با یه تینتِ ۱۰٪ی جایگزین می‌کرد (دقیقاً همون خانواده‌باگِ sticky-hoverِ موبایل که قبلاً روی دکمه‌ی «جزئیات» هم دیده بودیم). فیکس: رنگِ پس‌زمینه/متنِ دکمه‌های مدت رفت روی inline style (که همیشه از هر قانونِ استایل‌شیت، حتی هاور، برنده‌ست).

## نسخه: v1.74.0 (کارت‌های پلن ریدیزاین شدن + سرعتِ «چرا ما» + تخفیفِ سالانه)

### چی تغییر کرد
- **دکمه‌های ماه برگشتن، دقیقاً هم‌سبکِ عکسِ مرجعِ کاربر**: پیلِ تخت، انتخاب‌شده پرِ رنگِ اصلی با یه نشانِ چک‌مارکِ سفید روی گوشه‌ی بالا-راستش — نه اسکیمورفیک/بوردردار مثلِ نسخه‌ی segmented-control قبلی.
- **تخفیفِ سالانه (واقعی، نه ساختگی)**: طبقِ قانونِ کسب‌وکار («به‌ازای هر یک سال، ۴۵ روز رایگان»)، وقتی «۱۲ ماهه» انتخاب بشه، قیمتِ اصلی (۱۲× ماهانه) به‌صورت خط‌خورده کنارِ قیمتِ واقعی (۱۰.۵× ماهانه) نشون داده می‌شه، به‌همراه یه خطِ توضیح.
- **کارت‌های پلن**: دکمه‌های انتخابِ ماهانه حذف شدن (همیشه قیمتِ یک‌ماهه نشون داده می‌شه)، هر پلن یه آیکونِ اختصاصی سمتِ راستِ اسمش گرفت (پایه=روتین، بدنسازی=دمبل، ترید=نمودار، مکس=Sparkles)، آیکونِ سبد خرید به دکمه‌های «شروع رایگان»/«خرید اشتراک» اضافه شد، و «/ ۱ ماهه» کنارِ قیمت حذف شد. زیرساختِ قیمتِ خط‌خورده (تخفیف) هم آماده‌ست، منتظرِ اعدادِ واقعی.
- **دکمه‌ی «جزئیات»**: دیگه بعد از کلیک/لمس، زیرش خط نمی‌افته (باگِ کلاسیکِ sticky-hover موبایل — `hover:underline` کامل حذف شد).
- **«چرا ما؟» سریع‌تر شد**: سرعتِ حرکت از ۲۲ به ۶۰ پیکسل/ثانیه رفت بالا — قبلاً یک دورِ کاملِ چرخه ~۱۳۷ ثانیه طول می‌کشید (توی بازه‌ی معمولِ نگاه‌کردنِ کاربر، اصلاً چرخشِ کامل دیده نمی‌شد و حسِ «رد شد و ایستاد» می‌داد)؛ الان ~۵۰ ثانیه‌ست و با تستِ طولانی هم wraparoundِ بی‌درزش تأیید شد.

## نسخه: v1.73.0 (دور دوم فیکس‌های لندینگ + منو)

### چی تغییر کرد
- **«چرا ما؟» همیشه می‌چرخه**: دیگه از `prefers-reduced-motion` پیروی نمی‌کنه — چون بخشِ محتواییِ اصلیه (نه صرفاً تزئینی)، طبقِ درخواستِ صریح همیشه بی‌درز می‌چرخه، حتی وقتی گوشیِ کاربر Low Power Mode/Reduce Motion روشنه.
- **دکمه‌های ماهِ پلن‌ها ریدیزاین شدن**: به یه segmented control تخت (نه سه‌بعدی/بوردردار/بلوردار) تبدیل شدن — تراز انتخاب‌شده پرشده با رنگِ اصلی و متنِ سفید، بقیه شفاف با متنِ کم‌رنگ.
- **پاسِ دومِ بهینه‌سازیِ موبایل**: فیزیکِ بلاب‌های مایعِ داخلِ کارتِ برنامه (`LiquidBlobBox` — rAF هر فریم + چند لایه `filter:blur`) که هیچ‌جا محدود نشده بود، حالا مثلِ بقیه‌ی افکت‌های تزئینی روی دستگاه‌های لمسی (`pointer:coarse`) کلاً اجرا نمی‌شه.
- **کارت‌های «قابلیت‌ها» هم‌اندازه شدن**: ارتفاعِ ثابت (به‌جای این‌که با تغییرِ طولِ متنِ هر قابلیت جابه‌جا بشه) + یه ردیفِ نقطه‌ی شماره‌ی اسلاید زیرش اضافه شد.
- **خطِ «اطلاعات شما امن»**: راست‌چین شد و آیکونِ shield به سمتِ راستِ متن اومد (قبلاً با `justify-end` توی RTL عملاً چپ می‌نشست).
- **ترتیبِ بخش‌ها**: «چرا ما؟» و «امروز چی داریم؟» جابه‌جا شدن.
- **قفلِ ماژول‌های نخریده توی منو**: کنارِ رودمپ‌ها/بدنسازی/ترید (اگه کاربر دسترسیِ فعال نداشته باشه) یه آیکونِ قفلِ کوچیک نشون داده می‌شه — از همون `/api/account` که `ModuleGate` هم استفاده می‌کنه، پس سوپریوزر هیچ‌وقت قفل نمی‌بینه.
- **منو دیگه هیچ گروهی رو خودکار باز نشون نمی‌ده**: قبلاً اگه توی زیرصفحه‌ی «بدنسازی» بودی، با باز کردنِ منو همون گروه از قبل باز/فعال بود؛ طبقِ درخواستِ صریح، الان منو همیشه با همه‌چیز بسته باز می‌شه.

## نسخه: v1.72.0 (فیکس دسته‌ای باگ‌های صفحه‌ی لندینگ)

### چی تغییر کرد
- **بلاب سبزِ بالای هدر**: `aurora-blob-wrap-a` عقب‌تر برده شد (`top:-26vmax` به‌جای `-20vmax`) تا نوارِ خامِ بالای هدرِ شناور (که پشتِ شیشه‌ی هدر بلور نمی‌شه) دیگه رنگِ کاملاً اشباع‌شده‌ی بلاب رو نشون نده.
- **داغ‌کردنِ موبایل**: انیمیشنِ `filter:blur(120px)` روی سه بلابِ همیشه‌درحال‌transform، روی `pointer:coarse` (لمسی) کامل خاموش شد — قبلاً حتی توی موبایل هم مدام repaint می‌شد و به‌احتمال زیاد دلیلِ اصلیِ داغی/کندی بود.
- **هنگ‌کردن روی نگه‌داشتنِ دکمه‌ی اشتراک**: `whileHover={{scale:1.02}}` روی کارتِ پلن (که خودش `backdrop-blur-xl` داره) حذف شد — ترکیبِ hover-scale + بلور روی لمسِ طولانی، دقیقاً همون الگوییه که باعثِ هنگ/تیکه‌تیکه‌شدنِ گزارش‌شده می‌شه.
- **جدول مقایسه**: لیبلِ «روتین روزانه، خواب و کارهای روزمره» کوتاه شد به «روتین روزانه».
- **خط‌های دورنگ**: بوردر کارت‌ها/جداکننده‌ها توی تمِ تاریک از `border-white/10` (نیمه‌شفاف، رنگش بسته به پس‌زمینه‌ی زیرش عوض می‌شد) به رنگِ تخت و ثابت تغییر کرد.
- **فاصله‌ی بخش‌ها**: `paddingTop` پراکنده‌ی سکشن‌ها (۲۴/۲۸/۸/۸/۸) یکدست شد به ۳۲px.
- **دکمه‌های ماهِ پلن‌ها**: سایه‌ی داخلیِ برجسته‌نما (`inset shadow`) که ظاهرِ سه‌بعدی می‌داد حذف شد.
- **کارتِ «امروز چی داریم»**: انیمیشنِ پرشدن حالا با هر بار اسکرول‌به‌پایین دوباره پخش می‌شه (قبلاً فقط یک‌بار در کلِ عمرِ صفحه اجرا می‌شد)، و با اسکرول‌به‌بالا و خروج از دید، بی‌صدا ریست می‌شه تا دفعه‌ی بعد از نو پخش بشه.
- **رنگِ «جزئیات»**: از رنگِ کم‌رنگِ خنثی به رنگِ اصلیِ برند تغییر کرد تا جلبِ توجه کنه.

## نسخه: v1.71.1 (فیکس‌های زیرساختِ دیپلوی — پیدا شده در دیپلویِ واقعیِ روی وی‌پی‌اس)

### چی تغییر کرد
- **`Dockerfile`**: نصبِ `openssl` در هر سه مرحله (`deps`/`builder`/`runner`) — موتورهای Prisma به `libssl` نیاز دارن که `node:20-alpine` خامْ فاقدشه؛ بدونش موتور با خطای غیر-JSON کرش می‌کرد.
- **`docker-compose.yml`**: سرویسِ یک‌بارمصرفِ `migrate` (پروفایلِ `tools`) اضافه شد که از استیجِ `builder` (نه `runner`ِ نهایی) استفاده می‌کنه — چون ایمیجِ production عمداً بدونِ devDependencies (Prisma CLI، `tsx`) ساخته می‌شه، `docker compose exec app npx prisma ...` جدیدترین نسخه‌ی Prisma رو از اینترنت می‌گرفت (ناسازگار با نسخه‌ی pin‌شده‌ی پروژه).
- **`deploy/nginx.conf.example`**: IPِ واقعیِ کاربر از هدرِ `CF-Connecting-IP` خونده می‌شه (نه `$remote_addr`) تا وقتی سایت پشتِ Cloudflare پروکسی می‌شه، rate limiting سمتِ اپ IPِ واقعیِ بازدیدکننده رو ببینه، نه IPِ خودِ Cloudflare.
- **`.env.example`**: متغیرهای `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB` که قبلاً فقط در docker-compose لازم بودن ولی توی نمونه نبودن، اضافه شدن — با هشدارِ صریح که رمزِ Postgres نباید کاراکترهای خاصِ URL (`/ @ : # ?`) داشته باشه، چون باعثِ خطای «invalid port number»/«database string is invalid» در Prisma می‌شه (باگی که واقعاً حین دیپلوی رخ داد).

## نسخه: v1.71.0 (زیرساختِ دیپلوی — Docker + Nginx)

### چی تغییر کرد
- **`Dockerfile`**: بیلدِ سه‌مرحله‌ای (`deps` → `builder` → `runner`)، بر پایه‌ی `node:20-alpine`. `prisma generate` قبل از `next build` اجرا می‌شه؛ ایمیجِ نهایی از خروجیِ `output: "standalone"` نکست استفاده می‌کنه (فقط فایل‌های لازمِ اجرا، نه کل `node_modules`)، با یوزرِ غیر-روت (`nextjs`).
- **`next.config.js`**: `output: "standalone"` اضافه شد — پیش‌نیازِ ایمیجِ سبکِ داکر.
- **`docker-compose.yml`**: دو سرویس — `app` (بیلدِ همین ریپو) و `db` (`postgres:16-alpine` با volume پایدار)؛ `app` منتظرِ healthy-شدنِ `db` می‌مونه؛ پورتِ اپ فقط روی `127.0.0.1:3000` باز می‌شه (نه مستقیم روی اینترنت) چون Nginx جلوش reverse-proxy می‌کنه.
- **`.dockerignore`**: جلوگیری از کپی‌شدنِ `node_modules`/`.next`/`.env`/`.git` به کانتکستِ بیلد.
- **`deploy/nginx.conf.example`**: قالبِ آماده‌ی reverse-proxy برای Nginx روی هاست (نه کانتینری) — برای هماهنگی با Certbot/SSL.
- هیچ منطق/routing/APIِ اپ دست نخورده؛ این فقط زیرساختِ اجرا روی سرورِ شخصیه (آیتمِ ۶ از roadmap بلندمدت، با درخواستِ صریح شروع شد).

## نسخه: v1.70.0 (Notepad — صفحه‌ی Home با Recents، کاورِ صفحه، بلاکِ کد، منوی Actions کامل)

### چی تغییر کرد
- **صفحه‌ی Home**: وقتی صفحه‌ای باز نیست ولی حداقل یه صفحه ساخته شده، به‌جای CTAِ خالی، یه صفحه‌ی Home نشون داده می‌شه — ردیفِ کارت‌های «اخیر» (با تصویرِ کاور، وگرنه آیکون) + لیستِ «خصوصی» (صفحاتِ سطحِ اول). حالتِ کاملاً خالی (صفر صفحه) همچنان همون CTAِ مینیمالِ قبلیه.
- **کاورِ صفحه**: هر صفحه می‌تونه یه عکسِ کاور داشته باشه (آپلود/تغییر/حذف)، با آیکونِ صفحه که روی لبه‌ی کاور overlap می‌شه — دقیقاً مثلِ الگوی دیتا-URLِ فشرده‌ای که برای آواتار/تصویرِ بلاک از قبل استفاده می‌شد (بدونِ storage ابری).
- **بلاکِ کد**: نوعِ بلاکِ جدید با انتخابِ زبان (JS/TS/Python/SQL/Bash/JSON/CSS/HTML) و رنگ‌بندیِ نحویِ سبک (بدونِ کتابخانه‌ی خارجی‌ای مثلِ Prism/CodeMirror — یه tokenizerِ رجکسیِ خودمون).
- **منوی Actions کامل**: انتخابِ فونتِ صفحه (پیش‌فرض/سریف/مونو)، کپیِ لینک، کپیِ محتوای صفحه (متنِ ساده)، افزودن/تغییر/حذفِ کاور، دیدنِ مسیرِ صفحه، عرضِ کامل، متنِ کوچک، و قفلِ صفحه (غیرفعال‌کردنِ کاملِ ادیت — تایتل/بلاک‌ها/درگ/حذف). طبقِ درخواستِ صریح، «Share»، «Available offline»، و «Use with AI» ساخته نشدن.
- هیچ منطق/routing/APIِ قبلی دست نخورده؛ همه‌چیز روی همون Design System (توکن‌های سراسری، `.ios-toggle`، `.dash-breakout`) سوار شده — که خودش خودکار با آخرین پالتِ اعمال‌شده (هرچی `--bg`/`--accent`/`--card-bg` باشه) هماهنگ می‌مونه.

## نسخه: v1.69.0 (بک‌گراندِ دارک‌مود یه‌پله روشن‌تر از مشکیِ خالص)

### چی تغییر کرد
- **`--bg` برگشت به `#0E1011`**: مشکیِ خالص (`#000000`) خودش یه تغییرِ قدیمی‌تر بود (کامیتِ `9ffd42c`، قبلِ همه‌ی کارهای این نشست، گم‌شده وسطِ یه کامیتِ بزرگِ نامرتبط با فیچرهای دیگه) — طبقِ خواسته‌ی صریح، به نسخه‌ی قبل‌ترش برگشت. فقط همین دو مقدار (`--bg`/`--bg-rgb`)، هیچ‌جای دیگه‌ی اون کامیتِ قدیمی (نوت‌ها، ویزاردِ روتین، فلوهای کالری) دست نخورده.

## نسخه: v1.68.0 (بازگشتِ تمِ دارک‌مود به نسخه‌ی قبلی)

### چی تغییر کرد
- **پالتِ دارک‌مود و بک‌گراندِ aurora برگشت به حالتِ قبل**: بک‌گراند دوباره مشکیِ خالص، اکسنت دوباره سبزِ زمردیِ قبلی، و سه بلابِ شناورِ پس‌زمینه («سه تا توپ») دوباره برگشتن — طبقِ خواسته‌ی صریح، اون نسخه بهتر بود.
- `--card-bg`/`--input-bg` (که الان Notepad و چندجای دیگه بهشون وابسته‌ن) نگه داشته شدن ولی مقدارشون به یه تیرگیِ نزدیک به مشکیِ خالص برگشت، هم‌خونی با بقیه‌ی پالتِ قدیمی.

## نسخه: v1.67.0 (بازطراحیِ Notepad برای هم‌راستاییِ کامل با Design System اصلیِ Arion)

### چی تغییر کرد
- **بازطراحیِ کاملِ Notepad**: پالتِ مستقلِ بنفش/ایندیگوی راندِ قبلی حذف شد — حالا Notepad مستقیماً از توکن‌های سراسریِ Arion (`--bg`, `--text`, `--muted`, `--accent`, `--card-bg`, `--input-bg`, `--line`) استفاده می‌کنه، هم در تمِ تاریک هم روشن؛ سبز فقط برای عناصرِ مهم (دکمه‌ی اصلی، تبِ فعال، لینک) به‌کار می‌ره، نه به‌عنوانِ بک‌گراندِ سراسری.
- **حذفِ حسِ «محصولِ جدا»**: Workspace دیگه بک‌گراندِ مجزا/بوردرِ بالایی نداره — کاملاً با بک‌گراندِ خودِ صفحه یکی شده؛ عرضِ breakout هم به‌جای مقدارِ اختصاصی، از همون کلاسِ مشترکِ `.dash-breakout` (که بقیه‌ی بخش‌های دیگرِ اپ هم استفاده می‌کنن) استفاده می‌کنه.
- **سایدبار سبک‌تر**: از ۲۶۴ به ۲۲۴ پیکسل باریک‌تر شد، بک‌گراندِ مجزا حذف شد (فقط یک بوردرِ نازک جداکننده)، دکمه‌ی «صفحه‌ی جدید» بوردرِ نقطه‌چین‌شِ سنگینش رو از دست داد، و هایلایتِ آیتمِ فعال دیگه واشِ سبزِ پرزرق‌وبرق نداره (فقط رنگِ متن سبز می‌شه).
- **Empty State مینیمال‌تر**: متنِ «شروع به نوشتن کنید» / «ایده‌ها، یادداشت‌ها و برنامه‌های خود را در Arion ثبت کنید.» + دکمه‌ی «+ صفحه جدید».
- رادیوس‌ها کمی بزرگ‌تر شدن (۸/۱۲/۱۶ به‌جای ۶/۸/۱۰) تا با حسِ گردترِ بقیه‌ی کارت‌های Arion هماهنگ باشن — این تغییر خودکار روی Database (جدول/بورد/تقویم/گالری/لیست) هم اعمال شد چون از همون توکن‌های مشترک استفاده می‌کنه.
- هیچ منطق/state/routing/API‌ای دست نخورده — فقط CSS و کپیِ صفحه‌ی خالی.

## نسخه: v1.65.0 (Notepad — بازطراحیِ کاملِ بصری + Phase 2: دیتابیس، حذفِ کاملِ «یادداشت‌ها»)

### چی تغییر کرد
- **حذفِ کاملِ «یادداشت‌ها»**: صفحه‌ی `/notes`، روت‌های API، مدلِ `Note`، و لینکِ منو کاملاً حذف شدن — Notepad تنها بخشِ یادداشت/دانشِ اپه.
- **بازطراحیِ کاملِ هویتِ بصریِ Notepad**: یه سیستمِ رنگی/توکنِ کاملاً مستقل (`--np-*`) جدا از بقیه‌ی اپ — بدونِ گلس/بلور، پالتِ تیره‌ی خنثی با اکسنتِ بنفش/ایندیگو (`#7C6AF2`)، رادیوس‌های کوچیک‌تر، ظاهرِ حرفه‌ای و کاملاً متفاوت با تمِ سبزِ liquid-glass بقیه‌ی صفحات؛ در تمِ روشن هم پالتِ جداگانه‌ی خودش رو داره.
- **Notepad Phase 2 — Database**: هر بلاک می‌تونه یه دیتابیسِ inline (جدولِ Notion-مانند) داشته باشه، با پنج ویو:
  - **جدول**: ستون‌های قابلِ‌افزودن/ویرایش/حذف از هر نوع (متن/عدد/انتخابی/چندانتخابی/چک‌باکس/تاریخ/لینک/ایمیل/فرمول/ارتباط)، ردیف‌های قابلِ‌افزودن/حذف.
  - **بورد**: گروه‌بندیِ کارت‌ها بر اساسِ یه propertyِ انتخابی، جابه‌جاییِ کارت بینِ ستون‌ها با درگ‌ودراپِ بومیِ HTML5.
  - **تقویم**: گریدِ ماهانه، رکوردها زیرِ روزِ متناظرِ propertyِ تاریخ، ناوبریِ ماه.
  - **گالری**: گریدِ کارتِ واکنش‌گرا با چندتا از propertyهای اصلی روی هر کارت.
  - **لیست**: یه ردیفِ ساده به‌ازای هر رکورد.
  - فیلتر/سورت/مخفی‌کردنِ ستون‌ها (مشترک بینِ همه‌ی ویوهای یه دیتابیس)، و یه ارزیاب‌گرِ سبکِ فرمول (بدونِ `eval`، فقط `+ - * /` روی propertyهای عددی).
  - قابلیت‌های AI/اشتراک‌گذاری/کامنت (Phase 3) طبقِ خواسته عمداً اضافه نشدن.

## نسخه: v1.64.0 (Notepad — Phase 1: ورک‌اسپیسِ بلاک‌محورِ کامل)

### چی تغییر کرد
- **بخشِ کاملاً جدید — Notepad** (`/notepad`): یه Workspace بلاک‌محورِ واقعی، جدا از «یادداشت‌ها»ی ساده‌ی قبلی. الهام‌گرفته از معماریِ اطلاعاتیِ Notion (نه ظاهرش) — کاملاً با هویتِ بصریِ Arion:
  - **صفحاتِ تودرتو**: ساخت/تغییرِ نام/آیکون (ایموجی)/حذف (سطلِ زباله، نه حذفِ فوری)/بازگردانی/حذفِ همیشگی/کپی‌کردن/جابه‌جاکردنِ زیرِ صفحه‌ی دیگه/سنجاق‌کردن (علاقه‌مندی‌ها)، همه واقعاً روی Prisma/Postgres ذخیره می‌شن (مدل‌های مستقلِ `NotepadPage`/`NotepadBlock`، هرکدوم id مستقل).
  - **ادیتورِ بلاکی واقعی**: پاراگراف، عنوان ۱/۲/۳، لیستِ نقطه‌ای/شماره‌دار، چک‌لیست (Todo)، تاگل (با بچه‌های تودرتوی قابلِ‌جمع‌شدن)، نقل‌قول، خطِ‌جداکننده، تصویر (آپلود فشرده یا لینک).
  - **Slash Command** («/» وسطِ نوشتن یه منویِ قابل‌جستجو باز می‌کنه) + میان‌برهای مارک‌داون (`# `، `- `، `[] `، `> `، `---`) برای تبدیلِ نوعِ بلاک، و `**بولد**`/`*ایتالیک*`/`` `کد` ``/`~~خط‌خورده~~` برای فرمت‌دهیِ درون‌خطی؛ یه تولبارِ شناور هم روی انتخابِ متن (بولد/ایتالیک/آندرلاین/خط‌خورده/کد/لینک) با `execCommand` بومیِ مرورگر.
  - **Drag & Drop واقعی** برای ترتیبِ بلاک‌ها (framer-motion Reorder، همون الگویی که توی برنامه‌ی دستیِ ورزش استفاده شده).
  - **Autosave دیبانس‌شده** با نشانگرِ «در حال ذخیره…/ذخیره شد»، و یه قفلِ سمتِ‌کلاینت که جلوی هم‌پوشانیِ دو درخواستِ ذخیره رو می‌گیره؛ سمتِ سرور هم با `upsert` اتمیک نوشته شده تا حتی اگه دو درخواست هم‌زمان برسن خطای unique-constraint نده.
  - **Command Palette** (⌘K/Ctrl+K) با جستجوی سراسری روی عنوانِ صفحات و متنِ بلاک‌ها + دستورهای پایه (صفحه‌ی جدید، toggle سایدبار).
  - سایدبار با بخش‌های Favorites/Recent/همه‌ی صفحات (درختی، تودرتو)/سطلِ زباله؛ موبایل: سایدبار می‌شه Drawer.
  - طبقِ phase‌بندیِ خواسته‌شده، Database/Table/Board/Calendar (Phase 2) و AI/Sharing/Collaboration (Phase 3) فعلاً ساخته نشدن.

## نسخه: v1.63.0 (یادداشت‌ها، ویزاردِ دو‌مرحله‌ایِ افزودنِ برنامه، بازطراحیِ برنامه‌ی کالری/تمرینِ دستی، بک‌گراندِ کاملاً سیاه)

### چی تغییر کرد
- **بخشِ جدید — یادداشت‌ها**: یه صفحه‌ی کاملاً تازه (`/notes`) توی منو اضافه شد — یادداشتِ متنی یا چک‌لیست، تگ‌گذاریِ آزاد با فیلترِ تگ، سنجاق‌کردن، و یادآوری/الرتِ اختیاری (تاریخ+ساعتِ جلالی) که با نوتیفیکیشنِ مرورگرِ خودِ اپ هماهنگه. مدلِ دیتابیسیِ مستقل (`Note`)، جزوِ پلنِ پایه (نیازِ به اشتراک نداره). دامنه‌ی این پیاده‌سازی عمداً محدودِ همینا نگه داشته شده، نه یک کپیِ کاملِ Notion.
- بدنسازی — «وارد کردنِ دستی»: پاپ‌آپِ تعدادِ ست/تکرار/تایمر بازطراحی شد (فیلدها شیشه‌ای، انتخابِ روزها کروش‌مانند)، دکمه‌ی تاییدش رفت پایین‌راست؛ دکمه‌ی «ثبتِ برنامه» شد فوترِ چسبانِ ته‌ی پاپ‌آپ با بک‌گراندِ مات.
- کالری — «محاسبه‌ی هوشمند» شد سه‌مرحله‌ای: اول هدف، بعد تعدادِ وعده، آخرش جنسیت/قد/وزن/سن.
- کالری — «وارد کردنِ دستی»: جای برچسب و عددِ جمعِ کالریِ روزانه برعکس شد؛ دکمه‌ی «افزودنِ وعده» رفت هم‌ردیفِ عنوان (چپ)، با کلیک یه ردیفِ جدید با انیمیشن باز می‌شه (نامِ وعده + کالری + دکمه‌ی تاییدِ بدونِ بک‌گراند)؛ ردیف‌های ثبت‌شده حالا یه دکمه‌ی سه‌نقطه (ویرایش/حذف) دارن به‌جای ضربدرِ همیشگی. دکمه‌ی ثبت شد «ثبتِ برنامه کالری» با انیمیشنِ تاییدِ موفقیت/خطا و محدودیتِ ۲۰کاراکتری برای اسمِ وعده.
- کالری — دکمه‌ی واحدِ اندازه‌گیری توی فرمِ افزودنِ غذا باریک‌تر شد.
- **روتینِ من — «افزودن برنامه جدید» شد ویزاردِ دومرحله‌ای**: مرحله‌ی اول اسم/روزها/ساعت‌شروع‌وپایان + یه چک‌باکسِ «این یک دوره است» (فقط با تیک‌خوردن، تاریخِ شروع/پایانِ دوره پرسیده می‌شه)؛ مرحله‌ی دوم میزانِ اهمیت (پیش‌فرض «متوسط») و تگ. اعتبارسنجی مستقلِ هر مرحله با پیامِ خطا کنارِ فیلد، و دکمه‌ی «بعدی» با ورودیِ ناقص یه لرزشِ خیلی ملایم می‌خوره. ثبتِ نهایی همون دکمه‌ی گردِ تیک/ضربدر/اسپینر با انیمیشنِ همیشگی‌ست.
- بک‌گراندِ حالتِ تاریک شد کاملاً سیاه (`#000000`).

## نسخه: v1.62.0 (فیکسِ واقعیِ باگِ تایم‌لاینِ هفتگی، قفلِ اسکرولِ پشتِ پاپ‌آپ‌ها، بازطراحیِ برنامه‌ی دستیِ کالری/تمرین)

### چی تغییر کرد
- **فیکسِ باگِ واقعی و ریشه‌ای — برنامه‌ی هفتگی**: دو باگِ لایه‌روی‌لایه پیدا و فیکس شد که با هم باعثِ افتادنِ برنامه‌ها روی کلمه‌ی «بیداری»/«خواب» می‌شدن. (۱) وقتی ساعتِ خوابِ کاربر از نیمه‌شب رد نمی‌شد (مثلاً ۲۳:۳۰)، فرمول همیشه ۲۴ساعت بهش اضافه می‌کرد و بازه‌ی «بیداری» رو به‌جای ~۱۶ ساعتِ واقعی، ~۴۰ ساعتِ غلط حساب می‌کرد. (۲) وقتی چند برنامه هم‌زمان نزدیکِ هر دو انتها (هم نزدیکِ بیداری هم نزدیکِ خواب) بودن، الگوریتمِ فاصله‌گذاری برای درست‌کردنِ لبه‌ی آخر، همه‌ی نقطه‌ها رو یک‌جا به عقب شیفت می‌داد و اولی رو می‌فرستاد زیرِ حداقلِ فاصله‌ی مجاز از «بیداری». الان با یه الگوریتمِ رفت-برگشتی درست حل شده.
- **فیکسِ باگِ واقعی — تب‌های شیشه‌ای**: نشانگرِ سبزِ SegmentedTabs (هدف/جنسیت/وعده‌غذایی/…) داخلِ پاپ‌آپ‌ها جای اشتباه می‌نشست، چون اندازه‌گیری‌ش دقیقاً وسطِ انیمیشنِ ورودِ پاپ‌آپ (transform:scale) انجام می‌شد. با offsetLeft/offsetWidth (که از transform اثر نمی‌گیره) فیکس شد.
- **پاپ‌آپ‌ها**: وقتی هر پاپ‌آپی باز باشه، صفحه‌ی پشتش دیگه اسکرول/کلیک نمی‌خوره (قفلِ اسکرولِ سراسری، شمارشگر برای پاپ‌آپ‌های تودرتو).
- بدنسازی — «وارد کردنِ دستیِ برنامه»: بازشدن/بسته‌شدنِ هر روز نرم و انیمیت شد؛ دکمه‌ی «افزودن» رفت بالا-چپِ باکسِ هر روز؛ جابه‌جاییِ حرکات در حالتِ ویرایش الان با دسته‌ی درگ (⠿) واقعاً کشیدنی‌ه، نه دو دکمه‌ی بالا/پایین؛ دکمه‌ی «ثبت برنامه» چپ‌چین با بک‌گراندِ توپر و انیمیشنِ اسپرینگ.
- برنامه‌ی هفتگیِ اصلی هم مثلِ بالا، بازشدنِ هر روز نرم‌تر شد.
- کالری — «وارد کردنِ دستی»: ردیف‌های وعده‌ها بازطراحی شدن (یه کارتِ واحد به‌جای دو باکسِ جدا)، جمعِ کالریِ روزانه بزرگ و برجسته نمایش داده می‌شه. تایتلِ «محاسبه‌ی هوشمند»/«وارد کردنِ دستی» رفت زیرِ هدر (نه کنارش)، آیکونِ «محاسبه‌ی هوشمند» شد همون آیکونِ ستاره‌ی جمنای-مانند که جاهای دیگه‌ی اپ برای هوش مصنوعی استفاده می‌شه.
- کالری — باکسِ واحدِ اندازه‌گیری کوچیک‌تر شد.
- دوستان — پاپ‌آپ دیگه دو راهِ جداگانه برای افزودنِ دوست نداره؛ فقط یه دکمه‌ی «+ افزودن دوست» بالا-چپِ لیست.
- پروفایل — تلاش برای پیاده‌سازیِ کامپوننتِ smoothui's agent-avatar (هم از طریق fetch مستقیم هم CLI) به‌خاطرِ بلاک‌بودنِ دامنه توی این محیط ناموفق بود؛ آواتارِ گرادیانیِ متحرکِ قبلی (که خودش جایگزینِ آیکونِ آدمکِ ثابت بود) دست‌نخورده موند.

## نسخه: v1.61.0 (برنامه‌ی دستیِ آکاردئونی، فیکسِ باگِ تاگل‌های شیشه‌ای، اسکرولِ داخلیِ لیست‌ها، آواتارِ گرادیانی)

### چی تغییر کرد
- بدنسازی — «وارد کردنِ دستیِ برنامه» کاملاً بازطراحی شد: به‌جای دو مرحله‌ی جدا (انتخابِ روزها → نمای هر روز → مرورِ نهایی)، حالا یه آکاردئونِ عمودیِ روزهای هفته‌ست — دقیقاً هم‌شکلِ «برنامه هفتگی». هر روز با کلیک باز می‌شه و برنامه‌ش رو نشون می‌ده، یه دکمه‌ی «افزودن» داره و — وقتی حرکتی داشته باشه — یه دکمه‌ی «ویرایش» که جابه‌جاییِ ترتیبِ حرکات (بالا/پایین) و حذفشون رو فعال می‌کنه.
- باگِ واقعی پیدا و فیکس شد: هر پاپ‌آپی که تبِ شیشه‌ای (SegmentedTabs، مثلاً «هدف»/«جنسیت»/وعده‌غذایی) داشت، وقتی باز می‌شد نشانگرِ سبزش جای اشتباه می‌نشست — چون انیمیشنِ ورودِ پاپ‌آپ (اضافه‌شده توی نسخه‌ی قبل) یه transform:scale داشت و اندازه‌گیریِ نشانگر دقیقاً همون لحظه‌ی مقیاسِ کوچیک‌تر انجام می‌شد. حالا اندازه‌گیری بر اساسِ offsetLeft/offsetWidth‌ه که از transform اثر نمی‌گیره.
- لیستِ «برنامه تمرینی» و «برنامه غذایی»: وقتی آیتم‌ها زیاد می‌شن، دیگه صفحه کش نمیاد — خودِ باکس با یه اسکرول‌بارِ نازکِ شیشه‌ای (بدونِ بکگراند/دکمه‌های بالا-پایین) سمتِ راست اسکرول می‌شه.
- کالری — باکسِ واحدِ اندازه‌گیری (توی فرمِ افزودنِ غذا) کوچیک‌تر شد.
- کالری — پاپ‌آپِ «تغییر برنامه»: تایتلِ «محاسبه‌ی هوشمند»/«وارد کردنِ دستی» جاش عوض شد (اومد زیرِ ردیفِ دکمه‌های بازگشت/بستن، نه کنارشون — هم‌قاعده‌ی بقیه‌ی ویزاردهای اپ).
- دوستان — پاپ‌آپِ «دوستان» بازطراحی شد: دیگه بخشِ جستجو/افزودنِ دوست همیشه بالای لیست نیست؛ با بازکردنِ پاپ‌آپ فقط لیستِ دوستات (اسم، اواتار، درصد، استرایک، دکمه‌ی حذف) دیده می‌شه، و یه آیکونِ «+» کنارِ ضربدرِ بستن می‌بره به نمای جستجو/افزودن.
- بدنسازی — ترتیبِ کارت‌ها توی نمای موبایل: برنامه‌ی تمرینی → دوستان → پیشرفتِ هفتگی → مشاهده‌ی حرکات (دسکتاپ دست‌نخورده موند).
- پروفایل — آواتارِ پیش‌فرض (وقتی عکس آپلود نشده) از یه آیکونِ آدمکِ ثابت به یه گرادیانِ زنده‌ی درحال‌چرخش با حرفِ اولِ اسم تغییر کرد؛ آپلودِ عکسِ واقعی همچنان جایگزینِ کاملش می‌شه.

## نسخه: v1.60.0 (عکسِ پروفایل، شخصی‌سازیِ داشبورد، انیمیشنِ پاپ‌آپ‌ها، فیکسِ باگِ جایگزینیِ حرکت)

### چی تغییر کرد
- پنلِ کاربری — عکسِ پروفایل اضافه شد (آپلود/تغییر/حذف، سمتِ کلاینت فشرده می‌شه، توی چیپِ پروفایلِ هدر هم نشون داده می‌شه). دو بخشِ جدید: «کدوم دسته‌ها یادآوری بگیرن» (یادآوریِ برنامه/تمرین/درخواستِ دوستی جدا خاموش‌روشن می‌شن) و «شخصی‌سازیِ چیدمانِ داشبورد» (کارتِ یادآوری‌ها/دوستان/نمودارها رو می‌شه از داشبوردهای روتین، بدنسازی و کالری مخفی کرد).
- پاپ‌آپ‌ها — همه‌شون (تغییر برنامه، افزودن، دوستان، پنلِ کاربری، …) حالا با یه انیمیشنِ نرمِ اسلایدِ بالا+فید ظاهر می‌شن، نه یهویی — چون قبلاً کلاسِ open از همون اولین رندر روی عنصر بود و transition هیچ فریمِ «بسته»‌ای برای انیمیت‌کردن نداشت؛ با animation (نه transition) این مشکل حل شد.
- بدنسازی — باگِ واقعیِ جایگزینیِ حرکت پیدا و فیکس شد: پیشنهادهای جایگزین هیچ‌وقت چک نمی‌کردن که پیشنهادشون از قبل توی همون روز نیست؛ نتیجه‌ش این بود که گاهی جایگزینی، یه هم‌نامِ تکراری از یه حرکتِ دیگه‌ی همون روز می‌ساخت (نه یه جایگزینِ واقعاً جدید) — الان بقیه‌ی حرکاتِ همون روز از لیستِ پیشنهادها حذف می‌شن.
- بدنسازی — جای کارتِ «دوستان» و «پیشرفتِ هفتگی» عوض شد.
- کالری — ردیف‌های «برنامه غذایی»: وزن جلوی اسمِ غذا نوشته می‌شه (نه زیرش)، باکسشون کاملاً گرد شد.
- دوستان — دکمه‌ی «افزودن دوست» وسط‌چین و کم‌رنگ شد (با هاور پررنگ می‌شه)؛ هاورِ «مشاهده همه» دیگه زیرخط نداره.
- دیتابیسِ غذا — همه‌ی اسم‌های برندی حذف شدن (مثلاً «چیپس مزمز»/«چیپس چاکلز» → فقط «چیپس سیب‌زمینی») تا لیستِ جستجو با نسخه‌های تکراریِ هم‌کالری شلوغ نباشه.

## نسخه: v1.59.0 (پرتال‌شدنِ پنل‌های پروفایل/اطلاعیه، پاکسازیِ فرمِ افزودنِ غذا، تنقلاتِ ایرانی)

### چی تغییر کرد
- پروفایل/اطلاعیه‌ها — این دو پنل حالا به بادی پورتال می‌شن (نه دیگه فرزندِ هدر): چون خودِ هدر backdrop-filter داره، پنلِ توی فرزندانش که خودش هم backdrop-filter داشت فقط لایه‌ی از‌قبل‌بلورشده‌ی تقریباً خالیِ همون stacking context رو می‌دید، نه محتوای واقعیِ پشتِ صفحه — همین باعث می‌شد هیچ‌وقت واقعاً مات نشه. همچنین دیتای هردو پنل حالا کش می‌شه (نه هر بار refetch)، پس هر بار که باز می‌شن دیگه «در حال بارگذاری…» چشمک نمی‌زنه.
- کالری — ردیف‌های «برنامه غذایی» دیگه آیکون ندارن، باکسشون کروترشد، و «kcal» حالا سمتِ راستِ عدد نوشته می‌شه. «جزئیاتِ برنامه‌غذایی» (قبلاً «پروفایل مغذی‌ها») دیگه دکمه‌ی اسکنِ AI نداره. «استرایک» (قبلاً «مسیر پیشرفت») اسمش برگشت.
- کالری — فرمِ «افزودن غذا»: واحدهای کیلوگرم/اونس حذف شدن، دراپ‌داونِ واحدها کاملاً مات شد (بکگراندِ نیمه‌شفافِ قبلی حذف شد) و اسکرول‌بارش با یه ترفندِ dir همیشه سمتِ راست می‌شینه؛ فیلدهای درشت‌مغذی به بالای دکمه‌ی «افزودن» منتقل شدن؛ توضیحاتِ اضافه‌ی زیرِ تایتلِ درشت‌مغذی‌ها حذف شد.
- کالری — دیتابیسِ داخلیِ غذاها با ~۵۰ تنقلات و خوراکیِ ایرانیِ رایج (چیپس/پفک/تخمه/آجیل/شیرینی سنتی/نوشیدنی) گسترش پیدا کرد.
- دوستان — همه‌جا که پیام خالی‌بودنِ لیست بود، حالا یه دکمه‌ی «+ افزودن دوست» جاش نشسته که مستقیم می‌بره به بخشِ جستجو.
- بدنسازی — دکمه‌ی نهاییِ «ثبت برنامه» توی فرمِ دستی الان دقیقاً هم‌شکلِ دکمه‌ی معادلش توی فرمِ هوشمنده (بکگراند/جای‌گیری یکسان).
- پاپ‌آپ‌ها — گردیِ گوشه‌ی پنل‌های اصلی (تغییر برنامه، افزودن، دوستان، اطلاعیه‌ها، …) بیشتر شد، نزدیک‌تر به سبکِ اپل.
- برنامه‌ی هفتگی و جایگزینیِ حرکتِ تمرینی هر دو دوباره با داده‌ی واقعی تست شدن و درست کار می‌کنن؛ اگه جایی هنوز مشکل دیدی، با جزئیاتِ دقیق‌تر (کدوم دکمه، چه اتفاقی افتاد) بگو تا دوباره بررسی بشه.

## نسخه: v1.58.0 (فیکسِ واقعیِ باگِ چینشِ کالری، تایمرِ دایره‌ایِ تمرین، صفحه‌ی اشتراک، رودمپِ بازطراحی‌شده)

### چی تغییر کرد
- کالری — باگِ فضای خالیِ بینِ کارت‌ها بالاخره واقعاً حل شد: مشکل از این بود که CSS Grid ارتفاعِ هر ردیف رو بر اساسِ بلندترین سلولِ همون ردیف (توی همه‌ی ستون‌ها) تعیین می‌کنه؛ `align-items:start` فقط از کِش‌اومدنِ خودِ کارت جلوگیری می‌کرد، نه از خالی‌موندنِ خودِ ردیف. حالا ستونِ وسط و ستونِ چپ هرکدوم یه `flex column` کاملاً مستقل‌ان، نه شریکِ ردیفِ مشترک.
- کالری — «تغییر برنامه»: صفحه‌ی انتخابِ اولش دقیقاً هم‌قاعده‌ی «افزودن برنامه»ی بدنسازی شد (دو دکمه‌ی بزرگ). حالتِ دستی دیگه یه عددِ کالریِ کل نمی‌گیره — مستقیم کالریِ هر وعده رو ردیف‌به‌ردیف می‌گیره (همون ویرایشگری که قبلاً توی کارتِ «کالری هر وعده» بود، از اونجا حذف شد).
- کالری — «پروفایل مغذی‌ها» (قبلاً «ریز درشت‌مغذی‌ها») همیشه حلقه‌ها رو نشون می‌ده (صفر وقتی داده‌ای نیست)، نه یه متنِ جایگزین. «مسیر پیشرفت» (قبلاً «روند موفقیت») اسمش عوض شد. ردیف‌های لیستِ غذا: دکمه‌ی ضربدر دیگه بکگراند نداره، عددِ کالری با یه پسِ‌زمینه‌ی سبزِ کم‌رنگ خواناتر شد.
- منو — فاصله‌ی داخلیِ باکسِ منو کم شد، آیکون‌ها و متنِ زیرمجموعه‌ها توی تمِ روشن قهوه‌ایِ واضح شدن (نه بی‌رنگ/طلایی)، ضربدرِ منو و هاورِ دکمه‌ی روز/شب دیگه بکگراند/انیمیشن اضافه ندارن، و گروهِ بازشده (مثلاً «بدنسازی») تا وقتی بسته نشه رنگِ اصلی‌ش می‌مونه. پنلِ اطلاعیه‌ها/پروفایل هم‌شیشه‌ی خودِ منو شدن. بکگراندِ تمِ روشن یه‌کم نارنجی‌تر شد.
- برنامه هفتگی — باگِ روی‌همافتادنِ برچسبِ ساعت‌ها وقتی چند برنامه نزدیکِ هم بودن حل شد: عرضِ ریلِ زمانی حالا متناسب با تعدادِ آیتم‌های هر روز بزرگ می‌شه، نه یه عرضِ ثابت.
- دوستان — دکمه‌ی حذفِ دوست با پاپ‌آپِ تاییدِ «بله/نه» اضافه شد.
- بدنسازی — تایمرِ استراحتِ بینِ ست‌ها از نوارِ خطی به شمارش‌معکوسِ دایره‌ای تغییر کرد؛ پیامِ بعدِ استراحت واضح‌تر شد؛ «مشاهده حرکات» دیگه با شروعِ تمرین مخفی نمی‌شه.
- پروفایل — قد/وزن/سن حالا یه‌بار وارد بشه، توی فرمِ کالری و فرمِ بدنسازی هردو از قبل پر می‌شه؛ اگه بیشتر از دو هفته از آخرین ثبتِ وزن گذشته باشه، یه یادآوریِ کوچیک بالای داشبوردِ کالری نشون داده می‌شه.
- اشتراک — دکمه‌ی «اشتراک» دیگه پنلِ کاربری رو باز نمی‌کنه؛ به یه صفحه‌ی مستقل می‌ره که همه‌ی پلن‌ها با قیمت و ماژول‌های شامل‌شون کنارِ هم مقایسه می‌شن (درگاهِ پرداخت هنوز وصل نیست، پس دکمه‌ی هر پلن صادقانه «به‌زودی» نشون می‌ده).
- رودمپ — مسیرِ ایستگاه‌ها بازطراحی شد: یه خلاصه‌ی پیشرفت (چندتا از چندتا ایستگاه تکمیل شده) بالای صفحه، ایستگاه‌ها حالا کارتِ شیشه‌ای‌ان با یه ریلِ اتصالِ رنگی که با پیشرفت پر می‌شه، و آیتم‌های هر ایستگاه + منابعِ پیشنهادی به جست‌وجوی همون موضوع لینک می‌شن (واقعاً قابلِ‌اجرا، نه فقط یه چک‌لیستِ متنی).

## نسخه: v1.57.0 (منویِ ساده‌شده، پاپ‌آپ‌های کالری بازطراحی‌شده، واحدهای اندازه‌گیریِ بیشتر)

### چی تغییر کرد
- منو — زیرمجموعه‌ها دیگه آیکون ندارن، فاصله‌بندی‌شون درست شد، و کلِ منو دیگه نشون نمی‌ده کاربر دقیقاً کدوم بخشه (نه گروه، نه زیرمجموعه‌ها هایلایتِ سبز نمی‌گیرن).
- سراسری — متنِ کنارِ «توجه:» توی پایینِ هر صفحه از سبز به رنگِ معمولیِ متن (سفید توی تمِ تیره) تغییر کرد.
- کالری — «تغییر برنامه»: فیلدهای قد/وزن/سن حالا هم‌بکگراندِ شیشه‌ایِ SegmentedTabsِ بالای‌سرشون شدن (نه بکگراندِ تختِ قبلی)، رادیوسشون بیشتر شد، دکمه‌ی «انصراف» حذف شد و دکمه‌ی محاسبه به یه دکمه‌ی توپرِ پررنگ‌تر بازطراحی شد. مهم‌تر از همه: حالا دو مسیر داره — «محاسبه‌ی هوشمند» (فرمولِ قبلی) یا «وارد کردن دستی» (خودِ کاربر یه عددِ کالریِ روزانه می‌ده، بینِ وعده‌ها مساوی تقسیم می‌شه).
- کالری — «افزودن غذا»: دکمه‌ی «اسکن با هوش مصنوعی» از بالای فرم به زیرِ دکمه‌ی اصلیِ «افزودن» منتقل شد؛ همه‌ی فیلدها همون بکگراندِ شیشه‌ای رو گرفتن؛ واحدهای اندازه‌گیریِ بیشتری (کیلوگرم، اونس، میلی‌لیتر) اضافه شد و آخرین واحدِ انتخابی توی مرورگر ذخیره می‌مونه؛ فیلدهای درشت‌مغذی (پروتئین/کربوهیدرات/چربی) دیگه فقط وقتی غذا توی کاتالوگ نبود نیستن — همیشه در دسترسِ اختیاری‌ان؛ کلیکِ «افزودن» یه انیمیشنِ موفقیت (بلور + تیکِ سبز) نشون می‌ده.
- کالری — ردیف‌های لیستِ «برنامه غذایی» بازطراحی شدن: یه آیکون‌چیپِ کوچیک، اسمِ غذا برجسته‌تر، وزن زیرش، و عددِ کالری با رنگِ سبزِ برجسته.

## نسخه: v1.56.0 (فضای خالیِ باکس‌های کالری، شیشه‌ایِ تمِ روشن پررنگ‌تر، بازطراحیِ درختیِ منو)

### چی تغییر کرد
- کالری — باگِ فضای خالیِ بینِ کارت‌ها فیکس شد: چون `align-items` پیش‌فرضِ گرید `stretch` بود، وقتی یه کارتِ کوتاه‌تر (مثلاً «دوستان») کنارِ یه کارتِ بلندتر توی همون ردیف بود، تا قدِّ کلِ ردیف کش می‌اومد و یه فاصله‌ی خالیِ بزرگ قبلِ کارتِ بعدی جا می‌ذاشت. با `align-items:start` (و `align-self:stretch` فقط روی «برنامه غذایی» که عمداً بایدpull ارتفاعِ کامل رو بگیره) حل شد.
- تمِ روشن — بک‌گراندِ باکس‌های داشبورد (`bg-dash-card`) یه‌کم پررنگ‌تر شد (از ۵٪ به ۱۶٪ اپاسیتیِ سفید) چون نسخه‌ی قبلی اونقدر کم‌رنگ بود که کاربر گفت «انگار هیچ بکگراندی نداره» — همچنان شیشه‌ای می‌مونه، نه سفیدِ تخت.
- منو — بخشِ بازشونده‌ی زیرمجموعه‌ها کاملاً بازطراحی شد: هر زیرمجموعه حالا یه آیکون‌چیپِ کوچیکِ گردگوشه داره (نه فقط آیکونِ خام)، یه ریلِ عمودیِ نازک با نقطه روی هرکدوم که ارتباطِ بصریِ «این‌ها زیرمجموعه‌ی همون گروهن» رو نشون می‌ده، و حالتِ فعال با پس‌زمینه‌ی رنگیِ پررنگ‌تر + نقطه‌ی توپر + فونتِ بولدتر مشخص‌تر شده.

## نسخه: v1.55.0 (کالری — اصلاحِ چیدمان به ستون، منوی جعبه‌ایِ یکپارچه، رنگ/شیشه‌ای‌سازیِ تمِ روشن)

### چی تغییر کرد
- کالری — چیدمانِ دسکتاپ اصلاح شد: منظورِ کاربر از «ردیف» در واقع «ستون» بود — الان سه ستونِ عمودی: راست (اصلی) برنامه‌ی غذایی، وسط از بالا کالری‌هروعده/ریزِ درشت‌مغذی‌ها، چپ از بالا دوستان/روندِ موفقیت/نمودار. موبایل دست‌نخورده موند.
- منو — گروه‌های «بدنسازی»/«ترید»: باکسِ زیرمجموعه‌ها دیگه جدا از دکمه‌ی گروه نیست — وقتی باز می‌شه، خودِ دکمه + زیرمجموعه‌ها توی یه باکسِ یکپارچه‌ی واحد جا می‌گیرن.
- منو — باگِ تشخیصِ زیرمجموعه‌ی فعال فیکس شد: چون هر دو بچه‌ی یه گروه (مثلاً «برنامه تمرینی»/«برنامه غذایی») روی همون pathname بودن و فقط با `?tab=` فرق داشتن، قبلاً هر دو با هم سبز می‌شدن. حالا NavDrawer با `useSearchParams` واقعیِ `tab` رو هم می‌خونه و دقیقاً فقط زیرمجموعه‌ی واقعاً فعال سبز می‌شه.
- منو — رنگِ متنِ زیرمجموعه‌ها از `--muted2` (خیلی کم‌کنتراست، توی تمِ روشن اصلاً دیده نمی‌شد) به `--muted` تغییر کرد.
- تمِ روشن — بک‌گراندِ همه‌ی باکس‌های داشبورد (`bg-dash-card`) از یه سفیدِ نسبتاً توپر (اپاسیتیِ ۵۵٪) به همون دستورِ شیشه‌ایِ خودِ منو تغییر کرد (اپاسیتیِ ۵٪ + backdrop-blur) — دیگه سفیدِ تخت نیست. پاپ‌آپ‌ها طبقِ درخواستِ صریح دست‌نخورده موندن.
- بدنسازی — دکمه‌های «شروع تمرین»/«پایان تمرین» یه کلاسِ Tailwindِ `border` کم داشتن (فقط رنگِ بوردر inline بود، نه عرضش) — توی تمِ روشن این باعث می‌شد کلاً بی‌بوردر/نامرئی به‌نظر برسن؛ فیکس شد.

## نسخه: v1.54.0 (کالری — چیدمانِ نام‌دارِ جدید، نمودارِ نسخه‌ی سوم با گیتِ ۳روزه، دوربین/گالری اسکن، منوی جعبه‌ای)

### چی تغییر کرد
- کالری — چیدمانِ دسکتاپ: با `grid-template-areas` بازچیده شد — «برنامه غذایی» ستونِ اصلیِ راست، سمتِ چپ دو ردیف («دوستان، روند موفقیت، نمودار» بالا؛ «کالری هر وعده، ریزِ درشت‌مغذی‌ها» پایین)، دقیقاً طبقِ ترتیبِ خواسته‌شده. موبایل مستقل از این، فقط به ترتیبِ DOM: برنامه غذایی، روند موفقیت، دوستان، کالری هر وعده، ریزِ درشت‌مغذی‌ها، نمودار.
- کالری — جداکننده‌ی بینِ نوارِ پیشرفت و «برنامه غذایی» حذف شد؛ استریکِ «روند موفقیت» توی موبایل یه‌کم بیشتر از گوشه فاصله گرفت.
- کالری — نمودار: نسخه‌ی سوم، این‌بار سهمِ عرضِ بیشتری (نسبت به استریک/دوستان) گرفت و محورِ Yِ سبک برگشت. یه گیتِ جدید هم اضافه شد — تا کاربر حداقل ۳ روز کالری ثبت نکرده، به‌جای نموداری بی‌معنی، پیام می‌ده چند روزِ دیگه مونده.
- کالری — پاپ‌آپِ تقویمِ «تاریخچه» حالا هم‌بکگراندِ بقیه‌ی پاپ‌آپ‌های کالری‌ـه (`liquid-glass-panel`).
- کالری — اسکنِ عکسِ غذا: به‌جای یه دکمه‌ی مبهم که مرورگر خودش تصمیم می‌گرفت دوربین باز کنه یا گالری، حالا صریح دو دکمه‌ی «دوربین»/«گالری» جداست.
- منو: زیرمجموعه‌های «بدنسازی»/«ترید» حالا توی یه جعبه‌ی واحدِ زیرِ خودِ گروه‌ان (نه لینک‌های شناورِ تورفته)، هرکدوم آیکونِ اختصاصیِ خودشون (سیب برای برنامه‌ی غذایی، دفترچه برای ژورنال، چک‌باکس برای چک‌لیست)، فونتِ کوچیک‌تر. آیکونِ «روتین» هم به آیکونِ تقویمِ هفتگی (که قبلاً تعریف شده بود ولی این‌جا استفاده نمی‌شد) تغییر کرد.
- بدنسازی/ترید: تبِ روی‌صفحه (SegmentedTabs) کاملاً حذف شد — انتخابِ برنامه‌ی تمرینی/غذایی یا ژورنال/چک‌لیست فقط از منو ممکنه. برای اینکه کلیکِ لینکِ منو حتی وقتی از قبل توی همون صفحه‌ای هم زنده اثر کنه، این دو صفحه از `useSearchParams` واقعی (به‌جای خوندنِ یک‌بارِ `window.location`) استفاده می‌کنن.

## نسخه: v1.53.0 (کالری — چیدمانِ فشرده‌ی هم‌قدِّ برنامه‌ی تمرینی، استریکِ هدری، نمودارِ نسخه‌ی دوم، دوستان)

### چی تغییر کرد
- کالری — اندازه‌ی باکس‌ها: چیدمانِ داشبورد هم‌قاعده‌ی داشبوردِ بدنسازی شد (گریدِ `items-stretch` + `CalorieFoodPlanCard` با `h-full` و لیستِ غذاهای اسکرول‌شونده‌ی سقف‌دار) — باکسِ «برنامه غذایی» حالا دقیقاً هم‌قدِّ ستون‌های کناری‌ـه، نه یه باکسِ کوتاهِ جدا.
- کالری — روند موفقیت: تمبرِ دایره‌ایِ وسطِ کارت حذف شد؛ استریک حالا بالای کارت سمتِ چپ (کنارِ تایتل)، فقط آیکونِ شعله + عدد، بدونِ هیچ دایره/باکسی دورش (با `StreakFlame`ِ مشترکِ همون کامپوننتی که برای استریکِ دوست‌ها استفاده می‌شه).
- کالری — نمودار: `CalorieChartCard` کاملاً از صفر بازساخته شد، با ارتفاعِ بسیار کمتر و بدونِ محورِ Yِ شلوغ، مناسبِ ابعادِ کوچیک‌ترِ باکسِ جدید.
- کالری — بخشِ دوستان: `DashFriendsCard` (همون کامپوننتِ بدنسازی) به داشبوردِ کالری اضافه شد؛ `/api/friends` یه `module=calorie` جدید گرفت که «روزهای موفقِ» هفتِ اخیر + استریکِ هرکدوم از دوست‌ها رو بر اساسِ `FoodLogEntry`/`CalorieTarget` خودشون حساب می‌کنه.
- کالری — باکس‌های ریزِ درشت‌مغذی‌ها و کالری هر وعده جمع‌وجورتر شدن (پدینگ/فاصله/سایزِ آیکون‌ها کوچیک‌تر) تا کلِ داشبورد توی نسخه‌ی دسکتاپ بدونِ اسکرولِ اضافه توی یک صفحه جا بشه.

## نسخه: v1.52.0 (کالری — بازطراحیِ نمودار/روند موفقیت/تغییر برنامه، منویِ آکاردئونی)

### چی تغییر کرد
- کالری — نمودار: `CalorieChartCard` کاملاً بازطراحی شد تا با بقیه‌ی کارت‌های داشبورد یکدست باشه — تایتل با آیکون هم‌قاعده‌ی بقیه‌ی کارت‌ها، رنگِ سبزِ نئونیِ مستقل حذف و با `var(--accent)` سراسریِ اپ جایگزین شد، نمایِ هفتگی/ماهانه از خطِ شلوغ به میله‌ایِ سرگرد ساده تغییر کرد، لیبلِ هدف به یه پیلِ کوچیک (هم‌قاعده‌ی `DashFilterButton`) تبدیل شد.
- کالری — روند موفقیت: خلاصه‌ی بالای `CalorieStreakCard` از «عددِ بزرگ + متنِ روز متوالی» به یه «تمبرِ» دایره‌ای با آیکونِ شعله + فقط عدد تغییر کرد؛ ردیفِ دایره‌ی روزها زیرش دست‌نخورده موند.
- کالری — «کالری امروز»: کارتِ جدای `CalorieTodayCard` حذف شد؛ خلاصه‌ی عدد/هدف + خطِ نازکِ پیشرفت + دکمه‌ی «تغییر برنامه» حالا بالای همون باکسِ «برنامه غذایی»ان (با یه جداکننده)، بدونِ متنِ «کالری امروز» و بدونِ درصد.
- کالری — تغییر برنامه: کلیک روی «تغییر برنامه» دیگه کلِ صفحه رو با فرم عوض نمی‌کنه — یه پاپ‌آپِ واقعی (`CalorieGoalModal`) روی همون داشبورد باز می‌شه و با بستنش دقیقاً برمی‌گردی به همون‌جا؛ فرمِ اولین‌بارِ ساختِ هدف (وقتی هنوز هیچ برنامه‌ای نداری) دست‌نخورده و درون‌صفحه‌ای موند.
- منو: «بدنسازی» و «ترید» توی `NavDrawer` به گروه‌های آکاردئونی تبدیل شدن — کلیک روش‌ها زیرمجموعه‌هاشون (برنامه‌ی تمرینی/برنامه‌ی غذایی، ژورنال/چک‌لیست) رو باز می‌کنه و مستقیم به همون تبِ صفحه‌ی مقصد دیپ‌لینک می‌کنه (`?tab=`)؛ وقتی توی یکی از این صفحه‌هایی، همون گروه پیش‌فرض بازه.

## نسخه: v1.51.2 (کالری — تطبیقِ دقیقِ «کالری امروز» با طرح، حذفِ تاریخچه از برنامه غذایی)

### چی تغییر کرد
- کالری — «کالری امروز»: طبقِ تأکیدِ صریحِ کاربر، دیگه کارت/باکس نیست — فقط تایتل + عددِ مصرف‌شده/هدف + یک خطِ نازکِ افقی (بدونِ بوردر/بک‌گراند دورش، بدونِ درصد) که با انیمیشن پر می‌شه؛ دقیقاً هم‌شکلِ شماتیکِ کاربر.
- کالری — برنامه غذایی: آکاردئونِ «تاریخچه غذایی (۳۰ روز اخیر)» که قبلاً ته‌ِ این کارت بود حذف شد (طبقِ درخواستِ صریح) — دکمه‌ی «تاریخچه»ی بالای صفحه (تقویمِ ماهانه) جداست و دست‌نخورده باقی موند.

## نسخه: v1.51.1 (کالری — تقویمِ تاریخچه‌ی مخصوصِ کالری)

### چی تغییر کرد
- کالری — تقویمِ تاریخچه: دکمه‌ی «تاریخچه» کنارِ «امروز» اضافه شد؛ یه تقویمِ ماهانه (با ناوبریِ ماه قبل/بعد) باز می‌شه که هر روز رو بر اساسِ برخورد با هدفِ کالری رنگ می‌کنه — نقطه‌ی سبز یعنی اون روز ثبت شده و زیرِ هدف بوده، قرمز یعنی بالای هدف؛ روزهای بدونِ ثبت خنثی می‌مونن. کلیک روی هر روز مستقیم به همون روز می‌بره (نوارِ انتخابِ روزِ بالای صفحه هم دورِ همون تاریخ وسط‌چین می‌شه).
- عمداً کامپوننتِ `HistoryCalendar`ِ روتین دوباره استفاده نشد — چون اون به دیتای تسک/خواب وصله و این‌جا نمایشِ غلط می‌داد؛ به‌جاش `CalorieHistoryCalendar` مستقل با Tailwind/dash-scope ساخته شد، هم‌راستا با بقیه‌ی بخشِ کالری.

## نسخه: v1.51.0 (کالری — بازطراحیِ کامل طبقِ طرح: انتخابِ روز، نوارِ پیشرفتِ متحرک، برنامه‌ی غذایی یکپارچه)

### چی تغییر کرد
- کالری — انتخابِ روز: یه نوارِ روزهای هفته (عیناً هم‌الگوی انتخابِ روزِ داشبوردِ بدنسازی) بالای صفحه اضافه شد؛ کل صفحه (کالری امروز، برنامه‌ی غذایی، نمودار، ریزِ درشت‌مغذی‌ها، کالری هر وعده) حالا بر اساسِ روزِ انتخاب‌شده کار می‌کنه، نه فقط «امروز». روزهای گذشته/آینده فقط نمایشی‌ان — دکمه‌ی افزودن/حذف فقط برای امروز فعاله (دقیقاً هم‌قاعده‌ی برنامه‌ی تمرینیِ بدنسازی).
- کالری — کارتِ «کالری امروز»: حلقه به یه نوارِ افقی تبدیل شد که با انیمیشنِ پرشدن (+ هایلایتِ نرمِ متحرکِ روی نوار) پر می‌شه.
- کالری — برنامه‌ی غذایی: کارت‌های جدا‌ی «افزودن غذا» و «امروز» یکی شدن؛ حالا یه کارتِ واحدِ «برنامه غذایی» با یه دکمه‌ی «افزودن» که یه پاپ‌آپ باز می‌کنه — جستجو در کاتالوگ، وارد کردنِ دستی (حالا با پروتئین/کربوهیدرات/چربیِ اختیاری هم)، یا اسکن با هوش مصنوعی.
- کالری — ریزِ درشت‌مغذی‌ها: دیگه فقط به اسکنِ AI محدود نیست — با واردکردنِ دستیِ پروتئین/کربوهیدرات/چربی توی فرمِ افزودن هم باز می‌شه.
- کالری — کالری هر وعده: از کارتِ «کالری امروز» جدا شد و کارتِ بزرگ‌ترِ خودش رو گرفت، چون تعدادِ وعده‌ها متغیره (۲ تا ۶ تا).
- کالری — چیدمانِ دسکتاپ: گریدِ سه‌ستونه (عیناً هم‌الگوی بدنسازی) — کالری‌امروز+برنامه‌غذایی سمتِ راست، روندِ موفقیت وسط، نمودار+ریزِ‌درشت‌مغذی+کالری‌هروعده سمتِ چپ.
- کالری — باگ: درصدِ «کالری امروز» قبل از چک‌کردنِ «رد شدن از هدف» به ۱۰۰٪ سقف زده می‌شد، یعنی نوار/رنگ هیچ‌وقت قرمز نمی‌شد حتی وقتی کاربر چند برابرِ هدفش خورده بود — رفع شد؛ الان همیشه درصدِ واقعی محاسبه می‌شه (فقط عرضِ خودِ نوار برای نمایش سقف ۱۰۰٪ داره، نه رنگ/عددش).

## نسخه: v1.50.0 (کالری — ریزِ درشت‌مغذی‌ها با اسکنِ عکسِ غذا توسطِ هوش مصنوعی)

### چی تغییر کرد
- کالری — ریزِ درشت‌مغذی‌ها: سه باکسِ جداگانه‌ی پروتئین/کربوهیدرات/چربیِ طرحِ اولیه توی **یک کارتِ واحد** ادغام شدن. این کارت فقط وقتی عدد نشون می‌ده که کاربر حداقل یک غذا رو امروز با هوش مصنوعی اسکن کرده باشه — وگرنه یه حالتِ قفل با دعوت به اسکن نشون می‌ده. چون این اپ کاتالوگِ دستیِ درشت‌مغذی برای هزاران غذا نداره، این تنها راهِ داشتنِ عددِ واقعیِ پروتئین/کربوهیدرات/چربیه.
- کالری — اسکنِ عکسِ غذا با AI: یه پاپ‌آپِ جدید («اسکن غذا با هوش مصنوعی») عکس می‌گیره/آپلود می‌کنه، به‌کمکِ Claude API (چندوجهی/تصویری) کالری+پروتئین+کربوهیدرات+چربی رو تخمین می‌زنه، و قبل از ثبتِ نهایی نتیجه رو برای بازبینی نشون می‌ده (دقیقاً هم‌الگوی رودمپ/برنامه‌ی ورزشیِ AI). اگه عکس غذا نباشه یا کلیدِ API تنظیم نشده باشه، خطای واضح و بدونِ کرش نشون می‌ده.
- دیتابیس: مدلِ `FoodLogEntry` سه فیلدِ اختیاریِ `proteinG`/`carbsG`/`fatG` + `aiScanned` گرفت — فقط ثبت‌های اسکن‌شده با AI این فیلدها رو پر می‌کنن.
- امنیت: روتِ `/api/calorie/scan` مثلِ بقیه‌ی فیچرهای AIِ اپ، سمتِ سرور با `requireModule(CALORIE)` گیت شده و rate-limit جدا داره (۱۵ اسکن در ساعت به‌ازای هر کاربر، چون هزینه‌ی فراخوانیِ تصویری از متنی بیشتره)؛ حجمِ عکس و فرمتش هم قبل از فراخوانیِ AI اعتبارسنجی می‌شه.

## نسخه: v1.49.3 (نمودار کالری — بدونِ قرمزشدنِ خط بعدِ عبور از هدف، خطِ نازک‌تر)

### چی تغییر کرد
- کالری — نمودارِ روند: نقطه‌ها/خط دیگه بعدِ عبور از کالریِ مجاز قرمز نمی‌شن — همیشه همون سبزِ نئونیِ نمودار می‌مونن (فقط خطِ نقطه‌چینِ هدف مرجعِ بصریِ عبور از سقفه). ضخامتِ خطِ نمودار هم از ۲.۴ به ۱.۶ کم شد تا ظریف‌تر باشه.

## نسخه: v1.49.2 (نمودار کالری — فیکسِ رنگ، خط‌های راهنما، سایه‌ی زیرِ خط، هالهٔ نور)

### چی تغییر کرد
- کالری — نمودارِ روند: بعد از مقایسه‌ی مستقیم با عکسِ رفرنس چهار مشکل پیدا و رفع شد: (۱) رنگِ خط/نقطه‌ها/خطِ هدف از `var(--accent)`ِ کدرِ سراسریِ سایت (#00A86B) به یه سبزِ نئونیِ اختصاصیِ خودِ نمودار (#22C55E) عوض شد؛ (۲) خط‌های راهنمای افقیِ نمودار که تقریباً نامرئی بودن (opacity ۰.۳۵) حالا واضح دیده می‌شن؛ (۳) سایه/گرادیانِ سبزِ زیرِ خط که قبلاً حذف شده بود برگشت؛ (۴) هالهٔ نورِ خط که به‌خاطرِ اعمال‌شدنِ `drop-shadow` توی مختصاتِ داخلیِ SVG (که با مقیاسِ نمایشِ بزرگ‌تر، چند برابر پخش‌تر از انتظار می‌شد) بیش‌ازحد پخش بود، با کاهشِ چشمگیرِ شعاعِ بلور جمع‌وجورتر شد.

## نسخه: v1.49.1 (نمودار کالری — پیاده‌سازیِ عینِ طرحِ رفرنس)

### چی تغییر کرد
- کالری — نمودارِ روند: کاملاً بازسازی شد تا دقیقاً عینِ عکسِ رفرنسِ کاربر باشه: تایتل و تب‌های روزانه/هفتگی/ماهانه توی یک ردیف (تایتل راست، تب‌ها چپ)؛ محورِ Y همیشه ۵ فاصله‌ی گردِ سه‌رقمی‌جدا (مثلِ ۵۰۰/۱٬۰۰۰/۱٬۵۰۰...) به‌جای فاصله‌های درصدی؛ محورِ X توی حالتِ روزانه تیک‌های ثابتِ هر ۴ ساعت (۰۰:۰۰ تا ۲۴:۰۰) داره و نقطه‌ها بر اساسِ ساعتِ واقعیِ ثبت (نه ایندکس) روی همون محور جا می‌گیرن؛ خطِ هدف سبزِ نقطه‌چین شد (نه نارنجی) با لیبلِ «هدف: X کالری» بالا-راستِ خودِ نمودار؛ خط ضخیم‌تر با هاله‌ی نورانیِ سبز، نقطه‌های سفیدِ درخشان (نقطه‌ی آخر/انتخاب‌شده بزرگ‌تر)، تولتیپِ قرصی‌شکلِ سبز به‌جای باکسِ خط‌دار.
- کالری — باگ: تبِ روزانه/هفتگی/ماهانه وقتی کنارِ تایتل توی یه ردیف قرار گرفت (به‌جای ردیفِ تمام‌عرضِ خودش)، چون بچه‌های داخلیش `flex:1/min-width:0` دارن، به حداقلِ عرض جمع می‌شد و رویِ‌هم می‌افتاد؛ با یه عرضِ ثابتِ معقول (۱۸۴px موبایل / ۲۱۲px دسکتاپ) فیکس شد.
- کالری — باگ: لیبلِ «هدف: X کالری» وقتی به‌صورتِ `<text>`ِ SVG با محتوای ترکیبیِ فارسی/عدد رندر می‌شد، مرورگر `text-anchor="end"` رو برای این متنِ RTL درست حساب نمی‌کرد و از لبه‌ی راستِ نمودار بیرون می‌زد؛ به یه overlayِ معمولیِ HTML (دقیقاً مثلِ تولتیپِ زیرش) تبدیل شد که RTL رو درست هندل می‌کنه.

## نسخه: v1.49.0 (کالری — هم‌ترازی کامل با UI/UX بدنسازی و روتین + باگ‌فیکس ریسپانسیو)

### چی تغییر کرد
- کالری — معماریِ UI کاملاً عوض شد: به‌جای سیستمِ قدیمیِ CSS تخت (که فقط توی این بخش استفاده می‌شد)، الان دقیقاً از همون سیستمِ Tailwindِ `DashCard`/`dash-scope`/گریدِ ریسپانسیو استفاده می‌کنه که داشبوردِ روتین (`app/weekly`) و بدنسازی (`ExerciseDashboard`) دارن — همون رادیوس/بوردر/بک‌گراند/بلور/انیمیشنِ fade-up، همون مقیاسِ تایپوگرافیِ `text-[Npx] sm:text-[Mpx]`.
- کالری — چیدمانِ دسکتاپ: صفحه دیگه روی دسکتاپ به ستونِ باریکِ ۶۲۰پیکسلی محدود نیست — مثلِ بدنسازی/روتین تا ۱۳۲۰پیکسل باز می‌شه (`exercise-dash-breakout` حالا هم‌زمان روی هر دو تبِ «بدنسازی» و «کالری» فعاله، نه فقط بدنسازی) و از ۱۰۲۴پیکسل به بالا یه گریدِ دوستونه می‌شه (کارتِ کالریِ امروز+نمودار سمتِ راست، روندِ موفقیت+افزودنِ غذا+امروز سمتِ چپ)؛ زیرِ اون هم‌چنان تک‌ستونه می‌مونه.
- کالری — کارت‌ها به فایل‌های جدا تقسیم شدن (دقیقاً هم‌الگوی بدنسازی): `CalorieTodayCard`، `CalorieAddFoodCard`، `CalorieLogCard` تازه ساخته شدن؛ `CalorieChartCard` و `CalorieStreakCard` هم بازنویسی شدن روی `DashCard`.
- کالری — باگ: نمودارِ روند با `preserveAspectRatio="none"` روی یه ارتفاعِ ثابت/عرضِ سیال بود، یعنی با هر تغییرِ عرضِ صفحه (موبایل/دسکتاپ) نسبتِ ابعادش کش/فشرده می‌شد؛ حالا wrapper با `aspect-ratio` واقعی و SVG با `preserveAspectRatio="xMidYMid meet"` رندر می‌شه، بدونِ هیچ اعوجاجی.
- کالری — باگ: ردیف‌های «امروز» (لیستِ غذاهای ثبت‌شده) از کلاس‌های `.item-row`/`.name` استفاده می‌کردن که اصلاً توی CSS تعریف نشده بودن — یعنی بدونِ کارت/بوردر/هاور رندر می‌شدن؛ حالا ردیف‌های واقعیِ Tailwindِ کارت‌دار دارن.
- کالری — باگ: چند دکمه (دکمه‌های «بعدی/قبلی»ِ راهنمای اولین‌بار، دکمه‌های ذخیره/انصرافِ ویرایشِ وعده‌ها) وقتی زیرِ `dash-scope` قرار گرفتن، بوردرِ نامرئی داشتن — چون ریست‌کننده‌ی `button{}`ِ سراسری، `border-width` رو صفر می‌کنه و این دکمه‌ها فقط رنگِ بوردر رو inline ست کرده بودن، نه خودِ عرضِ بوردر رو؛ همه با کلاسِ `border`ِ Tailwind فیکس شدن.
- کالری — باگ: فیلدِ «مقدار» (کنارِ دکمه‌ی واحدِ اندازه‌گیری) عرضِ Tailwindی‌ش (`w-[68px]`) رو می‌باخت چون `@tailwind utilities` بالای `globals.css` ایمپورت می‌شه و کلاسِ عمومیِ `.wsearch-newform-name{width:100%}` (که پایین‌ترِ همون فایله) توی تساویِ اسپسیفیسیتی همیشه برنده بود — همین باعث می‌شد فیلدِ مقدار تمامِ عرض رو بگیره و دکمه/منویِ واحد به گوشه‌ی چپِ صفحه پرت بشه؛ با یه کلاسِ اختصاصیِ بعدِ `.wsearch-newform-name` فیکس شد.
- کالری — پاسخگویی: اندازه‌ی حلقه‌های پیشرفت (کالری امروز، سهم هر وعده)، فاصله‌ها و فونت‌ها الان `sm:` دارن (نه فقط یه سایزِ ثابت مثلِ قبل).

## نسخه: v1.48.0 (ریدیزاینِ کالری — نمودارِ روند، روندِ موفقیت، راهنمای اولین‌بار)

### چی تغییر کرد
- کالری — نمودارِ روند: بخشِ «کالری امروز» حالا یه نمودارِ خطیِ کاملاً دست‌سازِ SVG داره (بدونِ کتابخانه‌ی چارت) با سه تبِ روزانه/هفتگی/ماهانه — روزانه تجمعیِ کالریِ امروز بر اساسِ ساعتِ ثبت رو نشون می‌ده، هفتگی/ماهانه جمعِ هر روز رو؛ خطِ چین‌دارِ زردرنگ هدفِ روزانه رو نشون می‌ده و با کلیک/لمسِ هر نقطه یه تولتیپِ مقدار باز می‌شه.
- کالری — روندِ موفقیت: کارتِ جدیدی که «X روز متوالی» رو با یه ردیفِ دایره‌ی حرفِ‌روزها نشون می‌ده (تیکِ سبز = اون روز کالری ثبت شده و از هدف رد نشده)؛ امروز همیشه سمتِ راست و «در انتظار»ه چون هنوز روز تموم نشده. بدونِ دکمه‌ی «مشاهده جزئیات».
- کالری — راهنمای اولین‌بار: دقیقاً مثلِ آموزشِ چهارمرحله‌ایِ بخشِ ورزش، یه پاپ‌آپِ راهنما (حلقه‌ی امروز، نمودار، روندِ موفقیت، افزودنِ غذا) اولین‌باری که صفحه‌ی کالری با یه هدفِ ازقبل‌ساخته‌شده باز می‌شه خودکار نشون داده می‌شه، بعدش دیگه تکرار نمی‌شه.
- کالری — انیمیشن: بخش‌های اصلیِ صفحه (حلقه‌ی امروز، نمودار، روندِ موفقیت) با fade-up پلکانی لود می‌شن؛ خطِ نمودار با انیمیشنِ کشیده‌شدن (pathLength) و نقطه‌هاش با fade-inِ پلکانی ظاهر می‌شن.

> بخشِ درشتِ حلقه‌های پروتئین/کربوهیدرات/چربی که توی طرحِ دست‌کشیده بود، فعلاً پیاده نشد — چون FoodLogEntry و کاتالوگِ غذاهای فعلی (lib/foodSeed.ts) هیچ داده‌ی ماکرویی ندارن؛ نیاز به مایگریشنِ اسکیما + پرکردنِ درشتی‌مغذی‌های ~۲۳۰ آیتمِ کاتالوگ داره، یه کارِ جدا از ریدیزاینِ UI.

### چی تغییر کرد
- بدنسازی — مرورِ برنامه: دکمه‌ی «ویرایش» از پایینِ صفحه حذف شد؛ به‌جاش هر باکسِ روز یه آیکونِ مدادِ گوشه‌ی بالا-چپِ خودش داره که مستقیم می‌بره تو نمای همون روز برای ویرایش. متنِ دکمه‌ی نهایی هم از «ثبتِ برنامه» (با اعرابِ زیرِ ث که بد رندر می‌شد) به «ثبت برنامه» ساده تغییر کرد.
- بدنسازی — دیاگرامِ عضلاتِ درگیر: هر عضله حالا یه خطِ دورِ نازک داره که از عضله‌های کناری جداش می‌کنه (دقیقاً مثلِ عکسِ آناتومیِ مرجع)، و عضله‌های غیرفعال هم شفافیتِ بیشتری گرفتن تا همیشه قابلِ‌تشخیص باشن، نه فقط عضله‌ی هدف.

### چی تغییر کرد
- بدنسازی — افزودنِ دستی: موقعِ تعیینِ مشخصاتِ یک حرکت دیگه ازت نمی‌پرسه «ست‌وتکراره یا زمانی» — از روی الگوی خودِ حرکت (کاردیو/انعطاف‌پذیری = زمانی، بقیه = ست‌وتکرار) خودکار تشخیص می‌ده و فقط فیلدهای مربوطه رو نشون می‌ده.
- بدنسازی — مشاهده حرکات: بک‌گراندِ پاپ‌آپ و کارتِ جزئیات بلورِ بیشتر/کدرتری گرفتن (بلور ۲۲→۳۲px، شفافیتِ کمتر) چون قبلی خیلی شفاف بود و پشتش زیادی معلوم بود.

### چی تغییر کرد
- بدنسازی — کارتِ جزئیاتِ حرکت: دیگه چسبیده به لبه‌ی پایینِ صفحه نیست — مثلِ پاپ‌آپِ اصلی یه کارتِ شناورِ گردِ کامله؛ اسمِ حرکت از کنارِ ضربدر رفت زیرِ جای‌عکس؛ فاصله‌ی بخش‌ها فشرده‌تر و منظم‌تر شد.
- بدنسازی — دیاگرامِ عضلاتِ درگیر: کاملاً بازسازی شد با یه سیلوئتِ توپرِ بدن (نه فقط طرح‌واره‌ی خط‌دور) به‌عنوانِ زمینه، پشتِ منحنی‌های هر عضله.
- بدنسازی — افزودنِ دستی: بک‌گراندِ پاپ‌آپِ «افزودن حرکت» دوباره برگشت به سبکِ شیشه‌ایِ هدر (نه سیاه)؛ دکمه‌ی «مرحله بعد» کوچیک‌تر شد؛ فونتِ تایتلِ «کدوم روزها...» بزرگ‌تر شد؛ ردیف‌های حرکتِ ثبت‌شده‌ی هر روز دقیقاً هم‌سبکِ ردیف‌های «مشاهده حرکات» شدن.

### چی تغییر کرد
- بدنسازی — افزودنِ دستی: باکسِ «روز ...» بزرگ‌تر و پررنگ‌تر شد (پدینگ/فونت/رادیوسِ بیشتر) چون تنها لنگرِ بصریِ صفحه بود؛ بک‌گراندِ پاپ‌آپِ «افزودن حرکت» حالا مشخصاً سیاهه (هم‌سبکِ بلورِ هدر، نه شیشه‌ای‌ِ کم‌رنگِ مشترکِ مشاهده‌ی حرکات).
- بدنسازی — مشاهده حرکات: ۸ حرکتِ رایجِ جامانده اضافه شد (۱۴۱ به ۱۴۹): پرس سینه دست جمع، پرس سینه دکلاین، اسکوات تک‌پا، پروانه معکوس دمبل، پروانه معکوس دستگاه، دراز و نشست، بالا آوردن پا خوابیده، کرانچ معکوس.
- بدنسازی — کارتِ جزئیاتِ حرکت: دکمه‌ی ضربدر که به‌خاطرِ نبودِ کلاسِ dash-scope روی این کارتِ پورتال‌شده بک‌گراندِ پیش‌فرضِ سراسریِ دکمه‌ها رو گرفته بود، حالا شفافه؛ دستگیره‌ی اسلایدِ بالای کارت حذف شد؛ تایتل‌های داخلی (میزانِ سختی/دستورالعمل/عضلاتِ درگیر/مزایا) رنگِ اکسنت گرفتن.
- بدنسازی — دیاگرامِ عضلاتِ درگیر: به‌جای rect/circle/ellipseِ خامِ قبلی، حالا هر عضله یک مسیرِ بزیه‌ایِ دست‌ساز داره که شکلِ واقعی‌ترِ سینه/شکمِ سیکس‌پک/جلوبازوی دوکی/باسنِ گرد/... رو دنبال می‌کنه، و طرح‌واره‌ی بدن هم با سر/گردن/تنه/دست/پای منحنی‌تر جایگزین شد.

### چی تغییر کرد
- بدنسازی — مشاهده حرکات: گوشه‌های پاپ‌آپ گردتر شدن (سبکِ اپل)؛ ردیفِ هر حرکت دیگه توضیحِ گروهِ عضلانی نداره — فقط اسمِ حرکت و میزانِ سختی (ستاره) سمتِ چپ.
- بدنسازی — افزودنِ دستی: کلیک روی هر روز (مثلاً شنبه) دیگه نیاز به یه لیستِ جداگانه‌ی زیرِ پیل‌ها نداره — مستقیم می‌ره توی باکسِ همون روز؛ عنوانِ «حرکات این روز» شد «روز شنبه» و تایتلِ تکراریِ بالای صفحه حذف شد. توی پاپ‌آپِ «افزودن حرکت»: جهتِ نوارِ جستجو (که با dir="auto" قاطی می‌شد) با dir="rtl" فیکس شد و بک‌گراندش هم‌سبکِ هدرِ سایت شد؛ ردیف‌های حرکت دقیقاً هم‌سبکِ «مشاهده حرکات» (اسم + ستاره‌ی سختی، بدون توضیح) شدن؛ دکمه‌ی «افزودن» (که چون پاپ‌آپ پورتال‌شده به body بود، به‌جای استایلِ پیلِ سبز می‌افتاد رو دایره‌ی نقطه‌چینِ پایه) با کلاسِ مستقلِ خودش درست شد و آیکونِ «+» کنارش حذف شد.

### چی تغییر کرد
- بدنسازی — افزودن با AI: تایتلِ «قد و وزن خود را وارد کنید» با فونتِ بزرگ‌تر نوشته می‌شه؛ اینتر توی فیلدِ قد کاربر رو می‌بره فیلدِ وزن، و اینتر توی فیلدِ وزن (وقتی هر دو فیلد پر باشن) خودکار می‌ره مرحله‌ی بعد.
- بدنسازی — افزودنِ دستی: کلیک روی هر روز مستقیم واردِ باکسِ همون روز می‌شه؛ دکمه‌ی «افزودن» به‌جای رندرِ اینلاینِ لیستِ کاتالوگ، یه پاپ‌آپِ مستقل باز می‌کنه (دقیقاً مثلِ پاپ‌آپِ «مشاهده حرکات»)؛ کارتِ تعیینِ مشخصاتِ حرکت فقط یک ضربدرِ بی‌بک‌گراند بالا داره که برمی‌گردونه به لیستِ جستجو؛ دکمه‌های افزودن/بازگشت/بستن/ثبتِ باکسِ روز همه بی‌بک‌گراند شدن؛ بعدِ تکمیلِ همه‌ی روزها یه مرحله‌ی «مرورِ برنامه» جدید کلِ برنامه رو نشون می‌ده با دکمه‌ی ثبتِ نهایی و دکمه‌ی «ویرایش» که برمی‌گردونه به لیستِ روزها.

### چی تغییر کرد
- بدنسازی — کاتالوگِ حرکات: پاپ‌آپِ «مشاهده حرکات» وسطِ صفحه نمی‌نشست (باگِ تداخلِ ترنسفورمِ framer-motion با سنترینگِ CSS‌ی کلاسِ پایه) — حالا با یه wrapperِ فلکس‌باکسِ مستقل کاملاً وسط می‌شینه؛ بک‌گراند/بلورِ کارتِ جزئیات هم با استایلِ شیشه‌ایِ جدیدِ خودِ پاپ‌آپ هماهنگ شد.
- بدنسازی — افزودن با AI: آیکونِ اخطارِ کنارِ پیامِ خطای هر فیلد جلوی متن می‌افتاد نه پشتش — ترتیبش برعکس شد؛ تایتلِ اصلیِ صفحه‌ی انتخابِ «افزودن برنامه» بزرگ‌تر شد و فاصله‌ی بیشتری از بالا و از ردیفِ انتخابِ زیرش گرفت؛ دکمه‌ی «مرحله بعد» کوچیک‌تر و پرِ توپر (بک‌گراندِ رنگیِ اکسنت به‌جای فقط بوردر) شد.

## نسخه: v1.40.0 (پولیشِ فرمِ افزودنِ برنامه، کاتالوگِ حرکات به‌شکلِ پاپ‌آپ + ستاره‌ی سختی)

### چی تغییر کرد
- بدنسازی — افزودن با AI: دکمه‌ی «مرحله بعد» چپ‌چین شد (نه تمام‌عرض)، نقاطِ پیشرفتِ مراحل رفتن پایینِ صفحه (کنارِ همون دکمه)، خطای هر فیلد (قد/وزن) حالا با آیکونِ اخطار کنارش نشون داده می‌شه، و تایتلِ اصلیِ هر مرحله (مثلاً «قد و وزن خود را وارد کنید») با فونتِ بزرگ‌تر از لیبل‌های زیرفیلد نوشته می‌شه.
- بدنسازی — افزودنِ دستی: دکمه‌ی «افزودن» توی باکسِ «حرکات این روز» حالا متن+آیکون داره و سبزه؛ خودِ باکس بزرگ‌تر شد؛ عنوانِ «کدوم روزها رو می‌خوای برنامه داشته باشی؟» تصحیح و فاصله‌اش بیشتر شد؛ دکمه‌ی «ثبتِ برنامه» پرِ توپر و کاملاً گرد شد (نه فقط بوردر).
- بدنسازی — مشاهده حرکات: به‌جای صفحه‌ی تمام‌عرض، حالا یک پاپ‌آپ استاندارده (هم‌بک‌گراند/بلورِ هدرِ سایت)؛ فیلدِ جستجو راست‌چینِ اجباری شد؛ میزانِ سختیِ هر حرکت با ستاره (تا ۵ ستاره، بر اساسِ الگوی حرکتی) توی کارتِ جزئیات نشون داده می‌شه.

## نسخه: v1.39.0 (یکدست‌سازیِ گیتِ ورود، فیکسِ درزِ نوارِ قیمت، ریدیزاینِ افزودنِ حرکتِ دستی)

### چی تغییر کرد
- گیتِ «برای استفاده از این سرویس وارد شوید»: بخشِ کالری و رودمپ (هابِ رودمپ‌ها + رودمپِ جدید) داشتن پیام/ظاهرِ دست‌ساز و ناهماهنگ یا اصلاً هیچ گیتی نداشتن (رودمپ‌ها برای مهمون فقط دکمه‌ی «+» رو نشون می‌داد) — همه‌شون حالا از همون کامپوننتِ مشترکِ `AuthGate` استفاده می‌کنن، دقیقاً هم‌شکلِ بدنسازی/ترید.
- روتین: وقتی کاربر وارد نشده، کارتِ «برنامه‌های امروز» هم حالا همون گیتِ ورود رو نشون می‌ده (بقیه‌ی بخش‌های صفحه — انتخابِ روز، برنامه‌ی هفتگی و... — همچنان برای مهمون با localStorage کار می‌کنه).
- نوارِ قیمت‌های ترید: یک پدینگِ داخلِ خودِ ترکِ درحال‌چرخش باعث می‌شد محاسبه‌ی چرخشِ ۵۰٪ی دقیق نباشه و لحظه‌ی وصل‌شدنِ ابتدا به انتها یه پرش/شکاف داشته باشه — پدینگ منتقل شد به viewport (که ترنسفورم نمی‌گیره)، الان چرخش کاملاً بی‌درزه.
- بدنسازی — افزودنِ دستیِ حرکت: حرکتی که اضافه می‌کنی از لیستِ کاتالوگ محو می‌شه (و با حذف از برنامه دوباره برمی‌گرده)؛ پایینِ صفحه یک باکسِ «حرکات این روز» با دکمه‌ی افزودنِ گردِ بالا-چپِ خودِ باکس؛ فاصله‌ی بالای صفحه بیشتر شد؛ دکمه‌ی «ثبتِ برنامه» رفت سمتِ چپ به‌جای تمام‌عرض/وسط.

## نسخه: v1.38.0 (ریدیزاینِ کاملِ افزودنِ برنامه — AI و دستی)

### چی تغییر کرد
- بدنسازی — افزودن با AI: خطای قد/وزن حالا زیرِ همون فیلد نشون داده می‌شه؛ ترتیبِ مراحل به قد/وزن → هدف → روزها+سطح → توضیح تغییر کرد (حداقلِ روزهای هر هدف الان درست و با توجه به سطحِ واقعی چک می‌شه)؛ هدرِ فرم فقط یک دکمه‌ی آیکونیِ بازگشت + بستن داره (بدونِ تایتلِ ثابت)؛ متنِ مرحله‌ی اول و دوم واضح‌تر شد؛ باکسِ توضیحات مستطیلی و راست‌چین شد و دیگه ریسایز نمی‌شه؛ فاصله‌های همه‌ی مراحل بیشتر شد.
- بدنسازی — افزودنِ دستی: کلاً ریدیزاین شد — اول روزهای هفته رو (چندتایی) انتخاب می‌کنی، بعد با کلیک روی هر روز وارد نمای اختصاصیِ همون روز می‌شی و از اونجا حرکت اضافه می‌کنی؛ «تمرکزِ امروز» رو دیگه خودت تایپ نمی‌کنی — سیستم از روی حرکاتِ اضافه‌شده تشخیص می‌ده؛ دیگه نیازی به قبول‌کردنِ قوانین نیست.
- مشاهده حرکات: کاتالوگ حدود ۴۵ حرکتِ جدید (دستگاه/سیم‌کش/کاردیو/انعطاف‌پذیری) گرفت؛ جستجو+فیلتر+لیست همه توی یک باکسِ واحد شدن؛ ردیف‌های لیست دیگه جای‌عکس ندارن؛ اسکرول‌بارها مخفی شدن؛ دکمه‌ی بازگشت فقط آیکونه.

## نسخه: v1.37.0 (کاتالوگِ تمام‌صفحه، فرمِ چهارمرحله‌ایِ AI، افزودنِ دستیِ کاتالوگ‌محور)

### چی تغییر کرد
- روتین: انیمیشنِ تیک‌خوردن سریع‌تر شد؛ ضربدرِ قرمز فقط برای روزهای واقعاً گذشته نشون داده می‌شه (نه وسطِ همون روز).
- برنامه‌ی هفتگی: همپوشانیِ ساعتِ بیداری/خواب با متنِ کنارش برطرف شد.
- بدنسازی — مشاهده حرکات: به‌جای پاپ‌آپِ کوچیک، کلِ صفحه رو با انیمیشن می‌گیره؛ لیستِ حرکات + جستجو/فیلترِ گروهِ عضلانی، با زدن روی هرکدوم یه کارتِ جزئیات (جای عکس، دستورالعمل، عضلاتِ درگیر، مزایا) از پایین بالا میاد.
- بدنسازی — افزودن با AI: فرمِ تک‌صفحه‌ای به چهار مرحله تبدیل شد (قد/وزن → روزها+سطح → هدف → توضیحِ آزاد)؛ گزینه‌های هدف بیشتر شدن؛ فیلدِ «دوره‌ی تمرینی» حذف شد؛ محدودیتِ جسمی به مرحله‌ی توضیح منتقل شد؛ AI اول امکان‌پذیریِ خواسته رو بررسی می‌کنه و در صورتِ رد، مثلِ یه چت‌بات توضیح می‌ده؛ قوانین فقط بارِ اولِ ساختِ هر برنامه‌ای (AI یا دستی) نشون داده می‌شه، با دکمه‌ی بازگشتِ بالا-راست.
- بدنسازی — افزودنِ دستی: روزهای هفته حالا هفت باکس با حرفِ اول، همه در یک ردیف؛ حرکات دیگه تایپِ آزاد نیستن — از کاتالوگ با جستجو انتخاب می‌شن و بعد از زدنِ «افزودن»، تعدادِ ست/تکرار یا (برای حرکاتِ زمان‌محور) مدت‌زمان پرسیده می‌شه.
- ترید: ترتیبِ تب‌ها عوض شد — ژورنال اول، چک‌لیست دوم.

## نسخه: v1.36.0 (فیکسِ تیک‌زدنِ زودهنگام، جدولِ راست‌چین، سه حرکتِ جایگزین)

### چی تغییر کرد
- روتین: دیگه نمی‌شه یه برنامه‌ی امروز رو قبل از رسیدنِ ساعتِ شروعش تیک زد — چک‌باکسش تا اون لحظه غیرفعاله (مثلِ حالتِ گذشته/ضربدرخورده، ولی برعکس).
- بدنسازی: ستون‌های ست/تکرار-زمانِ جدولِ برنامه راست‌چین شدن (نه وسط‌چین/چپ‌چین) و خطِ عمودیِ بینِ ستون‌ها برداشته شد.
- بخشِ «توجه»: خودِ کلمه‌ی «توجه» قرمزه، بقیه‌ی متن رنگِ اصلیِ همون تم می‌گیره (سبز توی تمِ تیره، نارنجی/کهربایی توی تمِ روشن) — نه یک رنگِ ثابت برای کل جمله.
- انیمیشنِ ورود به حالتِ تمام‌صفحه‌ی «شروع تمرین» یه گلیچِ کوچیک داشت (کارت‌های کناری هم‌زمان با تغییرِ چیدمانِ گرید به‌جای خودشون می‌پریدن)؛ با `popLayout` روی AnimatePresence و ساده‌سازیِ لایه‌بندیِ انیمیشن فیکس شد.
- دکمه‌ی شروعِ تایمرِ حرکاتِ زمان‌محورِ تکی (پاپ‌آپِ ردیابیِ ست) کوچیک‌تر شد.
- «این تجهیزات رو ندارم» دیگه فوری به یک حرکتِ رندوم سوییچ نمی‌کنه — یه پاپ‌آپ با سه حرکتِ جایگزینِ آماده (از کاتالوگِ حرکات، بر اساسِ الگوی حرکتی/گروهِ عضلانیِ مشترک) نشون می‌ده تا خودت انتخاب کنی.

## نسخه: v1.35.0 (جدولِ واقعیِ برنامه، تایمرِ قابلِ‌پاز و پایدار، نوتیف، گیتِ ورود)

### چی تغییر کرد
- مشخصاتِ هر حرکت (ستَ/تکرار یا زمان) دیگه زیرِ اسمِ حرکت نوشته نمی‌شه — یک جدولِ واقعیِ HTML با ستون‌های آیکون‌دار (دمبل برای ست، آیکونِ تکرار برای تکرار/زمان) و خطوطِ جداکننده‌ی افقی/عمودی، دقیقاً وقتی تمرین فعاله.
- تایمرِ پاپ‌آپِ ردیابیِ ست حالا قابلِ پازه (هم برای هر ستِ زمان‌محور هم برای استراحتِ بینِ ست‌ها) و وضعیتش (فازِ فعلی، پاز بودن یا نه، باقی‌مونده‌ی دقیق) توی localStorage با یک epoch ذخیره می‌شه — نه بستن/بازکردنِ پاپ‌آپ نه رفرشِ کاملِ صفحه پیشرفت رو از دست نمی‌ده.
- وقتی یک ست یا یک حرکتِ زمان‌محورِ تکی (مثلاً دویدنِ ۴۰دقیقه‌ای) تموم می‌شه، یک نوتیفِ مرورگری فایر می‌شه (اگه از قبل اجازه داده شده باشه).
- دکمه‌ی شروعِ تایمرِ حرکاتِ زمان‌محورِ تکی کوچیک‌تر شد (۵۲ به‌جای ۶۴ پیکسل) و یه ring نبضِ آرومِ بی‌نهایت گرفت؛ ورود/خروجِ دایره‌های ست هم اسپرینگِ پلکانی گرفتن، نه یهویی.
- بخشِ «توجه» (زیرِ برنامه‌ی تمرینی/کالری/ترید) حالا سبزه، نه قرمز — رنگِ ثابت (نه وابسته به تمِ روشن/تیره).
- کاربرِ مهمون (لاگین‌نکرده) وقتی میره سراغِ بخشِ ورزش یا ترید، به‌جای یه متنِ ساده، یک گیتِ کاملِ بصری می‌بینه: پیش‌نمایشِ تارِ کلِ بخش + پیامِ «برای استفاده از این سرویس وارد شوید» + دکمه‌ی «وارد شوید» وسطِ صفحه — عیناً هم‌زبانِ بصریِ دیوارِ اشتراک (ModuleGate)، فقط برای «وارد نشدن» نه «اشتراک نداشتن». بعدِ ورود، چکِ اشتراکِ واقعی هنوز سرِ جاشه.

## نسخه: v1.34.0 (ریدیزاینِ فلوِ تکمیلِ حرکات — دکمه‌ی شروع، تایمرِ حرکاتِ زمان‌محور، جدولِ مشخصات)

### چی تغییر کرد
- وقتی «شروع تمرین» می‌زنی، صفحه با یک انیمیشنِ نرم (نه یهویی) کاملاً روی برنامه‌ی تمرینی متمرکز می‌شه — کارت‌های کناری و برنامه‌ی هفتگیِ پایینِ صفحه هم موقتاً محو می‌شن، نه فقط کارت‌های کناری.
- چک‌باکسِ دستیِ هر حرکت با یک دکمه‌ی «شروع» جایگزین شد؛ تنها راهِ تکمیلِ حرکت، همین دکمه‌ست که پاپ‌آپِ ردیابیِ ست‌به‌ست رو باز می‌کنه (کلیک روی اسمِ حرکت دیگه کاری نمی‌کنه).
- حرکاتِ زمان‌محور (پلانک، کاردیو) توی پاپ‌آپِ ردیابی به‌جای تیکِ دستی یه شمارش‌معکوسِ واقعی دارن — هم برای هر ستِ جداگانه (مثلاً پلانکِ ۳×۳۰ثانیه)، هم برای حرکاتِ تک‌زمانی (مثلاً دویدنِ ۲۵دقیقه‌ای) با یه دکمه‌ی پلی و کرنومتر.
- وقتی تمرین فعاله، زیرِ اسمِ هر حرکت یه جدولِ کوچیک تعدادِ ست و تکرار/زمانِ هر ست رو نشون می‌ده.
- تایمرِ زمانِ تمرین از یه باکسِ جدا زیرِ هدر، به جای دکمه‌ی «افزودن برنامه» (بالای کارت) منتقل شد و شکلِ کرنومتری گرفت.
- باگِ اصلیِ بلورنشدنِ پاپ‌آپِ ردیابیِ ست پیدا و فیکس شد: چون این پاپ‌آپ داخلِ یه `motion.div` (که همیشه یه `transform` غیرِخالی روش می‌مونه) رندر می‌شد، `backdrop-filter`ش هیچی برای بلورکردن پشتِ خودش پیدا نمی‌کرد — با پورتال‌کردنِ این پاپ‌آپ (و آموزشِ اولیه‌ش) به `document.body` حل شد.
- باگِ sync نشدنِ وضعیتِ «فعال/حرکاتِ تیک‌خورده» بعدِ رفرشِ صفحه (وقتی تمرین نصفه‌کاره رها شده بود) فیکس شد — یه dependency جا‌مونده تو یه useEffect بود.
- باگِ گلیچ/فلاشِ اسکرول‌بار موقعِ تعاملِ لیستِ حرکات با رزروکردنِ فضای اسکرول‌بار فیکس شد؛ سایه‌ی دورِ چک‌باکس/بجِ تکمیل‌شده هم دیگه از لبه‌ی چپِ لیست بریده نمی‌شه.
- آموزشِ اولیه‌ی «روشِ ردیابیِ ست‌به‌ست»: توی موبایل/تبلت با کشیدنِ انگشت بینِ اسلایدها رد می‌شی (بدونِ دکمه‌ی قبلی)، فقط اسلایدِ آخر دکمه‌ی «شروع برنامه» داره؛ توی دسکتاپ دکمه‌ی «بعدی» بزرگ‌تر پایین‌چپ نشسته و «قبلی» کوچیک‌تر شده.

## نسخه: v1.33.0 (فیکسِ باگِ روزهای برنامه‌ی تمرینی + بلورِ پنلِ پروفایل/اعلان‌ها)

### چی تغییر کرد
- باگِ روزهای برنامه‌ی تمرینی فیکس شد: قالبِ ایستای برنامه (وقتی بدونِ کلید API از AI ساخته می‌شه) دیگه روزهای هاردکدشده رو نادیده نمی‌گیره و روزهای واقعیِ انتخاب‌شده‌ی کاربر (مثلاً جمعه) رو به‌ترتیبِ هفته توی برنامه‌ی نهایی می‌ذاره؛ کنارِ انتخابِ روزها، حداقلِ تعدادِ روزِ لازم برای هر ترکیبِ هدف/سطح هم نشون داده می‌شه و فرم قبل از ثبت چکش می‌کنه.
- باگِ اصلیِ بلورنشدنِ پنلِ پروفایل و اعلان‌ها پیدا و فیکس شد: `transform` روی `.topbar-actions` یه stacking context جدید می‌ساخت که باعث می‌شد `backdrop-filter` این دو پنل چیزی برای بلورکردن پشتِ خودش پیدا نکنه؛ با جایگزینیِ اون transform با تکنیکِ `top:0;bottom:0;margin:auto 0` (بدونِ transform)، شیشه‌ی هر دو پنل درست رندر می‌شه.
- دکمه‌ی «مشاهده نحوه انجام» از ردیفِ هر حرکتِ برنامه‌ی تمرینی حذف شد.
- کرسرِ موس روی چک‌باکسِ غیرفعالِ حرکات دیگه «not-allowed» نمی‌شه.
- ستاره‌ی کوچیکِ آیکونِ «ساخت با هوش مصنوعی» از بالا‌چپ به بالا‌راست منتقل شد.
- توی «مشاهده حرکات»، به‌جای پیکتوگرامِ آدمکی، یه جای‌گیرِ ساده گذاشته شد تا بعداً عکسِ واقعیِ هر حرکت جایگزینش بشه.
- پاپ‌آپِ عمومیِ `.modal-panel` (پنلِ کاربری، انتخابِ تاریخ، کاتالوگِ حرکات و…) الان با یک اسکیل+فیدِ نرم‌تر باز می‌شه (هم‌حسِ پاپ‌آپ‌های افزودن/ویرایشِ برنامه) به‌جای فیدِ ساده‌ی قبلی.

## نسخه: v1.32.0 (حالتِ تمرینِ تمام‌صفحه + ردیابیِ ست‌به‌ستِ زنده با استراحت)

### چی تغییر کرد
- با زدنِ «شروع تمرین»، صفحه کاملاً روی «برنامه تمرینی» متمرکز می‌شه (تمام‌عرض، بدونِ کارت‌های کناری)، با یک تایمرِ بزرگِ زمانِ تمرین بالای لیستِ حرکات.
- هر حرکت الان یه دکمه‌ی «مشاهده نحوه انجام» داره که مستقیم همون حرکت رو توی کاتالوگ باز می‌کنه.
- روشِ اولِ جدیدِ تکمیلِ حرکت: کلیک روی اسمِ حرکت یه پاپ‌آپ باز می‌کنه با دایره‌ی چک‌باکس به‌اندازه‌ی ست‌های همون حرکت (خودکار از متنِ «۵×۵» استخراج می‌شه)؛ با هر تیک، ۹۰ ثانیه شمارش‌معکوسِ زنده‌ی استراحت شروع می‌شه و تا تمومِ اون زمان ستِ بعدی قفله؛ بعدِ آخرین ست، کلِ زمانِ صرف‌شده برای اون حرکت نشون داده می‌شه. روشِ دوم (همون چک‌باکسِ سریعِ قبلی) هم هنوز کنارش هست.
- اولین‌باری که کسی «شروع تمرین» رو بزنه، یه آموزشِ مرحله‌ایِ انیمیشنی نشونش می‌ده که روشِ اول چطوری کار می‌کنه؛ بعدِ دیدن دیگه تکرار نمی‌شه.
- بک‌گراندِ هر دو پاپ‌آپِ جدید (آموزش و ردیابیِ ست) عیناً هم‌بک‌گراند/بلورِ هدرِ سایته.

## نسخه: v1.31.0 (ریدیزاینِ برنامه هفتگیِ بدنسازی — گریدِ کامل بدونِ تاگل)

### چی تغییر کرد
- بخشِ «برنامه هفتگی»ِ بدنسازی از آکاردئونِ تاگل‌شونده (که هربار فقط یک روز باز می‌شد) به یک گریدِ ۷تایی تغییر کرد — همه‌ی روزها از همون اول، هم‌زمان دیده می‌شن (بدونِ کلیک/باز‌کردن)، دقیقاً الگویی که اپ‌های واقعیِ فیتنس (مثلِ Hevy/FitnessGrid) برای نمایشِ برنامه‌ی هفتگی استفاده می‌کنن.
- هر کارتِ روز، اسمِ روز + فوکوسِ برنامه + لیستِ کاملِ حرکات رو نشون می‌ده؛ روزِ استراحت با یه حالتِ کم‌رنگ‌تر و متنِ «روزِ استراحت» مشخصه؛ امروز با بوردر/بجِ سبزِ اکسنت هایلایت می‌شه.
- کامپوننتِ قدیمی (`ExerciseWeekAccordion`) که دیگه استفاده نمی‌شد حذف و با `ExerciseWeekGrid` جایگزین شد.
- عرضِ این بخش هم از محدودیتِ ۶۶۰پیکسلیِ قدیمی (که از استایلِ روتین قرض گرفته شده بود) آزاد شد و الان با بقیه‌ی بخش‌های داشبوردِ بدنسازی (~۱۳۲۰پیکسل) هم‌عرضه.

## نسخه: v1.30.1 (جعبه‌ی واحدِ قفلِ روزهای آینده، حذفِ ثبتِ بیداری از روتین، حذفِ توپ‌های بکگراندِ پنلِ دوستان، پاکسازیِ کدِ بی‌مصرف)

### چی تغییر کرد
- روزهای آیندهِ برنامه‌ی تمرینی دیگه بجِ تکراری روی هر ردیف نداره — یک جعبه‌ی واحد زیرِ کلِ لیست، با متنِ آبی‌رنگِ «وقتش نرسیده!» و بک‌گراند/بلورِ عیناً هم‌هدر.
- دکمه/بنرِ «ثبت بیداری الان» که این‌روزها به صفحه‌ی «روتین من» اضافه شده بود، کلاً حذف شد.
- توپ‌های شناورِ پس‌زمینه‌ی پاپ‌آپِ «دوستان» (LiquidBlobLayers) هم توی روتین هم توی بدنسازی حذف شدن — چون از یک کامپوننتِ مشترک میان، یک تغییر هر دو رو پوشش داد.
- کدِ بی‌مصرف پاکسازی شد: تابعِ `findCatalogEntry` و `normalizeExerciseName` توی `lib/exerciseCatalog.ts` (هیچ‌جا صدا زده نمی‌شدن) و propِ `initialName` توی `ExerciseCatalogModal` حذف شدن.

## نسخه: v1.30.0 (آیکونِ AI دو-ستاره‌ی توخالی، کاتالوگِ کاملِ حرکات با دیاگرامِ بدن، قفلِ روزهای آینده، لودینگِ سریع‌تر)

### چی تغییر کرد
- آیکونِ «ساخت با هوش مصنوعی» از یک ستاره‌ی توپرِ چرخان به دو ستاره‌ی چهارپَره‌ی توخالی (یکی بزرگ/اصلی پایین‌راست، یکی کوچیک بالا‌چپ — دقیقاً سبکِ جمنای) بدونِ انیمیشنِ داخلی تغییر کرد؛ فقط با هاورِ دکمه‌ی والد ری‌اکتیوه.
- بک‌گراندِ پاپ‌آپِ «دوستان» (هم توی بدنسازی هم روتین، چون از یک کامپوننتِ مشترک میان) عیناً هم‌بک‌گراند/بلورِ هدرِ سایت شد، به‌جای گرادیانِ تقریباً توپرِ قبلی.
- کاتالوگِ حرکاتِ بدنسازی (`lib/exerciseCatalog.ts`) به نزدیکِ دوبرابر بزرگ‌تر شد و همه‌ی حرکات با گروهِ عضلانیِ دقیق و الگوی حرکتی برچسب خوردن. «مشاهده حرکات» کامل بازطراحی شد: فیلترِ گروهِ عضلانی، پیکتوگرامِ انیمیشنیِ الگوی هر حرکت (`ExercisePictogram`)، و دیاگرامِ بدنِ جلو/پشت با هایلایتِ عضله‌های درگیر (`MuscleDiagram`) به‌جای متنِ خامِ قبلی.
- روزهای آینده‌ی برنامه‌ی تمرینی (که هنوز نرسیده) دیگه قابلِ تیک‌زدن/جایگزینی نیستن — چک‌باکس و دکمه‌ی جایگزینی تار می‌شن و روشون «وقتش نرسیده!» نوشته می‌شه؛ روزهای گذشته‌ای که حرکتش انجام نشده الان دقیقاً مثلِ ضربدرهای روتین (چک‌باکسِ قرمز با انیمیشنِ اسپرینگ) نشون داده می‌شن.
- زنجیره‌ی لودینگِ صفحه‌های پولی (ModuleGate → چکِ دسترسی → کامپوننتِ واقعی) قبلاً کاملاً سریالی بود و کامپوننتِ واقعی رو وسط‌کار un‌mount/remount می‌کرد (با یک پیامِ جداگانه‌ی «در حال بررسی دسترسی…»)؛ الان کامپوننتِ واقعی بلافاصله مونت می‌شه و فچِ خودش موازی با چکِ دسترسی اجرا می‌شه — enforcementِ واقعی هنوز کاملاً سمتِ سروره، این فقط UX رو سریع‌تر کرد.

## نسخه: v1.29.1 (برگردوندنِ اندازه‌ی حلقه‌های کارتِ آمارِ بدنسازی)

### چی تغییر کرد
- اندازه‌ی حلقه‌های درصدِ کارتِ آمارِ بدنسازی به همون اندازه‌ی قبل‌ترش (۶۰پیکسل/ضخامتِ ۵) برگشت — تغییرِ ۷۲پیکسلیِ نسخه‌ی قبل بی‌خیال شد.

## نسخه: v1.29.0 (فیکسِ چیدمانِ آکاردئونِ بدنسازی + آیکونِ AI + استریک + کنتراستِ حالتِ روز)

### چی تغییر کرد
- «برنامه هفتگی»ِ پایینِ صفحه‌ی بدنسازی از وسط به راست کشیده شده بود — چون داخلِ کانتینرِ `.bodybuilding-glass.exercise-dash-breakout` بود ولی خودش هم یه `dash-breakout` جدا داشت، یعنی فرمولِ مارجینِ breakout دوبار حساب می‌شد. حذفِ `dash-breakout`ِ اضافی حلش کرد.
- پاپ‌آپِ «افزودن برنامه»: تیترش از «افزودن برنامه ورزشی جدید» به «افزودن برنامه» کوتاه شد، دو دکمه‌ی انتخابِ مسیر (AI/دستی) بزرگ‌تر شدن، بینشون یه خطِ کم‌رنگ اضافه شد، و هر دو دکمه حالا با کلیک/هاور انیمیشنِ اسپرینگ دارن.
- آیکونِ هوش‌مصنوعی از آیکونِ ربات به یه ستاره‌ی ۴پر طلاییِ درخشان (سبکِ جمینای) با انیمیشنِ چرخش/پالسِ بی‌نهایت تغییر کرد (`AiSparkleIcon`).
- باگِ چشمک‌زدنِ صفحه‌ی onboarding سرِ هر ریلودِ صفحه‌ی بدنسازی: چون وضعیتِ `loading`ِ اولیه‌ی سشن مثلِ `unauthenticated` رفتار می‌کرد، هر بار قبل از این‌که سشنِ واقعی چک بشه یه لحظه فرمِ «بدونِ برنامه» به‌جای داشبورد نشون داده می‌شد.
- اندازه‌ی حلقه‌های درصدِ کارتِ آمارِ بدنسازی به همون اندازه‌ی حلقه‌های داشبوردِ روتین (۷۲پیکسل) برگشت تا کارت‌ها هم‌قد بشن.
- «برنامه تمرینی امروز» به «برنامه تمرینی» کوتاه شد؛ دکمه‌ی «افزودن برنامه» کنارِ همین تیتر جابه‌جا شد (هم‌الگوی دکمه‌ی «افزودن» توی روتین).
- آیکونِ زنگوله‌ی «یادآوری‌ها»: وقتی اطلاعیه‌اش فعاله زنگوله پره، وقتی غیرفعاله زنگوله توخالیه؛ روز و ساعتِ هر یادآوری هم حالا توی یک خط کنارِ هم نشون داده می‌شن.
- استریک هیچ‌وقت کار نمی‌کرد چون ثبتِ ساعتِ بیداری فقط از داخلِ `DayModal` ممکن بود، ولی `DayModal` این‌روزها فقط با انتخابِ تاریخ صدا زده می‌شه، نه برای ثبتِ بیداریِ امروز — یعنی هیچ‌کس هیچ‌وقت نمی‌تونست بیداریش رو ثبت کنه و استریک همیشه صفر می‌موند. یه دکمه‌ی «ثبت بیداری الان» مستقیماً توی صفحه‌ی «روتین من» اضافه شد.
- توی حالتِ روز (light mode)، کارت‌های داشبورد (`bg-dash-card`) از بک‌گراند تقریباً جدا نبودن چون اورلیِ سفیدشون فقط ۲٪ بود (مقداری که برای حالتِ تاریک درسته)؛ الان توی حالتِ روز این اورلی ۵۵٪ می‌شه، هم‌الگوی بقیه‌ی کارت‌های اپ.

## نسخه: v1.28.1 (فیکسِ عرضِ داشبوردِ بدنسازی + چیدمانِ سه‌بخشی + دکمه‌های بی‌بک‌گراند)

### چی تغییر کرد
- ریشه‌ی چندتا باگِ داشبوردِ بدنسازی پیدا شد: خودِ کانتینرِ صفحه (`.bodybuilding-glass`) یه عرضِ ثابتِ ۹۰۰پیکسلی از نسخه‌ی قدیمیِ فرم‌محور داشت، درحالی‌که داشبوردِ روتین با `.dash-breakout` تا ~۱۳۲۰پیکسل باز می‌شه — همین باعث می‌شد نوارِ روزها کمتر از روتین جا بگیره. الان فقط تبِ «بدنسازی» (نه کالری) عیناً هم‌عرضِ داشبوردِ روتینه.
- داشبوردِ بدنسازی داخلِ `.dash-scope` نبود (داشبوردِ روتین هست) — همین یعنی دکمه‌های Tailwindِ داشبورد (شروع/پایان تمرین، افزودنِ برنامه، ...) به‌جای بک‌گراندِ شفافِ داشبورد، بک‌گراندِ لیکوئیدگلسِ پیش‌فرضِ کلِ سایت رو می‌گرفتن. اضافه‌شدنِ `dash-scope` این‌و حل کرد؛ چندجای دیگه هم که خودم دستی بک‌گراند ست کرده بودم (دکمه‌ی شروع/پایان تمرین، دکمه‌های انتخابِ AI/دستی) به همون سبکِ بوردر-فقط بقیه‌ی دکمه‌های اصلیِ اپ برگشت.
- چیدمان به همون سه‌بخشیِ داشبوردِ روتین برگشت: راست «برنامه تمرینی امروز» (با دکمه‌ی «افزودن برنامه ورزشی جدید» همون ردیفِ تیتر)، وسط یک دکمه‌ی تکِ «مشاهده حرکات» (بدونِ پیش‌نمایشِ لیست)، چپ آمار+دوستان زیرِ هم.
- تبِ «بدنسازی/کالری» بالای صفحه کوچیک و راست‌چین شد؛ خطِ «سطح: ...» بالای داشبورد حذف شد.
- «شروع دوباره» بعدِ پایانِ تمرین حذف شد.
- اگه تعدادِ حرکاتِ امروز از جای باکس بیشتر بشه، خودِ لیست اسکرول می‌شه (اسکرول‌بارِ نازک بدونِ بک‌گراند، هم‌رنگِ تم) و باکس کش نمیاد؛ دکمه‌ی شروع/پایانِ تمرین همیشه چسبیده به تهِ باکس می‌مونه.

### چی تغییر کرد
- بخشِ «بدنسازی» کاملاً بازطراحی شد تا دقیقاً هم‌ساختار داشبوردِ «روتین من» باشه: همون نوارِ روزها (امروز/تاریخچه)، همون کارتِ «دوستان» (نسخه‌ی مخصوصِ بدنسازی، فقط پیشرفتِ همین ماژول رو نشون می‌ده)، و همون الگوی کارت‌ها/دکمه‌ها.
- کارتِ جدیدِ «تعداد جلسات» بالای صفحه: چند جلسه از این هفته رفتی/مونده، به‌همراه دو حلقه‌ی درصدِ پیشرفتِ امروز و هفتگی.
- کارتِ «وضعیتِ امروز»: استریکِ روزهای متوالیِ تمرین (فقط روزهای واقعیِ باشگاهِ برنامه حساب می‌شه) + پیغامِ «امروز هنوز تمرین نکردی» وقتی امروز روزِ باشگاهه و هنوز چیزی ثبت نشده.
- «برنامه تمرینی امروز»: با دکمه‌ی «شروع تمرین» یک تایمر می‌افته، حرکت‌ها رو می‌تونی حین تمرین تیک بزنی، و با «پایان تمرین» جلسه ثبت می‌شه — هر حرکتی که تیک نخورده باشه یک ضربدرِ قرمز می‌گیره (دقیقاً منطقِ «برنامه‌ی گذشته‌ی تیک‌نخورده»ی روتین).
- «مشاهده حرکات»: کاتالوگِ کاملِ حرکاتِ بدنسازی با جستجو؛ کلیک روی هرکدوم نحوه‌ی انجام، عضلاتِ درگیر و مزایاش رو نشون می‌ده.
- «افزودن برنامه ورزشی جدید» حالا دو مسیر داره: ساختِ خودکار با هوش‌مصنوعی (فرمِ قبلی) یا واردکردنِ دستیِ برنامه‌ی شخصیِ خودِ کاربر (روز‌به‌روز، بدونِ پرسش‌نامه‌ی سطح/هدف).
- دکمه‌ی چرخشیِ کنارِ هر حرکت («این تجهیزات رو ندارم») حالا آیکونه، نه دکمه‌ی متنی — با همون منطقِ جایگزینیِ قبلی.
- «برنامه هفتگی» پایینِ صفحه هم به همون آکاردئونِ باز/بسته‌شونده‌ی روتین (week-day) تبدیل شد.
- زیرساخت: `ExerciseLog` حالا حرکاتِ تیک‌خورده‌ی هر جلسه رو هم ذخیره می‌کنه (`completedItems`)، و `/api/friends` می‌تونه پیشرفتِ بدنسازیِ دوستان رو هم برگردونه (`?module=exercise`).

### چی تغییر کرد
- بک‌گراندِ پاپ‌آپ‌های افزودن/ویرایش/انتقالِ برنامه، و پنل‌های پروفایل/اطلاعیه‌های هدر، الان دقیقاً هم‌رنگِ خودِ هدر (شیشه‌ای/نیمه‌شفاف) هستن — قبلاً یه گرادیانِ تیره‌ی جدا داشتن.
- دکمه‌ی «میزان اهمیت» توی پاپ‌آپِ فیلتر کار نمی‌کرد؛ الان با انتخابِ هر سطح، لیستِ چک‌باکسِ برنامه‌ها فقط برنامه‌هایی که همون سطحِ اهمیت رو دارن نشون می‌ده، و یه دکمه‌ی تیک برای اعمالِ نهایی اضافه شد.
- از روزهای گذشته دیگه نمی‌شه برنامه حذف کرد (مثلِ منطقِ «انتقال» که قبلاً بلاک شده بود) — هم توی منوی سه‌نقطه‌ی «برنامه‌های امروز» هم توی تابعِ حذفِ خودِ صفحه.
- باگِ سه‌نقطه‌ی «برنامه‌های امروز» فیکس شد: هاور روی هر ردیف باعث می‌شد ردیفِ پایینی بپره بالا (به‌خاطرِ یه انیمیشنِ `y: -2` روی هاور که لازم نبود).
- توی پاپ‌آپِ افزودن/ویرایشِ برنامه، دکمه‌ی دایره‌ایِ «−» برای حذفِ ردیف با یه دکمه‌ی متنیِ قرمزِ «حذف این روز» جایگزین شد، با جایگذاریِ درست‌تر (زیرِ فیلدهای ساعت).
- وقتی هرکدوم از پاپ‌آپ‌های هدر (پروفایل/اطلاعیه‌ها) باز بود، کلیک روی بقیه‌ی صفحه نمی‌بستشون — الان با کلیک روی هرجای صفحه بسته می‌شن (همون راه‌حلی که برای منوی سه‌نقطه قبلاً جواب داده بود).
- برنامه‌ای که ساعتش گذشته و تیک نخورده، الان به‌جای دایره‌ی خالی یه ضربدرِ قرمز با پس‌زمینه‌ی قرمز و انیمیشنِ پالس نشون می‌ده.
- ستونِ درصدِ سمتِ چپِ نمودارِ «آمار هفتگی» (۱۰۰٪/۵۰٪/۰٪) حذف شد.

### چی تغییر کرد
- منوی سه‌نقطه‌ی «برنامه‌های امروز» بک‌گراندش تقریباً شفاف بود (فقط ۲٪ سفید + backdrop-blur) — الان مثلِ بقیه‌ی منوهای شناورِ اپ یه بک‌گراندِ تقریباً توپر داره، همیشه خواناست.
- کارتِ «یادآوری‌ها»: هر آیتم الان سه خط جداست (اسم / روز / ساعتِ شروع‌وپایان)، بدون خطِ تیره بینِ روز و ساعت، و همه‌ی باکس‌ها حداقل‌ارتفاعِ یکسان دارن.
- توی «برنامه هفتگی»، آیتم‌هایی که ساعتشون خیلی نزدیکِ بیداری/خواب بود روی لیبلِ همون نقطه‌ی انتهایی overlap می‌کردن — فاصله‌ی امنِ لبه‌ها (EDGE_GAP) بیشتر شد.
- فیلترِ جست‌وجوی برنامه/تگ الان قبل از مقایسه، «ي»/«ك» عربی رو به «ی»/«ک» فارسی و نیم‌فاصله رو به فاصله نرمالایز می‌کنه (`normalizeFa` توی `lib/utils.ts`) — قبلاً اگه اسمِ برنامه با کیبوردِ عربی تایپ شده بود، جست‌وجو با کیبوردِ فارسی چیزی پیدا نمی‌کرد.
- تقویمِ انتخابِ تاریخ (`JalaliDatePicker`، همه‌جا از افزودنِ برنامه تا انتقال) دیگه از `<select>` بومیِ مرورگر برای ماه/سال استفاده نمی‌کنه — یه دراپ‌داونِ کاملاً سفارشی و هم‌رنگِ اپ جایگزینش شد که لیستِ ۱۰۰+ سالِ داخلش هم اسکرول‌بارِ خامِ مرورگر نداره.
- توی پاپ‌آپِ افزودنِ برنامه، بعضی تایتل‌ها (`<label>`های اصلی) به‌خاطرِ یه تای اسپسیفیسیتیِ CSS خاکستری می‌موندن درحالی‌که بقیه (تاریخ‌ها/ساعت‌ها) سبز بودن — الان همه یک‌دست سبزن. فاصله‌ی زیادِ بینِ لیبلِ «میزان اهمیت» و باکسِ زیرش هم اصلاح شد.
- هدر: کلیک روی پروفایل/زنگوله‌ی اعلان‌ها/همبرگر همزمان چندتاشون رو باز نگه می‌داشت — الان باز کردنِ هرکدوم اون دوتای دیگه رو می‌بنده. پنلِ پروفایل هم چون خیلی نزدیکِ لبه‌ی راستِ صفحه بود و وسط‌چین می‌شد، گاهی نیمه‌ش از صفحه می‌زد بیرون — حالا از لبه‌ی راستِ دکمه‌ی خودش باز می‌شه به‌جای وسط‌چین‌شدن، همیشه داخلِ صفحه می‌مونه.

## نسخه: v1.25.6 (اصلاحِ منطقِ انتقال + دکمه‌ی حذفِ ردیف + یادآوری‌های نزدیک)

### چی تغییر کرد
- کارتِ «یادآوری‌ها» الان فقط برنامه‌های «امروز» و «فردا» رو نشون می‌ده (قبلاً کلِ هفته بود) — واقعاً یادآوریِ چیزیه که نزدیکه، نه هرچی توی هفته با تگِ اهمیتِ بالا ثبت شده. اگه تعداد زیاد بشه، کارت خودش اسکرول می‌شه (بدون اسکرول‌بارِ قابل‌دیدن)، دیگه تا ته صفحه کش نمیاد.
- باگِ دکمه‌ی «−» توی افزودن/ویرایشِ برنامه فیکس شد: قبلاً وقتی دو ردیف بود، دکمه‌ی حذفِ ردیفِ اول دقیقاً روی مرزِ بینِ دو ردیف می‌نشست و معلوم نبود مالِ کدومه («حسِ دوتا دکمه‌ی منفی»)؛ الان کنارِ خودِ day-picker همون ردیفه، همیشه واضحه کدوم ردیف رو حذف می‌کنه.
- «روز مقصد» توی پاپ‌آپِ انتقال شد «انتقال به روز:».
- باگِ تقویمِ انتقال فیکس شد: قبلاً اگه مثلاً برنامه دوشنبه بود، همه‌ی شنبه/یکشنبه‌های تقویم (حتی هفته‌های آینده) غیرفعال می‌شدن؛ الان فقط تاریخ‌هایی که واقعاً از امروز گذشتن غیرفعالن — چون برنامه‌ها هفتگی تکرار می‌شن، شنبه/یکشنبه‌ی هفته‌ی بعد کاملاً معتبره.
- دکمه‌ی ثبتِ پاپ‌آپِ انتقال هم مثلِ افزودن/ویرایش، چپ‌چین شد.
- تیکِ «ساعتش هم تغییر کنه» حذف شد — فیلدهای ساعتِ شروع/پایان همیشه توی پاپ‌آپِ انتقال هستن، پیش‌فرض همون ساعتِ فعلیِ برنامه، کاربر آزاد که تغییرشون بده یا نه.
- بک‌گراندِ پشتِ پاپ‌آپ‌های افزودن/ویرایش/انتقالِ برنامه بلورِ بیشتر و تیره‌تر گرفت (حسِ مات‌تر).

## نسخه: v1.25.5 (پرداختِ ریزه‌کاری‌هایِ پاپ‌آپِ افزودن/انتقالِ برنامه)

### چی تغییر کرد
- پاپ‌آپِ «انتقال به یک روز دیگر»: دیگه نمی‌شه به روزی زودتر از روزِ فعلیِ خودِ برنامه منتقل کرد (مثلاً از دوشنبه به یکشنبه یا شنبه) — چون برنامه‌ها هفتگی تکرار می‌شن، «زودتر» یعنی موقعیتِ روز توی ترتیبِ هفته (شنبه..جمعه)، نه فقط گذشته‌بودنِ تاریخِ تقویمی. روزهای غیرمجاز توی خودِ تقویم خاکستری/غیرفعالن، هم موقعِ ثبت دوباره چک می‌شه.
- ساعتِ برنامه‌ها توی کارتِ «یادآوری‌ها» با عددِ انگلیسی نمایش داده می‌شه (قبلاً فارسی بود، هماهنگ نبود با بقیه‌ی جاهای اپ).
- دکمه‌ی ثبت (تیک) توی پاپ‌آپِ افزودن/ویرایشِ برنامه از دکمه‌ی «افزودن روز دیگر» جدا شد — «افزودن روز دیگر» سمتِ راست می‌مونه، دکمه‌ی ثبت با یه فاصله (`space-between`) میره سمتِ چپِ همون ردیف.
- خودِ باکسِ پاپ‌آپ کمی بزرگ‌تر شد (تا معمولاً همه‌چیز بدونِ اسکرول جا بشه) و اسکرول‌بارش کامل مخفی شد (لمس/درگ همچنان اسکرول می‌کنه، برای حالتِ چندتا ردیفِ روز).
- وقتی «تاریخ شروع دوره» انتخاب می‌شه و تقویم خودکار می‌ره روی «تاریخ پایان دوره»، الان یه عنوانِ سبز بالای تقویم نشون می‌ده کدوم فیلد رو داره پر می‌کنی — قبلاً این جابه‌جایی کاملاً بی‌صدا بود و کاربر متوجهش نمی‌شد.
- جلوی «تاریخ شروع دوره» و «تاریخ پایان دوره» نوشته شد «(اختیاری)» — مثلِ فیلدِ تگ.
- کامپوننتِ `JalaliDatePicker` دو پراپِ جدید گرفت: `disableWeekdays` (غیرفعال‌کردنِ یه‌سری روزِ هفته توی همه‌ی ماه‌ها) و `title` (عنوانِ بالای تقویم).

## نسخه: v1.25.4 (بلاکِ ثبت/ویرایش در گذشته + تقویمِ واقعی برای انتقال)

### چی تغییر کرد
- افزودن/ویرایشِ برنامه هم مثلِ انتقال، برای ساعتی که امروز از قبل گذشته بلاک می‌شه (پیام خطا: «این ساعت برای امروز گذشته — نمی‌شه براش برنامه ثبت کرد»)؛ چون برنامه‌ها هفتگی/تکرارشونده‌ان، این فقط وقتی معنی داره که روزِ انتخاب‌شده دقیقاً امروزه.
- پاپ‌آپِ «انتقال به یک روز دیگر» به‌جای پیل‌های هفته (شنبه..جمعه)، حالا یه تقویمِ واقعیِ جلالی نشون می‌ده (`JalaliDatePicker`، همون کامپوننتِ تاریخِ شروع/پایانِ دوره) — روزهای گذشته توش غیرفعالن. اگه روزِ انتخاب‌شده خودِ امروز باشه و ساعتِ مؤثر (ویرایش‌شده یا همون قبلی) گذشته باشه، همون‌جا با پیامِ خطا بلاک می‌شه.
- `JalaliDatePicker` یه پراپِ جدید `disablePast` گرفت (مکملِ `disableFuture`ی قبلی) تا بشه روزهای قبل از امروز رو غیرفعال کرد.

## نسخه: v1.25.3 (قابلِ‌سلکت‌شدنِ متنِ داخلِ فیلدها)

### چی تغییر کرد
- قانونِ سراسریِ `user-select:none` (برای حسِ اپ‌نیتیو/جلوگیری از سلکت‌شدنِ تصادفیِ متن) رو خودِ فیلدهای فرم (`input`/`textarea`/`contenteditable`) هم می‌گرفت — یعنی نه متنِ تایپ‌شده قابلِ سلکت بود نه قابلِ کپی، هیچ‌جای اپ. حالا همه‌ی فیلدها (اسمِ برنامه، تگ، ساعت‌ها، جستجوها، هر ورودیِ متنیِ دیگه‌ای) صریحاً `user-select:text` گرفتن، با رنگِ سلکت‌شدنِ قابل‌مشاهده (به‌جای شفافِ سراسری). بقیه‌ی صفحه (متن‌ها، دکمه‌ها، کارت‌ها) همچنان غیرقابل‌سلکته.

## نسخه: v1.25.2 (بلاکِ انتقالِ برنامه‌های گذشته)

### چی تغییر کرد
- «انتقال به یک روز دیگر» فقط برای برنامه‌هایی فعاله که هنوز اتفاق نیفتادن — اگه روزش گذشته باشه، یا امروز باشه ولی ساعتِ پایانش رد شده باشه، گزینه‌ی انتقال توی منوی سه‌نقطه غیرفعال (خاکستری، غیرقابل‌کلیک) می‌شه؛ ویرایش و حذف همچنان روی همون برنامه‌ی گذشته کار می‌کنن، فقط انتقال بلاک می‌شه. تشخیصِ «گذشته» با مقایسه‌ی تاریخِ واقعیِ روزِ انتخاب‌شده (`selectedIso`) و ساعتِ پایانِ برنامه با لحظه‌ی الان انجام می‌شه.

## نسخه: v1.25.1 (فیکسِ ارورِ هیدریشن + حذفِ توپ‌های بلاب از پاپ‌آپ‌ها + اسکرول‌بارها)

### چی تغییر کرد
- ارورِ هیدریشنِ `Did not expect server HTML to contain a <h2> in <div>` توی `DashReminderCard` فیکس شد: `typeof document !== "undefined"` برای گیت‌کردنِ پورتالِ toast، حتی توی رندرِ اولِ کلاینت (قبل از mount) هم `true` می‌شه چون `document` توی مرورگر همیشه وجود داره — درحالی‌که سمتِ سرور `false`ه. با یه state که فقط توی `useEffect` (بعدِ هیدریشن) `true` می‌شه جایگزین شد تا رندرِ اولِ سرور/کلاینت یکی بمونه.
- توپ‌های لیکوئید بلاب از پاپ‌آپ‌های افزودن/ویرایش/انتقالِ برنامه کامل حذف شدن — فقط روی کارتِ «نمایش درس» (پشتِ کارتِ برنامه) باقی موندن. بک‌گراندِ این پاپ‌آپ‌ها هم به حالتِ ساده/تخته برگشت.
- اسکرول‌بارِ افقیِ داخلِ «برنامه هفتگی» (`.week-timeline`) کامل مخفی شد (اسکرولِ لمسی/درگ همچنان کار می‌کنه).
- اسکرول‌بارِ کلیِ صفحه (سمتِ راستِ مرورگر) برای دارک‌مود تیره شد (و برای لایت‌مود هم متناسب با پس‌زمینه‌ی روشن)، به‌جای اسکرول‌بارِ پیش‌فرضِ سفیدِ مرورگر.

## نسخه: v1.25.0 (بازطراحیِ پاپ‌آپ‌های افزودن/ویرایش/انتقالِ برنامه)

### چی تغییر کرد
- پاپ‌آپِ «افزودن برنامه جدید»: بلورِ پشتِ پاپ‌آپ خیلی بیشتر شد و توپ‌های لیکوئید بلاب توی همین پاپ‌آپ (و ویرایش/انتقال) دیگه تکون نمی‌خورن — ثابت می‌مونن تا حواسِ کاربر موقعِ پرکردنِ فیلدها پرت نشه (بقیه‌ی جاهایی که از همین بلاب استفاده می‌کنن، مثلِ پشتِ کارتِ برنامه، دست‌نخورده و همچنان متحرکه).
- باگِ راست‌چینیِ ردیفِ «افزودن روز دیگر»/دکمه‌ی ثبت فیکس شد: چون `direction` صفحه rtlـه، `justify-content:flex-end` روی این ردیف‌ها عملاً چپ‌چین می‌کرد نه راست؛ با `flex-start` درست شد. دکمه‌ی تیکِ ثبت هم به همون ردیفِ «افزودن روز دیگر» منتقل شد (به‌جای یه ردیفِ جدا و پایین‌تر) تا بالاتر و کنارِ هم باشن.
- دورِ دایره‌ایِ نقطه‌چینِ اطرافِ آیکونِ + حذف شد — فقط خودِ علامتِ + بدون قاب.
- پاپ‌آپِ «ویرایش برنامه» کامل بازسازی شد تا دقیقاً همون دیزاینِ «افزودن برنامه جدید» رو داشته باشه (همون بک‌گراند، همون منطقِ ردیف‌ها) و همین باعث شد باگِ «توی ویرایش نمی‌شه چند روز رو هم‌زمان انتخاب کرد» هم حل بشه — حالا هر ردیفِ ویرایش هم مثلِ افزودن، چندروزه‌ست و دکمه‌ی «افزودن روز دیگر» داره.
- گزینه‌ی جدید «انتقال به یک روز دیگر» به منویِ سه‌نقطه‌ی هر برنامه اضافه شد؛ با یه پاپ‌آپِ هم‌دیزاین (`MoveOccurrenceModal`) که روزِ مقصد رو می‌گیره و یه تیکِ اختیاری داره برای اینکه ساعتِ برنامه هم توی روزِ جدید عوض بشه یا نه.
- فیلدِ «اسم درس» توی افزودنِ برنامه به «اسم برنامه» تغییر نام کرد، یه مثالِ placeholder (بدون کلمه‌ی «مثلاً») بهش اضافه شد، و کلمه‌ی «مثلاً» از placeholder فیلدِ تگ حذف شد.
- تمامِ لیبل‌های کوچیکِ این پاپ‌آپ‌ها (اسم برنامه، تگ، روز، ساعت شروع/پایان و...) سبز شدن (رنگِ برندِ اپ)، و رنگِ متنِ مثال‌های داخلِ فیلدها (placeholder) که قبلاً خیلی کم‌کنتراست و عملاً نامرئی بود، خواناتر شد.
- توی این پاپ‌آپ‌ها، زدنِ اینتر روی یه فیلد حالا فوکوس رو می‌بره فیلدِ بعدی (مثلِ Tab) — دیگه لازم نیست کاربر دونه‌دونه روی هر فیلد کلیک کنه.

## نسخه: v1.24.0 (بستنِ پاپ‌آپِ سه‌نقطه با کلیکِ هرجا + اصلاحِ انتخابِ روز از تاریخچه)

### چی تغییر کرد
- پاپ‌آپِ سه‌نقطه‌ی کنارِ اسمِ برنامه (ویرایش/حذف) قبلاً فقط با کلیکِ داخلِ همون کارت بسته می‌شد، نه کلِ صفحه — چون `backdrop-blur` روی کارتِ والد یه containing-block جدید برای `position:fixed` می‌سازه و لایه‌ی «کلیک بیرون برای بستن» رو محدود به همون کارت می‌کرد. حالا با یه لیسنرِ سطحِ document بسته می‌شه، پس کلیک هرجای صفحه می‌بندتش.
- انتخابِ یه روز از تقویمِ گریدیِ «تاریخچه» فقط محتوای «برنامه‌های امروز» رو عوض می‌کرد، ولی نوارِ روزهای بالای صفحه (که پنجره‌ی نمایشش صرفاً بر اساسِ ناوبریِ فلش‌ها حساب می‌شد، نه روزِ انتخاب‌شده) نه اون روز رو نشون می‌داد نه به‌عنوانِ فعال هایلایتش می‌کرد. حالا با انتخاب از تاریخچه، پنجره‌ی نوار هم دورِ همون روز وسط‌چین می‌شه و به‌عنوانِ فعال دیده می‌شه.

## نسخه: v1.23.2 (رفعِ کرشِ صفحه‌ی ترید موقعِ خطای شبکه)

### چی تغییر کرد
- `loadMonth` توی ژورنالِ ترید و `load` توی چک‌لیستِ ترید، بعد از گرفتنِ ریسپانس مستقیم `res.json()` صدا می‌زدن بدون هیچ مدیریتِ خطایی — اگه بدنه‌ی ریسپانس به هر دلیلی (مثلاً یه ریکوئستِ قبلی که هنوز درحالِ اجرا بود و درخواستِ جدید جایگزینش شد) خالی یا ناقص می‌رسید، کل صفحه با یه خطای رانتایمِ مدیریت‌نشده کرش می‌کرد. الان هر دو تو `try/catch` هستن، وضعیتِ `res.ok` هم چک می‌شه، و `loading` همیشه (حتی موقعِ خطا) خاموش می‌شه.

## نسخه: v1.23.1 (حالتِ نئونی برای تایم‌لاینِ برنامه هفتگی)

### چی تغییر کرد
- نوارِ پرشونده، دایره‌های برنامه‌ها، و برچسبِ بیداری/خواب توی تایم‌لاینِ «برنامه هفتگی» یه هاله‌ی نئونیِ ثابت (بدون انیمیشن، فقط `box-shadow`/`text-shadow` با رنگِ `--accent`) گرفتن — دایره‌های انجام‌شده هاله‌ی قوی‌تری دارن. توی تمِ تیره (سبز) کاملاً حسِ نئونی می‌ده، توی تمِ روشن (نارنجی/گرم) به‌طورِ طبیعی ملایم‌تره چون پس‌زمینه روشنه.

## نسخه: v1.23.0 (حذفِ بافتِ متحرکِ راه‌راهِ نوارِ پرشونده‌ی تایم‌لاین)

### چی تغییر کرد
- نوارِ پرشونده‌ی تایم‌لاینِ «برنامه هفتگی» یه بافتِ راه‌راهِ سفید/سبزِ متحرک روش بود (انیمیشنِ بی‌پایان، حسِ دورنگ می‌داد) — کامل حذف شد. حالا نوار فقط یه رنگِ سبزِ ثابتِ ساده‌ست، بدون هیچ انیمیشنی.

## نسخه: v1.22.2 (بلورِ لبه‌ی نوارِ تاریخ فقط وقتی واقعاً چیزی داره خارج می‌شه)

### چی تغییر کرد
- بلورِ لبه‌های نوارِ تاریخ قبلاً همیشه و بدون قید‌وشرط فعال بود — حتی وقتی هیچ روزی واقعاً از صفحه خارج نبود، آخرین روزِ کاملاً دیده‌شده (درست کنارِ دکمه‌ی قبلی/بعدی) دائم تار به‌نظر می‌رسید و انگار خودِ دکمه هم توی بلور بود.
- حالا هر طرفِ بلور فقط وقتی نمایان می‌شه که نوار واقعاً توی همون جهت اسکرول‌شدنی باشه (بر اساسِ موقعیتِ واقعیِ اسکرول، نه همیشگی) — با اسکرول‌کردن به‌آرومی فِید می‌شه، در حالتِ عادی (که تقریباً همیشه دقیقاً هفت‌روز جا می‌شه) کاملاً محو و شفافه و دکمه‌ها و روزها همیشه واضح می‌مونن.

## نسخه: v1.22.1 (شفافیتِ کمترِ نوارِ تاریخ + کلیپ‌شدنِ درستِ گوشه‌هاش)

### چی تغییر کرد
- راندِ قبلی رو گنداشتم — منظورِ کاربر از «پنل تقویم» تقویمِ گریدیِ تاریخچه نبود، همون **نوارِ انتخابِ تاریخ** بالای صفحه‌ی هفتگی بود (روزها به‌صورت نواری کنارِ هم). این نوار بک‌گراندش فقط ۲٪ کدر بود (`bg-dash-card` سفیدِ تقریباً نامرئی) و چون `overflow` نداشت، بلور/گردیِ گوشه‌هاش درست کلیپ نمی‌شد.
- بک‌گراندش به یه رنگِ شیشه‌ایِ تم‌آگاهِ ۱۶٪ کدر (`rgba(var(--bg-rgb), .16)`) عوض شد — دیگه سفیدِ هاردکد نیست، توی هر دو تم درست جواب می‌ده.
- `overflow:hidden` اضافه شد تا محتوای اسکرول‌شونده و بلورِ پس‌زمینه دقیقاً به گوشه‌های گردِ نوار کلیپ بشه (این روش، برخلافِ مودال‌ها، نیازی به ترفندِ mask نداشت چون خودِ نوار اسکرولِ داخلی نداره).

## نسخه: v1.22.0 (شفافیتِ کمترِ پنلِ تقویم/مودال‌ها + اصلاحِ چند باگِ کراس-براوزر)

### چی تغییر کرد
- بک‌گراندِ شیشه‌ایِ مودال‌ها (که تقویمِ تاریخچه هم توش رندر می‌شه — همون کلاسِ `.modal-panel` بینِ همه‌ی مودال‌های سایت مشترکه) توی تمِ تیره خیلی شفاف بود (۴٪ کدری) و محتوای پشتش زیادی نمایان بود؛ حالا کدرترِ (۱۶٪) و شبیه‌تر به یه کارتِ شیشه‌ایِ واقعی شد.
- باگِ شناخته‌شده‌ی سافاری/فایرفاکس که گوشه‌های گردِ پنل‌های بلورشده رو درست کلیپ نمی‌کنن (بلورِ پس‌زمینه از زیرِ گوشه‌های گرد می‌زنه بیرون) روی پنلِ مودال‌ها و کشوی منو (nav-drawer) فیکس شد.
- بلورِ لیکوئید-گلسِ دورِ هدر (`liquidGlassFilter`) توی فایرفاکس اصلاً اعمال نمی‌شد (چون فایرفاکس `url()` رو داخلِ backdrop-filter پارس نمی‌کنه و کلِ declaration نامعتبر می‌شه) — یه فال‌بکِ ساده قبلش اضافه شد تا حداقل بلورِ پایه توی فایرفاکس هم بمونه.
- فلیپ‌کارتِ برنامه‌ها (باز کردنِ جزئیاتِ هر برنامه) پیشوندِ `-webkit-` رو برای `transform-style`/`perspective` نداشت که باعثِ می‌شد چرخشِ سه‌بعدی توی سافاری درست کار نکنه؛ اضافه شد.
- توجه: این محیط فقط کرومیوم داره، پس این فیکس‌ها بر اساسِ الگوهای شناخته‌شده و مستندِ باگ‌های سافاری/فایرفاکس اعمال شدن (نه تستِ زنده روی خودِ اون مرورگرها) — بهتره یه‌بار روی دستگاهِ واقعیِ آیفون/سافاری هم چک بشه.

## نسخه: v1.21.0 (حذفِ ردیفِ خط‌کشِ راه‌راه بالای تایم‌لاین)

### چی تغییر کرد
- بالای نوارِ تایم‌لاینِ «برنامه هفتگی» یک ردیف خط‌های ریزِ خط‌کش‌مانند بود که راه‌راه دیده می‌شد — حذف شد. حالا فقط همون یک خطِ توپرِ ساده‌ی زیرش می‌مونه که دایره‌های برنامه‌ها دقیقاً روش قرار می‌گیرن، هم توی دسکتاپ هم موبایل.

## نسخه: v1.20.1 (اصلاحِ هم‌راستاییِ دایره‌ها با خطِ تایم‌لاینِ موبایل)

### چی تغییر کرد
- توی نسخه‌ی موبایل، دایره‌های روی تایم‌لاین دقیقاً روی خطِ افقی نبودن (چند پیکسل بالاتر) چون ارتفاعِ ستونِ مارکر کوچیک‌تر شده بود ولی موقعیتِ خودِ خط ثابت مونده بود. حالا دقیقاً وسطِ خط قرار می‌گیرن — با اندازه‌گیریِ پیکسلیِ واقعی (نه فقط چشمی) تأیید شد، هم توی دسکتاپ هم موبایل.

## نسخه: v1.20.0 (تایم‌لاینِ موبایلِ برنامه هفتگی، دقیقاً افقی مثلِ دسکتاپ)

### چی تغییر کرد
- تایم‌لاینِ موبایلِ «برنامه هفتگی» دیگه چیدمانِ عمودیِ جدا نداره — دقیقاً همون ساختارِ افقیِ دسکتاپ رو داره (بیداری راست، خواب چپ، نقطه‌ها روی یه خطِ افقی، خط‌کشِ ریز بالای نوار)، فقط یه‌پله کوچیک‌تر شده (فونت، دایره، عرضِ تراک) تا توی موبایل جا بشه و در صورتِ نیاز با اسکرولِ افقی دیده بشه.

## نسخه: v1.19.0 (اصلاحِ رنگ/تیک/موبایلِ تایم‌لاینِ برنامه هفتگی)

### چی تغییر کرد
- رنگِ اختصاصیِ نارنجیِ راندِ قبلِ تایم‌لاین حذف شد — حالا از همون `--accent` عمومیِ سایت استفاده می‌کنه که خودش طبقِ سیستمِ رنگیِ دو-تمِ اپ درست عمل می‌کنه: تمِ تاریک سبز، تمِ روشن گرم/نارنجی.
- دایره‌ی وسطِ هر برنامه روی تایم‌لاین حالا واقعاً به تیک‌خوردنِ همون روز وصله — اگه برنامه انجام‌شده باشه یک تیکِ سبز/رنگیِ پر می‌گیره، وگرنه دایره‌ی خالی می‌مونه (قبلاً فقط بر اساسِ گذشتنِ زمان بود، نه تیک‌خوردنِ واقعی).
- تایمِ‌لاینِ موبایل دیگه اسکرولِ داخلیِ جداگانه نداره — کلِ روز یک‌جا و ثابت نمایش داده می‌شه، و اندازه‌ی همه‌چیز (فونت‌ها، دایره، فاصله‌ها) توی موبایل کوچیک‌تر شد.

## نسخه: v1.18.0 (بازطراحیِ نارنجیِ تایم‌لاینِ بیداری/خواب طبقِ طرحِ مرجع)

### چی تغییر کرد
- تایم‌لاینِ بیداری/خواب توی «برنامه هفتگی» رنگِ اختصاصیِ نارنجی گرفت (جدا از سبزِ عمومیِ سایت) — نوارِ پرشونده، دایره/ضربدرِ هر برنامه، و برچسب‌های «بیداری»/«خواب» — دقیقاً طبقِ طرحِ مرجعی که فرستاده شد.
- یک ردیف خط‌های ریزِ خط‌کش‌مانند بالای نوار اضافه شد.
- توی هر دو تمِ روشن و تاریک تست و تأیید شد. دکمه‌های ادیت و تاریخچه‌ی طرحِ مرجع طبقِ خواسته اضافه نشدن.

## نسخه: v1.17.0 (کوچیک‌ترکردنِ فونت‌ها/چک‌باکس توی نسخه موبایل)

### چی تغییر کرد
- فونت‌های بخشِ «روتین من» (هدر، نوارِ انتخابِ روز، ردیفِ برنامه‌ها، یادآوری‌ها، دوستان، آمار هفتگی، دکمه‌های فیلتر) توی نسخه‌ی موبایل یه‌پله کوچیک‌تر شدن — نسخه‌ی دسکتاپ دست‌نخورده موند.
- چک‌باکسِ تیک‌زدنِ برنامه‌ها و آیکون‌ها (سه‌نقطه، زنگوله، پلاسِ افزودن) هم توی موبایل کوچیک‌تر شدن.
- نتیجه: نوارِ انتخابِ روز حالا روزهای بیشتری توی همون عرض جا می‌ده، و اسمِ برنامه‌ها فضای بیشتری برای خوانده‌شدن دارن.

## نسخه: v1.16.0 (رفعِ چیدمانِ به‌هم‌ریخته‌ی ردیفِ برنامه‌های امروز)

### چی تغییر کرد
- ردیفِ هر برنامه توی «برنامه‌های امروز» دیگه توی عرض‌های باریک به‌هم نمی‌ریزه و روی هم نمی‌افته — تگ و بجِ اهمیت حالا عرضِ ثابت گرفتن، پس دیگه سه‌نقطه بالای اسمِ برنامه نمی‌ره و بینِ ردیف‌های مختلف هم دقیقاً زیرِ هم می‌ایستن (نه هرکدوم یه‌جا، بسته به طولِ متنِ بج/تگ).
- تگ همچنان سمتِ چپِ اسمِ برنامه‌ست.

## نسخه: v1.15.1 (استریکِ هر دوست، نه استریکِ خودمون)

### چی تغییر کرد
- استریکی که توی کارتِ «دوستان» نشون داده می‌شه اصلاح شد — قبلاً استریکِ خودِ کاربر بود، الان استریکِ خودِ هرکدوم از دوست‌هاست (هم توی لیستِ خلاصه‌شده هم توی پاپ‌آپِ «مشاهده همه»)، چون محاسبه‌ی استریکِ هر دوست باید سمتِ سرور انجام بشه (`/api/friends` حالا `streak` هم برمی‌گردونه).

## نسخه: v1.15.0 (استریک توی کارتِ دوستان، خاموش/روشن‌کردنِ یادآوری)

### چی تغییر کرد
- استریکِ روزهای پشت‌سرهم حالا توی کارتِ «دوستان» هم (کنارِ عنوان) نشون داده می‌شه، نه فقط توی هدر.
- کلیک روی زنگوله‌ی کارتِ «یادآوری‌ها» حالا واقعاً یادآوریِ اون برنامه رو روشن/خاموش می‌کنه — آیکون و رنگش هم با هر کلیک عوض می‌شه (زنگوله‌ی روشن ↔ زنگوله‌ی خط‌خورده) و پیامِ بالای صفحه هم متناسب باهاش تغییر می‌کنه.
- وقتی یادآوریِ یک برنامه خاموش باشه، دیگه نوتیفِ «۳۰ دقیقه مونده» یا «وقتشه» براش فرستاده نمی‌شه.

## نسخه: v1.14.1 (حذفِ پیکانِ رینگ، پرکردنِ نوارِ روزها، بلورِ لایه‌ای)

### چی تغییر کرد
- پیکانِ رینگِ پیشرفتِ امروز (که راندِ قبل اضافه شده بود) حذف شد.
- پیل‌های نوارِ انتخابِ روز حالا کاملاً عرضِ نوار رو پر می‌کنن (به‌جای این‌که یک فضای خالی توی یک طرف بمونه) — فاصله‌ی بینِ پیل‌ها یکنواخت پخش می‌شه.
- بلورِ لبه‌های نوارِ روزها لایه‌لایه شد (چند لایه با بلورِ فزاینده) تا حسِ واقعیِ «رفته‌رفته تارترشدن» بده، نه یک بلورِ یک‌دستِ ماسک‌شده.

## نسخه: v1.14.0 (جستجوی تگ، آپدیت زنده، پیکانِ رینگِ پیشرفت)

### چی تغییر کرد
- پاپ‌آپِ فیلتر حالا با تگِ برنامه‌ها هم جستجو/فیلتر می‌کنه، نه فقط اسمِ برنامه.
- منوی سه‌نقطه از کارتِ «یادآوری‌ها» حذف شد؛ کلیک روی زنگوله یعنی «نیم‌ساعت قبل از شروعِ این برنامه بهم اطلاع بده» — با یک پیامِ تأییدِ کوتاه بالای صفحه (و درخواستِ اجازه‌ی نوتیف مرورگر اگه هنوز گرفته نشده).
- با تیک‌زدنِ یک برنامه، رینگِ پیشرفتِ امروز و کارتِ «آمار هفتگی» بی‌درنگ آپدیت می‌شن — دیگه نیازی به رفرش صفحه نیست.
- رینگِ پیشرفتِ امروز یک پیکانِ مینیمال سرِ نقطه‌ی پیشرفتش گرفت.
- لبه‌های چپ/راستِ نوارِ انتخابِ روز به‌آرومی بلور می‌شن (نشونه‌ی این‌که محتوای بیشتری برای اسکرول هست).
- کلیک روی لوگوی Arion توی هدر حالا به صفحه‌ی اصلی می‌بره.

## نسخه: v1.13.0 (تگ برنامه، انیمیشن تیک، رفعِ بکگراندِ تمِ روشن)

### چی تغییر کرد
- بلور شدنِ ردیفِ برنامه بعد از تیک زدن حذف شد.
- نوارِ انتخابِ روز حالا به‌جای تعدادِ ثابت، هرچقدر عرضِ صفحه جا داشته باشه روز نشون می‌ده — همیشه با امروز وسطِ نوار.
- امکانِ اضافه‌کردنِ تگِ دلخواه به هر برنامه (فرمِ افزودن/ویرایش) — تگ سمتِ چپِ اسمِ برنامه نمایش داده می‌شه.
- تیک‌باکسِ برنامه‌ها بازطراحی شد و یک انیمیشنِ تیک‌خوردنِ نرم گرفت.
- باگِ ریشه‌ایِ بکگراندِ سفیدِ دکمه‌ها در تمِ روشن پیدا و رفع شد — تداخلِ specificity بینِ ریست عمومیِ `button` تمِ روشن و ریستِ `dash-scope` باعث می‌شد این باگ بعد از هر فیکسِ قبلی دوباره برگرده؛ حالا دکمه‌های افزودنِ برنامه/مشاهدهٔ همه/فلش‌های روز قبل-بعد/زنگِ نوتیفیکیشن توی هر دو تم بدونِ بکگراند هستن.
- بکگراندِ کارتِ برنامه (هنگامِ کلیک روی اسمِ درس) و فرمِ افزودنِ برنامه کاملاً تار/مات شد و گوی‌های پس‌زمینه دیگه حرکتِ محسوسی ندارن — دقیقاً هم‌رنگِ بکگراندِ اصلیِ سایت.
- ساعت‌ها همه‌جا (برنامه‌های امروز، یادآوری‌ها، برنامهٔ هفتگی، کارتِ برنامه) با اعدادِ انگلیسی نمایش داده می‌شن.
- گزینه‌های منوی سه‌نقطهٔ هر برنامه (ویرایش/حذف) حالا آیکونِ مخصوصِ خودشون رو دارن.
- توی کارتِ برنامه، روزِ هفته سمتِ راست و ساعتِ برگزاری سمتِ چپ نوشته می‌شه.

## نسخه: v1.12.1 (تنظیمِ عرضِ ستون‌های داشبورد)

### چی تغییر کرد
- عرضِ کلِ بخشِ «روتین من» توی دسکتاپ کمی بزرگ‌تر شد (از ۱۱۸۰ به ۱۳۲۰ پیکسل).
- نسبتِ ستون‌ها عوض شد: از عرضِ «یادآوری‌ها» کم شد، به «برنامه‌های امروز» اضافه شد.

### چی تغییر کرد
- چینشِ دسکتاپِ «روتین من» از حالتِ ردیف‌های تمام‌عرضِ زیرِ هم به یه گریدِ سه‌ستونیِ واقعی تغییر کرد — دقیقاً طبقِ طرحِ دستی‌ای که کاربر فرستاد: ستونِ راست (پهن‌تر) «برنامه‌های امروز» از بالا تا پایین، ستونِ وسط «یادآوری‌ها»، ستونِ چپ «دوستان» و «آمار هفتگی» زیرِ هم. موبایل/تبلت همچنان یک ستونِ عمودیِ ساده می‌مونه.

### چی تغییر کرد
- ردیفِ سوم «روتین من» (دوستان + آمار هفتگی) توی دسکتاپ هم کاملاً عمودی شد — دو باکسِ جدا زیرِ هم (اول دوستان بعد آمار)، نه کنارِ هم.

### چی تغییر کرد
- **باگِ واقعی پیدا و فیکس شد**: چندتا از مودال‌های خودِ داشبورد (فیلترِ برنامه‌ها، افزودن/ویرایشِ برنامه، تاریخچه، پاپ‌آپِ دوستان، اطلاعیه‌ها) بیرونِ ساختارِ `.dash-scope` رندر می‌شن (چه چون توی ریشه‌ی صفحه‌ن چه چون پورتال به `document.body` شدن)، پس دکمه‌هایی که فقط رنگ/بوردر رو inline ست می‌کردن (بدون کلاسِ اختصاصی — مثلِ دکمه‌ی ستاره‌ی فیوریت یا دکمه‌ی «همه»‌یِ فیلتر) قانونِ عمومیِ `button{}` سراسری رو می‌گرفتن و یه بک‌گراندِ ناخواسته پیدا می‌کردن؛ با اضافه‌کردنِ `dash-scope` روی خودِ این مودال‌ها فیکس شد.
- چینشِ دسکتاپِ «روتین من» برگشت به سه ردیف: ردیفِ اول برنامه‌های امروز، ردیفِ دوم یادآوری‌ها، ردیفِ سوم آمار هفتگی و دوستان کنارِ هم.
- بک‌گراندِ دکمه‌ی ستاره (فیوریت‌کردنِ دوست) پاک شد.

### قانونِ جدید (حتماً رعایت بشه)
- هیچ‌وقت خودسرانه به هیچ عنصری بک‌گراند اضافه نمی‌شه، مگر صریحاً خواسته بشه — جزئیاتش توی `CLAUDE.md` هست.

### چی تغییر کرد
- استریک توی هدر رفت سمتِ چپِ زنگوله‌ی اطلاعیه‌ها (قبلاً راستش بود).
- عنوانِ «روتین من» و حلقه‌ی پیشرفتِ امروز توی موبایل هم کنارِ هم می‌مونن، دیگه زیرِ هم نمی‌شکنن.
- بک‌گراندِ پاپ‌آپِ «دوستان» بازم تیره‌تر/کدرتر شد؛ فیلدِ جستجو حالا همیشه راست‌چینه (حتی اگه یوزرنیمِ لاتین تایپ کنی)، نه اینکه بسته‌به نوعِ متن چپ بشه.
- «لیست دوستان» فقط وقتی دوستی داری بالای لیست نوشته می‌شه — اگه دوستی نداری فقط همون پیامِ خالی نشون داده می‌شه. برچسبِ «افزودن دوست» هم سبز شد.
- نوارِ انتخابِ روز دیگه یک هفته‌ی کاملِ ثابت (شنبه تا جمعه) نیست — یه پنجره‌ی داینامیکِ ۵روزه‌ست که همیشه روی روزِ فعال وسط‌چینه (مثلاً اگه امروز جمعه‌ست: چهارشنبه/پنجشنبه/جمعه/شنبه/یکشنبه).

### چی تغییر کرد
- **باگِ واقعی پیدا و فیکس شد**: پاپ‌آپِ «پنل کاربری» و «اطلاعیه‌ها» توی موبایلِ واقعی می‌تونستن از صفحه بزنن بیرون — چون `vh` روی مرورگرهای موبایل نسبت‌به بزرگ‌ترین حالتِ ممکنِ ویوپورت (وقتی نوارِ آدرس مخفیه) حساب می‌شه، نه ارتفاعِ واقعاً دیده‌شده؛ با `dvh` (که نسبت‌به ویوپورتِ فعلی/واقعی حساب می‌شه) فیکس شد، مرورگرهای قدیمی‌تر هم fallback به همون `vh` قبلی دارن.
- **باگِ واقعیِ دیگه پیدا و فیکس شد**: دکمه‌های «فیلتر/امروز/تاریخچه» و روزهای انتخاب‌شده توی نوارِ تاریخ، وقتی صفحه باریک می‌شد و فاصله‌شون کم می‌شد، هالهٔ سبزِ دکمه‌ی فعال (که یه box-shadow با شعاعِ ۱۸px بود) می‌ریخت روی دکمه‌های کناری و رنگش «بیرون می‌زد» — شعاعِ هاله کم شد و فاصله‌ی بین آیتم‌ها کمی بیشتر شد.
- چینشِ دسکتاپِ «روتین من»: باکسِ «دوستان» و «آمار هفتگی» دیگه کنارِ هم نیستن — کاملاً زیرِ هم، عمودی (همون‌چیزی که چندبار خواسته شده بود).
- باکسِ «برنامه هفتگی» دیگه چسبیده به یه سمت نیست، وسطِ فضای دسکتاپ وسط‌چین می‌شه.
- بک‌گراندِ سنگین/تقریباً‌کدرِ پنلِ «اطلاعیه‌ها» برداشته شد، به‌جاش همون شیشه‌ی محوِ بقیه‌ی پاپ‌آپ‌های اپ رو گرفت.
- اگه لیستِ دوستان به هر دلیلی (خطای شبکه/سرور) لود نشه، دیگه برای همیشه «در حال بارگذاری…» نمی‌مونه — پیامِ «هنوز دوستی اضافه نکردی» رو نشون می‌ده.
- ساعتِ دیجیتال از منوی همبرگری کامل حذف شد؛ استریکِ روزهای پشت‌سرهم (با همون شعله‌ی چندسطحی و انیمیشن‌هاش) از منو به خودِ هدر (کنارِ زنگوله) منتقل شد.
- نوارِ قیمتِ لحظه‌ای صفحه‌ی ترید دیگه با هاورِ موس متوقف نمی‌شه (این باعث می‌شد به‌نظر برسه اسکرولِ بی‌درزش کار نمی‌کنه).
- صفحه‌های ترید/بدنسازی/رودمپ‌ها توی دسکتاپ دیگه توی همون ستونِ باریکِ وسط‌چینِ سایت گیر نیستن — سمتِ راستِ فضای باز می‌شینن، مثلِ صفحه‌ی ترید که قبلاً همین رفتار رو داشت (فقط بیشتر).
- لیبلِ «صفحه اصلی» توی منو به «روتین» تغییر کرد.

### چی تغییر کرد
- **باگِ واقعی پیدا و فیکس شد**: پاپ‌آپِ «دوستان» توی موبایل وسط صفحه نبود — کلاسِ `liquid-glass-panel` روی `.modal-panel` مقدارِ `position:fixed` رو با یه `position:relative` اشتباهی override می‌کرد، پس اصلاً fixed نبود و توی جریانِ عادیِ صفحه رندر می‌شد؛ حذف شد. علاوه‌براون، چون این پاپ‌آپ داخلِ `DashCard` (که `backdrop-blur` داره) رندر می‌شه، `backdrop-filter` رو کلاینت به‌عنوانِ containing-block برای عنصرِ fixed درنظر می‌گیره — با رندر کردنِ پاپ‌آپ توی یه پورتال (`createPortal` به `document.body`) این مشکل هم حل شد.
- نوارِ اسلایدِ روزها همیشه روزِ فعال (امروز/انتخاب‌شده) رو وسط نگه می‌داره، هم موقعِ لودِ اولیه هم موقعِ عوض‌شدنِ هفته.
- بک‌گراندِ لیکوئید گلسِ پاپ‌آپِ دوستان و فرمِ افزودنِ برنامه تیره‌تر شد که توپ‌های بک‌گراند مستقیم دیده نشن — دقیقاً حسِ بک‌گراندِ خودِ صفحه.
- فیلترِ برنامه‌ها: دکمه‌ی «هیچ‌کدام» حذف شد (فقط «همه» موند)، اسمِ برنامه سمت راست و سوییچِ انتخاب سمت چپ شد، و یه نوارِ جستجوی لیکوئید گلسِ گرد بالای لیست اضافه شد.
- توی فرمِ «افزودن برنامه»، هر ردیف الان می‌تونه چند روزِ هفته رو هم‌زمان انتخاب کنه (به‌جای اجبارِ یک روز)، همه‌ی فیلدها/دکمه‌ها لیکوئید گلس و گرد شدن، متنِ ساعتِ شروع/پایان وسط‌چین شد، دکمه‌های انتخابِ روز کاملاً گرد شدن، و دکمه‌ی «افزودن روز دیگر» یه دکمه‌ی مثبتِ راست‌چین با متن جلوی آیکون و بدونِ بک‌گراند شد.
- حلقه‌ی پیشرفتِ امروز یه باگِ رندرِ سایه داشت (سایه‌ی دراپ‌شادو توسطِ viewBoxِ خودِ SVG به شکلِ مربع کلیپ می‌شد) — با `overflow:visible` روی SVG فیکس شد.

### چی تغییر کرد
- **باگِ واقعی پیدا و فیکس شد**: چک‌باکسِ «برنامه‌های امروز» اصلاً مرز نداشت (نامرئی بود) — یه ریستِ سراسریِ دکمه‌ها روی صفحه‌ی داشبورد، `border:none` رو به‌جای صفر‌کردنِ فقط پهنا، استایلِ border رو هم none می‌کرد؛ طبق مشخصاتِ CSS با border-style:none پهنای واقعی همیشه صفر محاسبه می‌شه، حتی اگه یه کلاسِ Tailwind بعداً border-2 ست کنه. با border-style:solid صریح فیکس شد — این باگ می‌تونست هر دکمه‌ی دیگه‌ای با کلاسِ border هم بگیره.
- بج میزانِ اهمیت توی «برنامه‌های امروز» همیشه کنارِ اسمِ برنامه‌ست (نه زیرش، حتی توی موبایل).
- میزانِ اهمیت الان ۴ سطحه: کم/متوسط/زیاد/خیلی‌زیاد (به‌جای ۳ سطحِ قبلی).
- «افزودن برنامه» دیگه پاپ‌آپِ جستجو رو همزمان باز نمی‌کنه — یه فرمِ کاملاً جدا و مستقل با بک‌گراندِ لیکوئید گلس.
- روی هر برنامه‌ی «امروز»، سه‌نقطه یه منوی «ویرایش برنامه» / «حذف کامل برنامه» باز می‌کنه؛ ویرایش دقیقاً همون فرمِ قبلی‌ه. کلیک روی خودِ اسمِ برنامه فقط کارتِ نمایشی (بدون دکمه‌ی ویرایش/حذف) رو باز می‌کنه.
- بک‌گراندِ پاپ‌آپِ «دوستان» و فیلدِ جستجوی یوزرنیم لیکوئید گلس شدن (فیلد کاملاً گرد هم شد).
- دکمه‌ی «ارسال» حذف شد — همین‌که تایپ می‌کنی نتیجه‌ها زنده نشون داده می‌شن؛ کلیک روی هرکدوم بلافاصله درخواست می‌فرسته و «درخواست دادن» جلوی اسمش می‌مونه. طرفِ مقابل از بخشِ اطلاعیه‌ها (زنگوله‌ی هدر) قبول/رد می‌کنه.
- دوستانِ فعلی رو می‌شه فیوریت (ستاره) کرد؛ فیوریت‌ها بالای لیست میان.
- بک‌گراندِ «برنامه هفتگی» دیگه حباب‌های لیکوئید گلس نداره (فقط بلور ساده).

## نسخه: v1.6.0 (بازخورد دوم روی داشبورد ادغام‌شده)

### چی تغییر کرد
- حلقه‌ی پیشرفتِ امروز دوباره یک ردِ پس‌زمینه‌ی کم‌رنگ گرفت (نه پاک، فقط رد کامل پشتِ قسمتِ پرشده).
- نوارِ انتخاب تاریخ راست‌چین شد (شنبه راست‌ترین، جمعه چپ‌ترین) و فلش‌های قبلی/بعدی رو به بیرون اشاره می‌کنن، نه سمت لیست.
- دکمه‌ی «فیلتر» دیگه روشن/خاموش نیست — کلیکش یه پاپ‌آپ باز می‌کنه که هم فیلترِ میزان اهمیت هم لیستِ برنامه‌ها (با سوییچ، تیک‌بزن تا فقط همونا نشون داده بشن) توش هست؛ دراپ‌داونِ جدا و دکمه‌ی «این هفته» حذف شدن.
- دکمه‌ی «این هفته» به «تاریخچه» تبدیل شد: یه تقویمِ ماهانه باز می‌کنه که با کلیک روی هر روز، همون روز توی «برنامه‌های امروز» نشون داده می‌شه.
- دکمه‌ی جداگانه‌ی تاریخچه (کنار «برنامه هفتگی») و دکمه‌ی ویرایش/مداد حذف شدن — تنها راهِ باز کردنِ پنلِ افزودن/جستجوی برنامه، همون دکمه‌ی «افزودن برنامه» بالای صفحه‌ست.
- بخشِ «برنامه هفتگی» (پایینِ صفحه) روی دسکتاپ سمتِ راستِ فضای پهن می‌چسبه (نه وسط‌چین)، و بک‌گراندش لیکوئید گلس شد (همون افکتِ حباب‌های مایعِ بقیه‌ی اپ).
- کاربرهایی که با گوگل ثبت‌نام می‌کنن اصلاً یوزرنیم ندارن (فرم ثبت‌نام معمولی می‌گیره، ورود با گوگل نه) — تا الان هیچ راهی برای ست‌کردنش نبود، پس نه خودشون نه بقیه هیچ‌وقت با فیچر دوستان پیداشون نمی‌کردن. الان توی «پنل کاربری» اگه یوزرنیم نداشته باشی، فرم تنظیمش نشون داده می‌شه؛ اگه داشته باشی، دکمه‌ی «ویرایش» برای تغییرش هست.
- جستجوی یوزرنیم برای افزودن دوست حالا بدون حساسیت به بزرگ/کوچکیِ حروفه (هم‌راستا با همون قاعده‌ای که موقع ورود به حساب هست) — قبلاً باید دقیقاً همون حروف بزرگ/کوچکِ ثبت‌شده رو می‌زدی.
- `/dashboard` حذف شد؛ محتواش کامل با `/weekly` ادغام شد — این صفحه الان همون صفحه اصلی اپه (بالا: هدر+پیشرفت امروز، انتخاب تاریخ، برنامه‌های امروز، یادآوری‌ها، دوستان+آمار هفتگی؛ پایین: همون تایم‌لاین کامل «برنامه هفتگی» قبلی، دست‌نخورده).
- هیچ داده‌ی پیش‌فرض/آزمایشی‌ای توی این صفحه نیست — برنامه‌ها، یادآوری‌ها و دوستان همه از دیتای واقعی خودِ کاربر (سیستم واقعیِ `/weekly`: `CustomOccurrence`+`DailyEntry`) پر می‌شن.
- «افزودن برنامه» توی داشبورد، همون پاپ‌آپ واقعیِ افزودن برنامه‌ی `/weekly` رو باز می‌کنه؛ چک‌باکس هر برنامه فقط برای «امروز» فعاله و مستقیم `DailyEntry` رو آپدیت می‌کنه.
- تگ‌های دسته‌بندی قبلی («رشد فردی» و…) با سه تگِ «میزان اهمیت» (خیلی مهم/مهم/عادی) جایگزین شدن؛ این تگ روی هر برنامه قابل تنظیمه (هم موقع افزودن، هم موقع ویرایش).
- «یادآوری‌ها» حالا برنامه‌های همین هفته با تگ «خیلی مهم» یا «مهم» رو نشون می‌ده (نه یه لیست جدا).
- فیچر «دوستان» واقعی ساخته شد: مدل `Friendship` جدید، درخواست دوستی با یوزرنیم، قبول/رد، و درصد پیشرفتِ امروزِ هر دوست — با API واقعی (`/api/friends`, `/api/friends/requests`, `/api/friends/[id]`).
- نوار انتخاب تاریخ داشبورد الان واقعی و کاربردیه: فلش‌های قبلی/بعدیِ هفته (فقط دسکتاپ)، اسکرول با انگشت توی موبایل بدون اسکرول‌بارِ مرئی، و دکمه‌های «امروز»/«این‌هفته»/«فیلتر» + دراپ‌داون فیلتر بر اساس میزان اهمیت، همه واقعاً کار می‌کنن.
- باگ تغییر تم (روشن/تیره) روی دکمه‌های پرشده‌ی داشبورد (تاریخ فعال، دکمه‌ی فیلترِ فعال، چک‌باکسِ تیک‌خورده) برطرف شد — یه استایل سراسریِ سایت با اسپسیفیسیتی بالاتر از کلاس‌های Tailwind، پس‌زمینه‌شون رو توی تمِ روشن می‌شکست.
- کارتِ «دوستان» برای مهمان (لاگین‌نکرده) دیگه بی‌نهایت «در حال بارگذاری…» نمی‌مونه؛ پیام «برای استفاده از بخش دوستان اول وارد حساب بشو» نشون داده می‌شه.
- `/dashboard` حالا از منوی همبرگری هم قابل‌دسترسیه (لینک جدید).
- بک‌گراند سیاهِ صفحه کامل پاک شد — بک‌گراند خودِ اپ (canvas ذرات/گرادیان) از زیرش دیده می‌شه؛ کارت‌ها همچنان بک‌گراند تیره‌ی خودشون رو دارن.
- بهینه‌سازی موبایل: فونت‌ها و فاصله‌ها همه‌جا کوچیک‌تر و جمع‌وجورتر شدن (هدر، نوار تاریخ، کارت‌ها، بج‌ها).
- «اعضای تیم» شد «دوستان»؛ ترتیب سایدبار عوض شد: یادآوری‌ها → دوستان → آمار هفتگی.
- توی هدر کارتِ «برنامه‌های امروز»، جای عنوان و دکمه‌ی «افزودن برنامه» عوض شد.
- توی هدر کارتِ «دوستان»، جای عنوان و «مشاهده همه» عوض شد.
- هر برنامه حالا ساعت شروع و پایان رو نشون می‌ده (نه فقط شروع)؛ با زدنِ چک‌باکس، محتوای همون ردیف بلور می‌شه.
- نمودار «آمار هفتگی»: درصدِ هر روز بالای میله‌ش نوشته می‌شه؛ ترتیبِ روزهای هفته هم راست‌چینِ طبیعی شد (شنبه راست، جمعه چپ).
- کارتِ «حال امروزت چطوره؟» کامل حذف شد.

## نسخه: v1.4.5 (پیش‌نمایش داشبورد جدید در /dashboard)

### چی اضافه شد
- یک صفحه‌ی جدیدِ کاملاً مستقل و پیکسل‌محور در `/dashboard` — کلونِ یک طرحِ
  داده‌شده («روتین من»: هدر + حلقه‌ی پیشرفت، نوار انتخاب تاریخ، فیلترها،
  سایدبار اعضای تیم/یادآوری‌ها/آمار هفتگی/حال روزانه، و لیست برنامه‌های
  امروز)، دسکتاپ و موبایل ریسپانسیو.
- این صفحه با Tailwind + کامپوننت‌های سبکِ shadcn (دست‌ساز، بدون وابستگی به
  Radix) + Framer Motion ساخته شده و دیتاش mock/نمایشیه — به بک‌اند یا
  مدل‌های واقعیِ اپ (که «اعضای تیم»/«حال روزانه» توش وجود نداره) وصل نیست؛
  فقط برای تأیید ظاهر قبل از هر تصمیمِ ادغام با بقیه‌ی اپه.
- کلیدهای جدید: `tailwind.config.ts` رنگ‌های `dash-*`، `lib/utils.ts` (کمک‌تابع `cn`)، `lib/dashboardMockData.ts`.

## نسخه: v1.4.4 (رفع باگ جدول مقایسه پلن‌ها، نوارهای بی‌درز، درباره ما)

### چی تغییر کرد
- باگ جدول «مقایسه پلن‌ها» در لندینگ رفع شد: ردیف‌های پیش‌نمایشِ تارِ
  «به‌زودی» قبلاً یک ردیفِ flex با gap ثابت بودن که ستون‌هاش با ستون‌های
  واقعیِ جدولِ بالاش هم‌تراز نمی‌شد؛ حالا با همون colgroup جدولِ اصلی رندر
  می‌شن و چک/ضربدرها دقیقاً زیرِ ستونِ پلنِ خودشون می‌شینن.
- نوار «چرا ما؟» و نوار قیمتِ فارکس هر دو یک هوکِ مشترک (`useSeamlessMarquee`)
  گرفتن که پایه رو تا یک حداقلِ امن تکرار می‌کنه — حتی با تعداد آیتمِ کم
  (مثلاً کاربر فقط ۱ نماد انتخاب کرده)، دیگه وسطِ چرخه فضای خالی دیده نمی‌شه.
- سرعتِ چرخشِ نوار قیمتِ فارکس کمتر شد.
- «درباره من» شد «درباره ما»؛ اسم گروه از Araksis Group به Arion Group تغییر کرد.

## نسخه: v1.4.3 (فشرده‌سازی ترید، آمارهای قابل‌تنظیم، بازطراحی بخش کالری)

### چی تغییر کرد
- بخش ترید جمع‌وجورتر شد: دکمه‌ها و کارت‌های آماری کوچیک‌تر، فاصله‌ها کمتر.
- کاربر از پنل کاربری انتخاب می‌کنه کدوم آمارهای صفحه‌ی ترید نشون داده بشن —
  با یک سوییچ لیکوئید گلسِ سبک iOS (`ToggleSwitch.tsx`) به‌ازای هر آمار.
- فیلد «جفت‌ارز» توی فرم ثبت معامله حالا جست‌وجو-و-انتخابه (`lib/tradePairs.ts`)،
  نه تایپ آزاد؛ و اسم‌های تکراری فارکس توی انتخاب‌گر بازارها رفع شد.
- واحد اندازه‌گیری غذا (گرم/قاشق/پیمانه) از ۴ دکمه‌ی کنار هم به یک دکمه‌ی تکی
  با منوی بازشو جمع شد؛ باکس «مقدار» هم کوچیک و وسط‌چین شد.
- کالری امروز و سهم هر وعده حالا به‌شکل حلقه‌ی دایره‌ای (`ProgressRing.tsx`)
  نشون داده می‌شن، نه نوار خطی. دکمه‌ی «تغییر برنامه» جای بهتری گرفت و دکمه‌ی
  حذفِ آیتم غذایی به یک دایره‌ی آیکونیِ تمیز تبدیل شد.
- ساعت بیداری/خواب توی پنل کاربری وسط‌چین شد.
- دیتابیس داخلیِ غذاها (`lib/foodSeed.ts`) از ۲۴ آیتم به نزدیک ۲۵۰ آیتم
  گسترش پیدا کرد: غذاهای خانگی ایرانی، محصولات رایج سوپرمارکتی، میوه/سبزی/
  خشکبار، و مهم‌ترین غذاهای بین‌المللی.

## نسخه: v1.4.2 (ژورنال ترید: هدف ماهانه، پاپ‌آپ ثبت معامله، جابه‌جایی تنظیمات)

### چی تغییر کرد
- ساعت و استریک از هدر به داخل منوی همبرگری منتقل شد.
- محاسبه‌ی سود/زیان همه‌جا دقیقاً با یک رقم اعشار نمایش داده می‌شه (`computeTradePnl`).
- سوییچ تقویم شمسی/میلادی ژورنال ترید از صفحه‌ی ترید به پنل کاربری منتقل شد.
- «ثبت معامله جدید» به‌جای اینکه همیشه وسط صفحه باشه، حالا با کلیک دکمه به‌صورت پاپ‌آپ باز می‌شه.
- کاربر می‌تونه یک هدف سود ماهانه توی پنل کاربری تنظیم کنه؛ پیشرفتش به‌صورت یک آمار دایره‌ای (halo/ring) بالای صفحه‌ی ترید نشون داده می‌شه.

## نسخه: v1.4.1 (رفع باگ: CSP باعث می‌شد dev هیدریت نشه)

### رفع باگ فوری
صفحه فقط مثل یه سند ثابت (بدون تعامل) لود می‌شد چون CSP سخت‌گیرانه‌ای که
توی v1.4 اضافه شده بود، `eval` رو بلاک می‌کرد — و Next.js dev mode برای
Hot Reload به eval نیاز داره. الان هدرهای امنیتی فقط روی production
(`npm run build && npm run start`) اعمال می‌شن، نه روی `npm run dev`.

## نسخه: v1.4 (سخت‌سازی امنیتی)

## چی اضافه شد نسبت به v1.3

### محدودسازی نرخ درخواست (Rate Limiting)
`lib/rateLimit.ts` — یک محدودکننده ساده در-حافظه، وصل‌شده به:
- **ثبت‌نام**: حداکثر ۵ تلاش در ۱۰ دقیقه به‌ازای هر IP
- **ورود**: حداکثر ۸ تلاش ناموفق در ۱۰ دقیقه، هم به‌ازای IP هم به‌ازای همون
  شناسه ورود (یوزرنیم/موبایل/ایمیل) — جلوگیری از brute-force روی رمز عبور
- **ساخت رودمپ با AI**: حداکثر ۱۰ در ساعت به‌ازای هر کاربر (چون هر بار
  یعنی هزینه واقعی روی Claude API)

⚠️ محدودیت مهم این پیاده‌سازی: فقط در سطح یک instance کار می‌کنه. اگه
بعداً اپ روی چند سرور/serverless پخش بشه، باید با یک store مشترک (مثل
Redis) جایگزین بشه — نکته‌اش توی خود فایل هم هست.

### اعتبارسنجی ورودی‌ها
`lib/validate.ts` — فرمت ایمیل، فرمت شماره موبایل ایران، فرمت یوزرنیم
(فقط حروف انگلیسی/عدد/آندرلاین)، سیاست رمز عبور (حداقل ۸ کاراکتر + حداقل
یک حرف و یک عدد)، و محدودکردن طول رشته‌های آزاد کاربر (اسم، یادداشت،
موضوع رودمپ و…). این‌ها روی مسیرهای ثبت‌نام، ترید، کالری، ورزش، و
رودمپ‌ساز اعمال شدن.

### پیام خطای عمومی در ثبت‌نام
قبلاً اگه شماره یا یوزرنیم تکراری بود، پیام دقیق می‌گفت کدوم؛ الان پیام
عمداً کلیه («امکان ثبت‌نام با این اطلاعات وجود ندارد») تا نشه با امتحان
شماره‌های مختلف فهمید کدوم شماره قبلاً ثبت‌نام کرده (User Enumeration).

### هدرهای امنیتی HTTP
`next.config.js` حالا روی هر پاسخ این‌ها رو می‌فرسته:
`X-Frame-Options: DENY` (جلوگیری از clickjacking)،
`Content-Security-Policy`، `Strict-Transport-Security` (HSTS)،
`X-Content-Type-Options: nosniff`، `Referrer-Policy`، `Permissions-Policy`،
و حذف هدر `X-Powered-By` (که استک فنی رو لو می‌داد).

### چیزهایی که از قبل امن بودن (و همچنان هستن)
- همه کوئری‌ها با Prisma پارامتری‌شدن — بدون هیچ raw SQL، پس در برابر
  SQL Injection ایمنیم.
- هیچ‌جا `dangerouslySetInnerHTML` یا `eval` استفاده نشده — React خودش
  محتوای متنی رو escape می‌کنه، پس در برابر XSS ذخیره‌شده ایمنیم.
- همه روت‌های حذف/ویرایش با `where:{id, userId}` کوئری می‌زنن، نه فقط
  `{id}` — یعنی کاربر A نمی‌تونه داده کاربر B رو با حدس‌زدن ID دستکاری کنه (IDOR).
- رمزهای عبور با bcrypt (۱۲ round) هش می‌شن، هیچ‌جا متن‌خام ذخیره نمی‌شه.
- session با JWT امضاشده (NEXTAUTH_SECRET) کار می‌کنه؛ `isSuperAdmin` از
  سمت کلاینت قابل جعل نیست چون از دیتابیس میاد و توی JWT امضاشده قرار می‌گیره.

## هنوز مونده (برای قبل از لانچ واقعی)
- **حتماً رمز حساب سوپریوزر پیش‌فرض (`mohammadreza`) رو عوض کن** قبل از production
- Rate limiting با Redis (نه در-حافظه) اگه چند سرور شدید
- CSP سخت‌گیرانه‌تر با nonce به‌جای `'unsafe-inline'` روی script-src
- 2FA برای حساب ادمین
- لاگ‌گیری و مانیتورینگ تلاش‌های ورود مشکوک
- تست نفوذ (Penetration Testing) قبل از لانچ عمومی
- بک‌آپ خودکار دیتابیس

## اجرا
```bash
npm install
cp .env.example .env
npx prisma migrate dev --name init
npx prisma db seed
npm run dev
```
