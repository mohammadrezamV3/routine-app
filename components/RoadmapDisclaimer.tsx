import { AlertTriangle } from "lucide-react";

/**
 * هشدارِ ثابتِ پایینِ صفحه‌های رودمپ.
 *
 * مسیرها را یک مدلِ زبانی می‌سازد؛ نامِ کتاب، شماره‌ی درس و ترتیبِ مراحل
 * می‌توانند اشتباه باشند. این یادآوری باید همیشه دیده شود، نه فقط یک بار
 * موقعِ ساخت — برای همین یک فوتر است، نه یک پیامِ گذرا.
 */
export function RoadmapDisclaimer() {
  return (
    <div className="rm-disclaimer" role="note">
      <AlertTriangle size={14} />
      <span>
        هوش مصنوعی امکان اشتباه دارد — قبل از انجام، خودت هم درباره‌ی منابع و
        ترتیبِ مراحل تحقیق کن.
      </span>
    </div>
  );
}
