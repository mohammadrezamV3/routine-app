"use client";

import { useLockBodyScroll } from "@/lib/useLockBodyScroll";

// یه کامپوننتِ خیلی کوچیک به‌جای صدازدنِ مستقیمِ هوک — چون اکثرِ پاپ‌آپ‌های
// این اپ به‌صورتِ شرطی رندر می‌شن (`{open && <...>}`) داخلِ یه کامپوننتِ
// بزرگ‌تر که خودش همیشه مانته (مثلاً DashFriendsCard)، نمی‌شه هوک رو مستقیم
// اونجا صدا زد (هوک‌ها نباید شرطی باشن). همین المنت رو به‌عنوانِ اولین فرزندِ
// هر پاپ‌آپ می‌ذاریم — با مانت/آن‌مانت‌شدنِ خودِ پاپ‌آپ، قفل/بازشدنِ اسکرول
// خودکار انجام می‌شه.
export function LockBodyScroll() {
  useLockBodyScroll();
  return null;
}
